---
type: JS Module
title: fix-jscpd_config.mjs
resource: plugins/lang-js/rules/js/jscpd_config/fix-jscpd_config.mjs
docgen:
  crc: 2c7c97a5
  model: openai-codex/gpt-5.4-mini
  tier: cloud-min
  score: 100
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

`patterns` описує поведінку T0-autofix для правила `js/jscpd_config`: воно або створює відсутній `.jscpd.json` із канонічного шаблону правила, або доповнює наявний файл лише канонічними полями, залишаючи локальні налаштування без змін. Це забезпечує узгоджений базовий конфіг для `jscpd` як ціль для автоматичного вирівнювання.

## Поведінка

1. `patterns` визначає набір декларативних правил для T0-autofix у концерні `js/jscpd_config`, щоб або створити відсутній `.jscpd.json` з канонічного шаблону, або доповнити наявний файл лише дозволеними канонічними полями без перезапису локальних налаштувань.
2. Коли правило застосовується, результатом є один шаблонний запис для `.jscpd.json`, який спирається на конфіг `.jscpd.json` як цільовий артефакт для вирівнювання.
3. `patterns` не виконує власних операцій запису та не втручається в інші файли чи сторонні сховища; його роль — описати, що саме має бути виправлено в межах цього конфігураційного контракту.

## Публічний API

- patterns — Фікс-патерни концерну: один template-deep-merge запис для `.jscpd.json`.

## Гарантії поведінки

- Власних операцій запису (ФС/БД) у файлі немає; виклики імпортованих модулів можуть писати.
