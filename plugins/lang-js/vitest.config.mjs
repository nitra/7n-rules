/** Vitest-конфіг плагіна lang-js: env-канон ядра + include лише тестів плагіна. */
import { createPluginVitestConfig } from '../vitest.config.shared.mjs'

/** Повертає canonical Vitest config для lang-js plugin. */
export default createPluginVitestConfig()
