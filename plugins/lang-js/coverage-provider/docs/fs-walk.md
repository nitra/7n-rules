---
type: JS Module
title: fs-walk.mjs
resource: plugins/lang-js/coverage-provider/fs-walk.mjs
docgen:
  crc: 99fb0524
  model: openai-codex/gpt-5.4-mini
  tier: cloud-min
  score: 100
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

`walk` — публічна функція для спільного рекурсивного обходу workspace-дерева, яка передає в колбек лише файли, потрібні root-детекторам `bun-native.mjs` і `storybook.mjs`. Вона відсікає dot-теки та службові директорії на кшталт `.git` і `node_modules`, щоб у пошук не потрапляв службовий шум від workspace-структури. Обхід працює fail-safe: помилки перехоплюються всередині, і назовні винятки не пробиваються.

## Поведінка

1. `walk` обходить дерево каталогів рекурсивно й передає в колбек кожен файл, який входить до робочого простору.
2. Під час обходу вона свідомо пропускає dot-теки та службові директорії, зокрема `.git` і `node_modules`, а також інші build- і report-артефакти.
3. Якщо каталог недоступний або його неможливо прочитати, `walk` тихо завершує цю гілку без помилки назовні.
4. Мета `walk` — дати root-детекторам чистий перелік релевантних файлів без шуму від службових і технічних папок.

## Публічний API

- walk — Рекурсивний обхід каталогу з відсіюванням службових директорій (dot-теки, IGNORE_DIRS).

## Гарантії поведінки

- Власних операцій запису (ФС/БД) у файлі немає; виклики імпортованих модулів можуть писати.
- Перехоплює помилки і не пропускає винятків назовні (fail-safe).
- Свідомо пропускає шляхи: `.git`, `node_modules`.
