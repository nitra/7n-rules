/** Vitest-конфіг плагіна lang-php: env-канон ядра + include лише тестів плагіна. */
import { createPluginVitestConfig } from '../vitest.config.shared.mjs'

/** Повертає canonical Vitest config для lang-php plugin. */
export default createPluginVitestConfig()
