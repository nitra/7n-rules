{
  "$schema": "https://unpkg.com/@7n/rules/schemas/concern.json",
  "fixability": "config",
  "policy": {
    "files": {
      "single": ".github/workflows/lint-docker.yml",
      "required": true
    },
    "missingMessage": ".github/workflows/lint-docker.yml не існує — створи за каноном правила (mixin @7n/rules-ci-github)"
  }
}
