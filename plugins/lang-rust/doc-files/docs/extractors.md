---
type: JS Module
title: extractors.mjs
resource: plugins/lang-rust/doc-files/extractors.mjs
docgen:
  crc: 383fdf08
  model: openai-codex/gpt-5.4-mini
  tier: cloud-min
  score: 100
  issues: judge-refine:kept-original,judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Описує публічний API файла через `extractFactsRust` і фіксує, що огляд побудовано з повторним використанням уже зібраних фактів у межах одного прогону.

## Поведінка

1. Збирає факт-лист для одного `.rs`-файлу як читабельний огляд його публічного API, імпортів і верхньорівневого опису.
2. Додає module-level doc із початку файла, якщо він є, щоб зберегти коротке призначення модуля.
3. Визначає публічні елементи файла, включно з тими, що стають доступними через exposure-атрибути, і формує по кожному назву, роль і короткий опис.
4. Окремо групує `use`-імпорти за походженням на стандартні, зовнішні та внутрішні, щоб швидко бачити залежності модуля.
5. Фіксує ознаку наявності `match`-обробки з `Err`-гілкою, щоб у фактах відобразити місце, де код явно працює з помилками.
6. Повертає результат без службових проміжних даних і без підтримуваних ним шляхів, які свідомо не покриваються цим факт-листом.

## Публічний API

- extractFactsRust — Витягує факт-лист для `.rs` файлу.

## Сценарії використання

- `plugins/lang-rust/doc-files/tests/extractors.test.mjs` (Rust (.rs) — extractFactsRust) — lang=rs; pub fn → exports; #[tauri::command] fn → exports навіть без pub; pub struct і pub enum → exports; markers.returnsFalsyOnFail: -> Result<; ще 5

## Гарантії поведінки

- Власних операцій запису (ФС/БД) у файлі немає; виклики імпортованих модулів можуть писати.
- Кешує результати в межах одного прогону.
