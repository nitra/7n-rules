/** Vitest-конфіг плагіна lang-rust: env-канон ядра + include лише тестів плагіна. */
import { createPluginVitestConfig } from '../vitest.config.shared.mjs'

/** Повертає canonical Vitest config для lang-rust plugin. */
export default createPluginVitestConfig()
