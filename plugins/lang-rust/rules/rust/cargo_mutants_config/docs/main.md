---
type: JS Module
title: main.mjs
resource: plugins/lang-rust/rules/rust/cargo_mutants_config/main.mjs
docgen:
  crc: f0141739
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  score: 95
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Огляд: Файл перевіряє конфігурацію проекту, зокрема налаштування для мови Rust. Він забезпечує, що у директоріях, що містять файли маніфестів Cargo, існує файл `mutants.toml`, копіюючи базовий файл, якщо він відсутній.

Поведінка:

1. Перевіряється, чи активована логіка для мови Rust.
2. Ідентифікуються всі файли маніфестів Cargo.
3. Перевіряється наявність базового файлу `mutants.toml.baseline`.
4. Для кожного знайденого файлу маніфесту Cargo, якщо файл `.cargo/mutants.toml` відсутній, він створюється, копіюючи вміст базового файлу. **[test.mdc]**
5. Процес завершується з кодом 0, якщо всі перевірки виконані або відповідні файли не виявлені/немає порушень; з кодом 1 у разі виявлення порушень.

## Поведінка

Поведінка:

1. Перевіряється, увімкнено чи ні правило для мови Rust.
2. З'ясовуються всі файли маніфестів Cargo.
3. Перевіряється наявність канонічного базового файлу `mutants.toml.baseline`.
4. Для кожного знайденого файлу маніфесту Cargo, якщо файл `.cargo/mutants.toml` відсутній, він створюється, копіюючи вміст базового файлу.
5. Процес завершується з кодом 0, якщо всі перевірки пройдені або якщо відповідні файли не знайдено/немає порушень; з кодом 1 у разі виявлення порушень.

## Гарантії поведінки

- (специфічних машинно-виведених гарантій немає)
