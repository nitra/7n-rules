/** Vitest-конфіг плагіна lang-python: env-канон ядра + include лише тестів плагіна. */
import { createPluginVitestConfig } from '../vitest.config.shared.mjs'

/** Повертає canonical Vitest config для lang-python plugin. */
export default createPluginVitestConfig()
