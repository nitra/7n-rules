---
bump: patch
section: Changed
---
Гейт правила `python` переїхав з виконуваного `python/applies/main.mjs` у декларативне поле `main.json:applies` (`{ "pathExists": "pyproject.toml" }`). Умова застосовності не змінилась — правило й далі вмикається за наявності `pyproject.toml` у корені; змінилась лише форма її оголошення, завдяки чому гейт читає й нативний Rust-CLI без запуску node.
