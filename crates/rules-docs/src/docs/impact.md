---
type: Rust Module
title: impact.rs
resource: crates/rules-docs/src/impact.rs
docgen:
  crc: d6eea4ce
  model: local-openai/gemma-4-26b-a4b-it
  tier: local-min
  score: 80
---

## Огляд

Privacy-safe зріз впливу для однієї теми — порт `impact.mjs`.  Зріз використовує приватні одиниці ЛИШЕ як внутрішні вершини обходу: їхні імена й ідентифікатори назовні не повертаються. Натомість зачеплені файли, тести, конфіги та зовнішні контракти лишаються доступними — саме вони потрібні для плану змін.

## Публічний API

- Failure — Структурована відмова.
- PublicTopic — Безпечна проєкція теми — без symbol-level якорів.
- Contract — Зовнішній контракт, зачеплений темою.
- ImpactSlice — Зріз впливу.
- create_impact_slice — Повертає зріз впливу домену за ID теми або її alias — порт `createImpactSlice`.  # Errors Граф без домену, невідома тема або тема з ІНШОГО домену: останнє — окремий код, бо це не «не знайдено», а спроба перетнути межу домену.

## Гарантії поведінки

- Власних операцій запису (ФС/БД) у файлі немає; виклики імпортованих модулів можуть писати.
- Містить локальні fail-safe гілки; інші помилки можуть поширюватися назовні.
- Деякі локальні fail-safe гілки повертають порожнє значення (напр. `null`) замість винятку.
