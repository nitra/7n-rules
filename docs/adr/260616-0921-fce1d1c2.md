---
session: fce1d1c2-4217-4a3a-bb26-649747c9653b
captured: 2026-06-16T09:21:11+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/fce1d1c2-4217-4a3a-bb26-649747c9653b.jsonl
---

Дозволи на запис спеки не надано — нічого, основний вердикт зафіксований у цій розмові.

## Підсумок по експерименту та наступних кроках

**Rust vs JS — вердикт: JS лишаємо.** Node-wrapper (`bin/mt.js`) завжди запускається (~35ms), тому Rust-шлях: Node(35) + spawn-Rust(10) + git(11) = ~56ms проти Node(35) + JS(17) + git(11) = **63ms** — різниця 7ms, а складність набагато вища. `mt worktree` **вже є** у JS (`npm/lib/commands/worktree.mjs`), треба лише вирівняти його до контракту.

**Worktree — ефемерний** (`remove` видаляє гілку — наявна mt-поведінка лишається).

**Дельта до контракту (що зробити в mt):**
- `add` → `create` (rename)
- `.worktrees/<sanit>.md` → `.worktrees/.meta/<sanit>.md`
- Додати `firstFreeBranch` (колізія → base2/base3)
- Покращити dirty-notice (≤10 файлів перелік)
- Нові команди: `prune`, `inventory` (JSON)
- Потім cursor-міграція: видалити `worktree-cli.mjs`, перемкнути снипет/правило на `mt worktree`

Починаємо реалізацію в mt. Хочеш, щоб я почав прямо зараз із вирівнювання `npm/lib/commands/worktree.mjs` у `/Users/vitalii/www/nitra/mt`?
