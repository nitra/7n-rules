---
session: bb3047db-5332-4649-a713-3f1cde68927a
captured: 2026-05-29T22:33:34+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/bb3047db-5332-4649-a713-3f1cde68927a.jsonl
---

[thinking]
This transcript shows a software engineering session where the user runs `/n-coverage-fix` - an automated skill to improve mutation test coverage. Let me analyze the key design decisions made:

1. **Decision about manual mutation testing vs waiting for Stryker**: When Stryker was hanging for 54+ minutes, the agent decided to manually test each mutant by temporarily replacing code with the mutant variant and running targeted tests, rather than waiting for Stryker to complete.

2. **Decision about CHANGELOG format**: The `# [1.30.0]` heading level was wrong (should be `##`), blocking coverage. Fixed as a prerequisite.

3. **Decision about killing old processes**: Two old `bun -e fixSurvivedMutants` processes (90242, 91321) from Wednesday were consuming 3000+ minutes of CPU each in an infinite loop. The agent asked the user and killed them.

4. **Decision about `auto-rules.test.mjs` bug**: Fixed a pre-existing bug where `root: process.cwd()` should be `root: dir` in a temp-dir test.

5. **Decision about version bump**: Changed npm version from 1.30.0 to 1.30.1 because tests were added to the npm package.

The main interesting design decisions are:
1. Manual mutant verification pattern (L189 `opts.fix` gate, L211 `{ fix: false }` default)
2. The `auto-rules.test.mjs` bug fix (using project CWD instead of isolated temp dir)
3. The approach to handle Stryker timeout - manual inspection instead of waiting

Let me write ADRs for the meaningful decisions.
</thinking>

## ADR Ручна перевірка мутантів замість очікування Stryker

## Context and Problem Statement
Stryker завис після 54+ хвилин виконання (143 мутанти в `npm/`); вихідний файл `be73rjpor.output` залишався порожнім, а CPU завантажували 9 `vitest` workers. Потрібно було встановити, чи вбивають нові тести 4 survived мутанти у `coverage.mjs` (L189 `if (opts.fix)`, L211 `{ fix: false }`), не чекаючи наступного успішного `bun run coverage`.

## Considered Options
* Ручна підстановка мутанта у джерело → `bunx vitest run … -t "…"` → відновлення
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Ручна підстановка мутанта у джерело", because Stryker завис і не генерував оновлений `mutation.json`; ручна ітерація по 4 мутантах займає ~2 хвилини замість 54+, а кожен варіант перевіряється точно тим самим тестом, що і Stryker.

### Consequences
* Good, because transcript фіксує очікувану користь: усі 4 мутанти (`if(true)`, `if(false)`, `{}`, `fix:true`) вловлені новими тестами — 57 passed після відновлення джерела.
* Bad, because ручний підхід не оновлює `incremental.json` Stryker, тому Score у `COVERAGE.md` залишився 93.62% (132/141) і не відобразив нових убитих мутантів.

## More Information
Мутанти L189–L211 у `npm/rules/test/coverage/coverage.mjs`; нові тести у `npm/rules/test/coverage/tests/coverage.test.mjs` describe-блоки `"opts.fix gate"` і `"2-ї стрілки"`; вбивство процесів: `kill 90242 91321` (старі `bun -e fixSurvivedMutants`, 3000+ хв CPU, PID з Wed09AM); кінцевий коміт `d42384b`.

---

## ADR Виправлення ізоляції тесту auto-rules (root: dir замість process.cwd())

## Context and Problem Statement
Тест `'AUTO_RULE_DEPENDENCIES: disable-rules vue → image-avif теж не додається'` у `npm/scripts/tests/auto-rules.test.mjs` (L275) викликав `detectAutoRules({ root: process.cwd(), … })` замість `{ root: dir, … }`, де `dir` — ізольована тимчасова директорія, створена `withTmpDir`. Це спричиняло падіння Stryker на стадії initial dry run.

## Considered Options
* Замінити `process.cwd()` на `dir` за аналогією з сусіднім тестом (L300)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Замінити `process.cwd()` на `dir`", because сусідній тест (L300) правильно використовує `dir`; передача `process.cwd()` означає, що тест залежить від стану реального репозиторію замість ізольованої tmp-директорії.

### Consequences
* Good, because transcript фіксує очікувану користь: `bunx vitest run scripts/tests/auto-rules.test.mjs -t "disable-rules vue"` → 1 passed після виправлення; Stryker отримав змогу запуститися.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Файл `npm/scripts/tests/auto-rules.test.mjs`, рядок 282 (`root: process.cwd()` → `root: dir`); команда верифікації: `bunx vitest run scripts/tests/auto-rules.test.mjs -t "disable-rules vue"`.

---

## ADR Усунення залежності між рівнями заголовків CHANGELOG і перевіркою версій

## Context and Problem Statement
Інтеграційний тест `check-* на реальному репозиторії` (`tests/integration-repo-checks.test.mjs:63`) падав із повідомленням `"npm/CHANGELOG.md: перша секція [1.30.0] не збігається з npm/package.json version"`. Причина: запис `[1.30.0]` у `npm/CHANGELOG.md` мав рівень `#` (h1) замість `##` (h2), тому парсер `checkChangelog` не розпізнав його як першу секцію релізу.

## Considered Options
* Виправити рівень заголовку: `# [1.30.0]` → `## [1.30.0]`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Виправити рівень заголовку `## [1.30.0]`", because формат Keep a Changelog вимагає `##` для секцій версій, а `#` — лише для заголовку `# Changelog`.

### Consequences
* Good, because transcript фіксує очікувану користь: після правки `"npm/CHANGELOG.md: перша секція [1.30.0] збігається з npm/package.json"` — тест позеленів.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Файл `npm/CHANGELOG.md`, рядок 6; перевірка: `check-changelog` через `npx @nitra/cursor check changelog`; правило `n-changelog.mdc`.
