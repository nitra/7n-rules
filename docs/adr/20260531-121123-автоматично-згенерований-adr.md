---
session: 09dcb1bc-929b-4d88-90b0-c0611bed3d2f
captured: 2026-05-31T12:11:23+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/09dcb1bc-929b-4d88-90b0-c0611bed3d2f.jsonl
---

Готово. `main` запушено (`b4d50d6..7b07bcc` — 12 комітів включно зі Spec B).

**Підсумок Spec B:**
- 9 задач виконано субагентами (TDD), ff-merged у `main`, worktree прибрано
- Ключове: `auto-rules.mjs` переписано на meta-інтерпретатор, 33 `meta.json` замінюють 29 `auto.md`, tauri автодетект увімкнено вперше
- Усі 148 test files ✅, push на origin

CI підхопить change-файли (`bump: minor` за Spec B + `bump: patch` за worktree-skill fix) і зробить release автоматично.

---

## ADR Автоматично Згенерований ADR

```markdown
## ADR Дата-орієнтований механізм активації правил через `meta.json`

## Context and Problem Statement
У пакеті `@nitra/cursor` авто-детект правил (які правила активувати в конкретному репо) був реалізований хардкодом у `auto-rules.mjs`: масиви `AUTO_RULE_ORDER`, `AUTO_RULE_DEPENDENCIES` і набір ручних JS-перевірок `autoRuleChecks`. Зміна умов активації одного правила вимагала модифікації коду CLI; нове правило без рядка в масиві просто не активувалося. Паралельно скіли вже мали декларативний `meta.json` — правила відставали.

## Considered Options
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "data-driven `meta.json` з 4-формами `auto` + реєстр предикатів", because user визначив підхід G1 (повний data-driven) під час brainstorming, а аналіз Explore-агента підтвердив, що хардкод охоплює 4 різних типи умов, які природно кодуються декларативно.

### Consequences
* Good, because transcript фіксує очікувану користь: додавання/зміна умов активації правила тепер — правка `meta.json`, без змін CLI-коду; `AUTO_RULE_ORDER` і `AUTO_RULE_DEPENDENCIES` виводяться з даних.
* Good, because tauri-правило, яке мало `auto.md` але не було підключено до детекту (dead code), тепер активується автоматично.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- `npm/scripts/auto-rules.mjs` — переписане ядро (meta-інтерпретатор)
- `npm/scripts/lib/rule-meta.mjs` — парсер 4 форм: `"завжди"`, `["rule"]`, `{glob}`, `{predicate,arg?}`
- `npm/scripts/lib/rule-predicates.mjs` — реєстр 6 предикатів: `repoUrlMarker`, `gqlTaggedTemplate`, `hasuraConfigMarker`, `depInAnyPackageJson`, `jsBunDbSignal`, `nestedPackageWithoutVite`
- `npm/schemas/rule-meta.json` — JSON-схема для валідації
- 33 `npm/rules/*/meta.json` (glob-форма для Type A, always для Type B, deps для Type C, predicate для Type D)
- 29 `auto.md` видалено
- Spec: `docs/superpowers/specs/2026-05-31-rule-meta-json-design.md`
- Plan: `docs/superpowers/plans/2026-05-31-rule-meta-json.md`
```
