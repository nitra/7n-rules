---
session: 38aa0305-a12b-4078-9085-ce03884efdd6
captured: 2026-06-06T15:45:59+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/38aa0305-a12b-4078-9085-ce03884efdd6.jsonl
---

Все чисто. Підсумок змін:

**`npx @nitra/cursor fix` тепер = автономний оркестратор:**
- Без `--json` → convergence-loop: T0-auto → LLM-worker (haiku→sonnet) → recheck, max 3 ітерації
- З `--json` → check-only (як раніше, для CI)
- `fix bun rego` → оркестратор тільки для вказаних правил

**Демо:** 3 violations (bun + rego + style-lint) закрито T0-auto за одну ітерацію, 0 LLM викликів.

**Змінені файли:** `npm/bin/n-cursor.js` (fix case redirect), `npm/skills/fix/js/orchestrator.mjs` (bugfix args parsing), `npm/skills/fix/SKILL.md` + `.cursor/skills/n-fix/SKILL.md` (спрощено до 5 кроків).

---

Далі: зробити те саме для `lint` і `taze`?

## ADR Автономний оркестратор `n-cursor fix`

## Context and Problem Statement
`npx @nitra/cursor fix` виконував лише перевірку (check-only): виводив violation-повідомлення і залишав виправлення агенту або людині. Команда потребувала окремого SKILL.md-workflow з 8 кроків, де LLM вручну інтерпретував вивід і застосовував правки.

## Considered Options
* `fix` без прапорця → автономний оркестратор; `--json` залишається check-only
* Окрема команда `fix-run` для оркестратора (поряд із `fix` як чекером)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "`fix` без `--json` → автономний оркестратор", because користувач явно сформулював вимогу: «хочу запускати оркестратор фікса як `npx @nitra/cursor fix`». `fix-run` залишається як застарілий псевдонім.

### Consequences
* Good, because SKILL.md скорочується з 8 до 5 кроків; LLM-агент більше не потрібен для інтерпретації вихідних даних.
* Bad, because `fix` без `--json` більше не виводить просто текстовий звіт — поведінка breaking change для сценаріїв, де `fix` використовувався як перегляд стану без виправлення. Transcript не містить підтверджених негативних наслідків.

## More Information
Реалізація:
- `npm/skills/fix/js/orchestrator.mjs` — convergence-loop: T0-auto → LLM (haiku→sonnet) → recheck, до `failed === 0` або `--max-iter 3`
- `npm/skills/fix/js/llm-worker.mjs` — Claude SDK агент з `read_file`/`write_file`/`run_command` tools; кеш системного промпту через `cache_control: { type: 'ephemeral' }`
- `npm/skills/fix/js/t0.mjs` — детермінований T0-рівень (0 LLM): паттерни `vscode-ext-add` і `rm-forbidden-file`
- `npm/bin/n-cursor.js:1600` — dispatch: `args.includes('--json')` → `runFixCommand`, інакше → `runOrchestratorCli`
- Demo: 3 violations (bun + rego + style-lint) закрито T0-auto за 1 ітерацію, 0 LLM
