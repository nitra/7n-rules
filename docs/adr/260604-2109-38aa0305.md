---
session: 38aa0305-a12b-4078-9085-ce03884efdd6
captured: 2026-06-04T21:09:06+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/38aa0305-a12b-4078-9085-ce03884efdd6.jsonl
---

Тепер маю всі числа. Ось точна оцінка:

---

## Оцінка витрат токенів: `n-coverage-fix` на цьому проєкті

### Вхідні дані

| Артефакт | Розмір | Токени (~÷4) |
|---|---|---|
| `COVERAGE.md` (survived JSON) | 2.76 МБ | **~690k** |
| Правила/CLAUDE.md (35 `.mdc`) | 290 КБ | ~78k |
| `SKILL.md` | 12.7 КБ | ~3.4k |
| Середній source-файл | 16.7 КБ | ~4.5k |
| Середній test-файл | 70.5 КБ | ~19k |
| Survived файлових груп | 122 шт., 22.6 КБ/гр. | ~6.1k/гр. |

---

### Критична проблема — контекстне вікно

Sonnet 4.6 має ліміт **200k токенів** на контекст. `COVERAGE.md` один займає **690k токенів** — в 3.4× більше. Скіл у нинішньому вигляді не може прочитати весь JSON-блок за один виклик.

**Як це вирішується на практиці:**

`coverage-classify.mjs` — окремий pre-processing модуль, який вже є в проєкті. Він викликає API **по одному мутанту** (source ±10 рядків + тестовий файл до 2000 рядків + системний промпт із prompt caching) і класифікує їх як `worth-testing / equivalent / defensive / glue / wrapper`. У `n-coverage-fix` цей крок не інтегровано — скіл очікує, що COVERAGE.md уже відфільтровано або читається по частинах через `Read(offset, limit)`.

---

### Кошторис на один повний прогін (3 ітерації, без classify-фільтрації)

**Шар 1 — orchestrating agent** (читає COVERAGE.md по 7 чанках ~100k токенів, групує, диспатчить):

| | Токени | Ціна (Sonnet 4.6) |
|---|---|---|
| Input (chunks + rules + SKILL) | 690k + 78k + 3.4k = 771k | **$2.31** |
| Cache write (rules/SKILL, 5 хв TTL) | ~81k | $0.30 |
| Output (диспатч-інструкції) | ~5k | $0.08 |
| × 3 ітерації | — | **~$8** |

**Шар 2 — 122 per-file sub-agents × 3 ітерації:**

| Компонент | Токени/агент | Ціна/агент |
|---|---|---|
| Mutant group (з COVERAGE.md) | 6.1k | $0.018 |
| Source file | 4.5k | $0.014 |
| Test file | 19k | $0.057 |
| SKILL-інструкції | 3.4k | $0.010 |
| **Input разом** | **33k** | **$0.099** |
| Output (нові тести) | ~1.5k | $0.023 |
| **Разом/агент** | — | **~$0.12** |

122 агенти × 3 ітерації = 366 викликів → **~$44**

**Підсумок:**

| Сценарій | Вартість |
|---|---|
| 1 ітерація (швидка конвергенція) | **~$17** |
| 3 ітерації (повний цикл) | **~$52** |
| 3 ітерації + prompt caching (80% hit на SKILL/rules) | **~$45** |

---

### Де реальна економія

**`coverage-classify` → фільтрація перед `coverage-fix`** — найбільший важіль. Якщо з 122 файлів класифікація відсіює 60% як `equivalent/glue/wrapper` (типово), залишається ~50 реально цінних груп:

- 50 sub-agents замість 122 → $18 замість $44
- **Загальний прогін: ~$25 замість $52** (−50%)

`coverage-classify` має власний кеш (`cache.mjs`) — повторні класифікації тих самих мутантів безкоштовні.

---

### Практичний висновок

**Найдорожче** — не тести (output дешевий), а читання `COVERAGE.md` в orchestrating agent. Для цього проєкту (2.76 МБ) потрібне або чанкування, або попередня фільтрація через `coverage-classify`. Без цього виклик одного `n-coverage-fix` генерує ~$50 витрат за неповний прогін — переважно на введення в контекст даних, які вже відомі.
