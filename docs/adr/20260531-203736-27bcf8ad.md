---
session: 27bcf8ad-3d79-4564-975c-e30f0be45f1d
captured: 2026-05-31T20:37:36+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/27bcf8ad-3d79-4564-975c-e30f0be45f1d.jsonl
---

Готово. Тепер поріг мінімальної версії **живе виключно в snippet** — жодних магічних чисел у Rego.

## Підсумок змін

**`template/package.json.snippet.json`** — додано поле `devDependencies` з мінімальним порогом:
```json
"devDependencies": { "@nitra/eslint-config": "^3.10.0" }
```

**`package_json.rego`** — Rego читає поріг з `data.template.snippet.devDependencies["@nitra/eslint-config"]`. Щоб підняти мінімум у майбутньому — міняється тільки snippet.

**`package_json_test.rego`** — `template_data` передає snippet з порогом, додано тест `test_eslint_min_driven_by_snippet`.

**Перевірка:**
- `opa fmt` — форматування чисте
- `regal lint` — **0 violations**
- `opa test` — **8/8 pass**
- Реальне eval: `^3.10.0` → `[]`, `^3.9.4` → deny з повідомленням `>= 3.10.0`
[user]
Дякую! Зроби ADR для змін в цій сесії, включно з відмовою від генерації, підйомом версії і ще важливими змінами.

---
