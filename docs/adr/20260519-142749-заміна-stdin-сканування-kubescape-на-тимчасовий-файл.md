---
session: 8a604541-32f2-4a17-b482-d057d3059bc4
captured: 2026-05-19T14:27:49+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/8a604541-32f2-4a17-b482-d057d3059bc4.jsonl
---

Looking good — I can read the full source file before editing.
[assistant]
[tool: Read] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","offset":180,"limit":60}
[user]
[tool_result] 180	 */
181	function runKustomizeBuild(kubectlPath, dir) {
182	  const r = spawnSync(kubectlPath, ['kustomize', dir], {
182	  const r = spawnSync(kubectlPath, ['kustomize', dir], {
183	    stdio: ['ignore', 'pipe', 'inherit'],
183	    stdio: ['ignore', 'pipe', 'inherit'],
184	    shell: false
184	    shell: false
185	  })
185	  })
186	  if (r.status !== 0) {
186	  if (r.status !== 0) {
187	    // ...
187	    // ...
188	  }
188	  }
189	  return r.stdout.toString()
189	  return r.stdout.toString()
190	}
190	}
191
191
192	/**
192	/**
193	 * ...
193	 * ...
194	 * @param {string} manifest
194	 * @param {string} manifest
195	 * @param {string} kubescapePath
195	 * @param {string} kubescapePath
196	 * @param {number[]} exceptionsArgs
196	 * @param {number[]} exceptionsArgs
197	 */
197	 */
198	function runKubescapeStdin(manifest, kubescapePath, exceptionsArgs) {
198	function runKubescapeStdin(manifest, kubescapePath, exceptionsArgs) {
199	  const r = spawnSync(kubescapePath, ['scan', '-', '--severity-threshold', 'high', ...exceptionsArgs], {
199	  const r = spawnSync(kubescapePath, ['scan', '-', '--severity-threshold', 'high', ...exceptionsArgs], {
200	    input: manifest,
200	    input: manifest,
201	    stdio: ['pipe', 'inherit', 'inherit'],
201	    stdio: ['pipe', 'inherit', 'inherit'],
202	    shell: false
202	    shell: false
203	  })
203	  })
204	  return r
204	  return r
205	}
205	}
206
206
207	/**
207	/**
208	 * @param {string} dir
208	 * @param {string} dir
209	 * @param {string} kubectlPath
209	 * @param {string} kubectlPath
210	 * @param {string} kubescapePath
210	 * @param {string} kubescapePath
211	 * @param {string} exceptionsFile
211	 * @param {string} exceptionsFile
212	 */
212	 */
213	function runKubescape(dir, kubectlPath, kubescapePath, exceptionsFile) {
213	function runKubescape(dir, kubectlPath, kubescapePath, exceptionsFile) {
214	  const manifest = runKustomizeBuild(kubectlPath, dir)
214	  const manifest = runKustomizeBuild(kubectlPath, dir)
215	  const exceptionsArgs = buildKubescapeExceptionsArgs(exceptionsFile)
215	  const exceptionsArgs = buildKubescapeExceptionsArgs(exceptionsFile)
216	  return runKubescapeStdin(manifest, kubescapePath, exceptionsArgs)
216	  return runKubescapeStdin(manifest, kubescapePath, exceptionsArgs)
217	}
217	}
218
218
219	/**
219	/**
[assistant]
[thinking]

[assistant]
Бачу проблему. Функцію `runKubescapeStdin` треба замінити на підхід із тимчасовим файлом.
[assistant]
[tool: Read] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","offset":1,"limit":20}
[user]
[tool_result] 1	/**
2	 * Запуск kubeconform та kubescape для каталогів `…/k8s`, де є YAML-маніфести (див. k8s.mdc).
3	 *
4	 * Знаходить унікальні корені каталогів із іменем `k8s` за шляхами файлів **`*.yaml`**
5	 * (той самий принцип сегмента `k8s`, що й у check-k8s.mjs; розширення **`.yml`** під `k8s` не викорис
[assistant]
[tool: Read] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","offset":1,"limit":10}
[user]
[tool_result] 1	/**
2	 * Запуск kubeconform та kubescape для каталогів `…/k8s`, де є YAML-маніфести (див. k8s.mdc).
3	 *
4	 * Знаходить унікальні корені каталогів із іменем `k8s` за шляхами файлів **`*.yaml`**
5	 * (той самий принцип сегмента `k8s`, що й у check-k8s.mjs; розширення **`.yml`** під `k8s` не викорис
[assistant]
[tool: Bash] {"command":"head -15 /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","description":"View import block in lint.mjs"}
[user]
[tool_result] 1	/**
2	 * Запуск kubeconform та kubescape для каталогів `…/k8s`, де є YAML-маніфести (див. k8s.mdc).
3	 *
4	 * Знаходить унікальні корені каталогів із іменем `k8s` за шляхами файлів **`*.yaml`**
5	 * (той самий принцип сегмента `k8s`, що й у check-k8s.mjs; розширення **`.yml`** під `k8s` не викорис
[assistant]
[tool: Bash] {"command":"grep -n '^import' /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","description":"List imports in lint.mjs"}
[user]
[tool_result] 16	import { spawnSync } from 'node:child_process'
17	import { existsSync, mkdirSync } from 'node:fs'
18	import { join } from 'node:path'
19
[assistant]
[thinking]

[assistant]
`writeFileSync` і `mkdtempSync` потрібні для тимчасового файлу. Перевіряю, що ще є в import-рядку.
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","old_string":"import { existsSync, mkdirSync } from 'node:fs'","new_string":"import { existsSync, mkdirSync, mkdtempSync, writeFileSync, unlinkSync } from 'node:fs'"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","old_string":"import { join } from 'node:path'","new_string":"import { join, tmpdir } from 'node:path'\nimport { tmpdir as osTmpdir } from 'node:os'"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
[assistant]
[thinking]

[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","old_string":"import { join, tmpdir } from 'node:path'\nimport { tmpdir as osTmpdir } from 'node:os'","new_string":"import { join } from 'node:path'\nimport { tmpdir } from 'node:os'"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
[assistant]
[thinking]

Now let me replace `runKubescapeStdin` with a temp-file-based implementation.
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","old_string":"function runKubescapeStdin(manifest, kubescapePath, exceptionsArgs) {\n  const r = spawnSync(kubescapePath, ['scan', '-', '--severity-threshold', 'high', ...exceptionsArgs], {\n    input: manifest,\n    stdio: ['pipe', 'inherit', 'inherit'],\n    shell: false\n  })\n  return r\n}","new_string":"function runKubescapeStdin(manifest, kubescapePath, exceptionsArgs) {\n  const dir = mkdtempSync(join(tmpdir(), 'kubescape-'))\n  const tmpFile = join(dir, 'manifest.yaml')\n  try {\n    writeFileSync(tmpFile, manifest)\n    return spawnSync(kubescapePath, ['scan', tmpFile, '--severity-threshold', 'high', ...exceptionsArgs], {\n      stdio: 'inherit',\n      shell: false\n    })\n  } finally {\n    try { unlinkSync(tmpFile) } catch {}\n    try { unlinkSync(dir) } catch {}\n  }\n}"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
[assistant]
[thinking]

[assistant]
[tool: Bash] {"command":"cd /Users/vitaliytv/www/nitra/cursor && node -e \"import('/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs')\" 2>&1","description":"Syntax check lint.mjs"}
[user]
[tool_result] file:///Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs:17:39: error: Could not resolve: "node:os"
[assistant]
[thinking]

The error is "Could not resolve: 'node:os'". This might be a Bun-specific issue or something related to how this is being run. Let me check the test to understand how the module is being run.
[assistant]
[tool: Bash] {"command":"grep -n 'node:os\\|os.tmpdir' /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/run-roots.test.mjs","description":"Check node:os usage"}
[user]
[tool_result] /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs:17:import { tmpdir } from 'node:os'
/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/run-roots.test.mjs:4:import { tmpdir } from 'node:os'
[assistant]
[thinking]

The test file also imports from `node:os`, so it should work. The error was from running with `node`, not `bun`. Let me check if there's a package.json that specifies bun.
[assistant]
[tool: Bash] {"command":"bun -e \"import('/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs').then(() => console.log('ok')).catch(e => console.error(e.message))\" 2>&1 | head -20","description":"Syntax check with Bun"}
[user]
[tool_result] ok
[assistant]
[thinking]

The file parses OK with Bun. Now let me update the CHANGELOG and version bump. Let me check the current version.
[assistant]
Синтаксис чистий. Але треба прибрати `mkdirSync` зі спроби видалити директорію — треба `rmdirSync`.
[assistant]
[tool: Read] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","offset":195,"limit":20}
[user]
[tool_result] 195	 */
196	function runKubescapeStdin(manifest, kubescapePath, exceptionsArgs) {
197	  const dir = mkdtempSync(join(tmpdir(), 'kubescape-'))
198	  const tmpFile = join(dir, 'manifest.yaml')
199	  try {
200	    writeFileSync(tmpFile, manifest)
201	    return spawnSync(kubescapePath, ['scan', tmpFile, '--severity-threshold', 'high', ...exceptionsArgs], {
201	    return spawnSync(kubescapePath, ['scan', tmpFile, '--severity-threshold', 'high', ...exceptionsArgs], {
202	      stdio: 'inherit',
202	      stdio: 'inherit',
203	      shell: false
203	      shell: false
204	    })
204	    })
205	  } finally {
205	  } finally {
206	    try { unlinkSync(tmpFile) } catch {}
206	    try { unlinkSync(tmpFile) } catch {}
207	    try { unlinkSync(dir) } catch {}
207	    try { unlinkSync(dir) } catch {}
208	  }
208	  }
209	}
209	}
[assistant]
`unlinkSync` для директорії не спрацює — потрібен `rmdirSync`.
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","old_string":"import { existsSync, mkdirSync, mkdtempSync, writeFileSync, unlinkSync } from 'node:fs'","new_string":"import { existsSync, mkdirSync, mkdtempSync, writeFileSync, unlinkSync, rmdirSync } from 'node:fs'"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs","old_string":"    try { unlinkSync(dir) } catch {}","new_string":"    try { rmdirSync(dir) } catch {}"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
[assistant]
[tool: Bash] {"command":"bun -e \"import('/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs').then(() => console.log('ok')).catch(e => console.error(e.message))\" 2>&1","description":"Syntax check after rmdirSync fix"}
[user]
[tool_result] ok
[assistant]
[thinking]

Good. Now let me update the CHANGELOG and bump version. Let me check CHANGELOG format.
[assistant]
Тепер оновити CHANGELOG і збільшити версію.
[assistant]
[tool: Read] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/CHANGELOG.md","offset":1,"limit":30}
[user]
[tool_result] 1	# Changelog
2
3	Усі помітні зміни цього модуля документуються тут.
4
3	Усі помітні зміни цього модуля документуються тут.
3	Усі помітні зміни цього модуля документуються тут.
4
4
5	Формат — [Keep a Changelog](https://keepachangelog.com/uk/1.1.0/), нумерація — [SemVer](https://semver.org/lang/uk/).
5	Формат — [Keep a Changelog](https://keepachangelog.com/uk/1.1.0/), нумерація — [SemVer](https://semver.org/lang/uk/).
6
6
7	## [1.13.50] - 2026-05-19
7	## [1.13.50] - 2026-05-19
8
8
9	### Changed
9	### Changed
10
10
11	- `lint-k8s`: kubescape тепер збирає kustomize-маніфест через **вшиту в kubescape** функцію сканування kustomize (`kubescape scan <kustomize-dir>`), а не попередньо через `kubectl kustomize` + stdin. Виправлено неправильно поставлений `--severity-threshold` — тепер передається як `--severity-threshold=high` (з `=`).
11	- `lint-k8s`: kubescape тепер збирає kustomize-маніфест через **вшиту в kubescape** функцію сканування kustomize (`kubescape scan <kustomize-dir>`), а не попередньо через `kubectl kustomize` + stdin. Виправлено неправильно поставлений `--severity-threshold` — тепер передається як `--severity-threshold=high` (з `=`).
12
12
13	## [1.13.49] - 2026-05-19
13	## [1.13.49] - 2026-05-19
[assistant]
[thinking]

I can see that 1.13.50 CHANGELOG entry says: "`lint-k8s`: kubescape тепер збирає kustomize-маніфест через вшиту в kubescape функцію сканування kustomize (`kubescape scan <kustomize-dir>`), а не попередньо через `kubectl kustomize` + stdin. Виправлено неправильно поставлений `--severity-threshold` — тепер передається як `--severity-threshold=high` (з `=`)."

But looking at the current source, the implementation in `runKubescape` still uses:
1. `runKustomizeBuild` (kubectl kustomize to get manifest string)
2. Then `runKubescapeStdin` with the manifest

The CHANGELOG for 1.13.50 says it scans kustomize directly, but the actual code doesn't do that. The user is telling me that the current 1.13.50 has a bug where stdin scanning doesn't work.

Let me re-read the CHANGELOG entry more carefully. The 1.13.50 CHANGELOG entry mentions:
- kubescape scanning kustomize directly via `kubescape scan <kustomize-dir>`
- `--severity-threshold=high` with `=`

But the actual source code shows `runKubescapeStdin` still uses stdin with `-`.

This might mean that 1.13.50 was supposed to fix this differently (scanning the dir directly) but was implemented incorrectly, or the CHANGELOG is outdated.

Wait, looking at the user's message again: "Проблема — у самій новій версії @nitra/cursor@1.13.50." So 1.13.50 is the version with the problem (using stdin). The CHANGELOG entry for 1.13.50 seems to describe a *previous* attempted fix or another version.

Actually, looking at the git history:
- a6f8c7d 1.13.50
- 1211bf2 1.13.49

The user is saying 1.13.50 has the bug. But the CHANGELOG for 1.13.50 says "kubescape тепер збирає kustomize-маніфест через вшиту в kubescape функцію сканування kustomize". This is confusing.

Wait, maybe the CHANGELOG for 1.13.50 was *already written* (in the same commit a6f8c7d) with the intent that this was supposed to fix things, but the actual implementation in the source file still uses stdin (the bug). So maybe in 1.13.50, the CHANGELOG was written prematurely saying the issue is fixed, but the code still has the stdin approach.

Actually, looking more carefully: the user said "lint-k8s → exit 1. Проблема — у самій новій версії @nitra/cursor@1.13.50." and "Реалізація runKubescapeStdin у node_modules/@nitra/cursor/rules/k8s/lint/lint.mjs:204-212 використовує kubescape scan - --severity-threshold high".

So the current 1.13.50 code (the one we're looking at in the repo at `/Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs`) still has the stdin approach. My task is to fix it by replacing stdin with temp file approach.

I've already made the edit (runKubescapeStdin now uses a temp file). Now I need to:
1. Update CHANGELOG to add a new entry (1.13.51) documenting the fix
2. Bump version to 1.13.51

Wait, but the CHANGELOG entry for 1.13.50 already exists. My fix would be 1.13.51. Let me add the new version.

Actually, looking at the CHANGELOG text for 1.13.50 again: "kubescape тепер збирає kustomize-маніфест через вшиту в kubescape функцію сканування kustomize (`kubescape scan <kustomize-dir>`), а не попередньо через `kubectl kustomize` + stdin."

This already says "замість stdin" - but the code still has stdin. This is confusing. Maybe the 1.13.50 commit was supposed to implement this differently but then the actual implementation wasn't correct.

Hmm, let me re-read the CHANGELOG more carefully. Actually it says "через **вшиту в kubescape** функцію сканування kustomize (`kubescape scan <kustomize-dir>`)". This says it scans the kustomize directory directly (not stdin, not temp file). But the current approach the user confirmed working is via `kubectl kustomize <dir> > /tmp/built.yaml && kubescape scan /tmp/built.yaml`.

So there were two possible approaches:
1. `kubescape scan <kustomize-dir>` - scan directly
2. `kubectl kustomize <dir> > tmpfile && kubescape scan tmpfile` - build first, then scan temp file

The user confirmed approach 2 works. But the CHANGELOG for 1.13.50 says approach 1 was chosen. Was approach 1 actually implemented in 1.13.50? Let me check what the actual code looks like now.

Looking at what I've changed: the `runKubescape` function still calls `runKustomizeBuild` (kubectl kustomize) and then `runKubescapeStdin`. I replaced stdin with temp file, so now it's approach 2.

The CHANGELOG for 1.13.50 describes approach 1 (direct kustomize scan), but the code shows it was approach 2 (temp file via kubectl kustomize + kubescape scan tmpfile). That means either:
- The CHANGELOG was written for a future version that wasn't implemented yet
- OR the code for 1.13.50 was different from what I'm seeing

Wait, let me re-read - maybe 1.13.50's CHANGELOG describes what was done in the commit before, and the code I'm looking at IS 1.13.50 (with the stdin bug). The CHANGELOG entry for 1.13.50 might have been added but the code wasn't properly updated.

This is getting complex. Let me just focus on what matters:
- The user confirmed temp-file approach works
- I've implemented it with `runKubescapeStdin` (misleading name now, but keeping the function)
- Need to bump version and update CHANGELOG

Actually, re-examining: I think what happened is:
- 1.13.49 introduced `runKubescapeStdin` with stdin (the bug)
- 1.13.50 was supposed to fix it via direct kustomize scan, but the fix in 1.13.50 still uses stdin (the CHANGELOG was written incorrectly or the implementation wasn't done properly)
- Now we need to implement the actual fix

So my fix (temp file) is new work. I should bump to 1.13.51 and add a new CHANGELOG entry explaining the actual fix.

Let me now update CHANGELOG and version.
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/CHANGELOG.md","old_string":"## [1.13.50] - 2026-05-19","new_string":"## [1.13.51] - 2026-05-19\n\n### Fixed\n\n- `lint-k8s`: kubescape v4 не підтримує зчитування маніфесту зі stdin (`-`); тепер `runKubescapeStdin` записує маніфест у тимчасовий файл (`mkdtemp`) і передає шлях до нього (`kubescape scan <tmpfile>`), а після сканування видаляє файл та директорію.\n\n## [1.13.50] - 2026-05-19"}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/CHANGELOG.md
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/npm/package.json","old_string":"\"version\": \"1.13.50\"","new_string":"\"version\": \"1.13.51\""}
[user]
[tool_result] Edited /Users/vitaliytv/www/nitra/cursor/npm/package.json
[assistant]
[tool: Bash] {"command":"cd /Users/vitaliytv/www/nitra/cursor && git diff --stat","description":"Review changed files"}
[user]
[tool_result] npm/CHANGELOG.md              |  8 ++++++
npm/package.json              |  2 +-
npm/rules/k8s/lint/lint.mjs   | 16 ++++++++---
3 files changed, 21 insertions(+), 5 deletions(-)
[assistant]
[tool: Bash] {"command":"cd /Users/vitaliytv/www/nitra/cursor && bun run lint 2>&1 | tail -30","description":"Run lint to verify clean"}
[user]
[tool_result] $ bun run lint
$ bun run lint-js && bun run lint-k8s && bun run lint-style && bun run lint-conftest
$ eslint .
$ bun run lint-k8s
$ bun /Users/vitaliytv/www/nitra/cursor/npm/rules/k8s/lint/lint.mjs
run/adminer/k8s: kubectl kustomize → 26 resources, kubescape …
[✓] run/adminer/k8s: All controls passed (kubescape)
run/api/k8s/base: kubectl kustomize → 3 resources, kubescape …
[✓] run/api/k8s/base: All controls passed (kubescape)
...
$ bun run lint-style
$ stylelint src/**/*.vue
$ bun run lint-conftest
$ bun /Users/vitaliytv/www/nitra/cursor/npm/rules/conftest/lint/lint.mjs
[✓] OPA conftest: all policies passed
[assistant]
`lint-k8s` тепер проходить. Всі перевірки чисті.
[assistant]
[tool: Bash] {"command":"cd /Users/vitaliytv/www/nitra/cursor && git add npm/CHANGELOG.md npm/package.json npm/rules/k8s/lint/lint.mjs && git commit -m \"$(cat <<'EOF'\nfix(lint-k8s): kubescape v4 stdin workaround — write manifest to tmpfile\n\nkubescape v4.0.8 does not accept stdin via `-`; replace stdin pipe\nwith mkdtemp + writeFileSync + rmdirSync cleanup.\n\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\nEOF\n)\"","description":"Commit fix"}
[user]
[tool_result] [main 6e79e10] fix(lint-k8s): kubescape v4 stdin workaround — write manifest to tmpfile
3 files changed, 21 insertions(+), 5 deletions(-)
[assistant]
Готово. Ось що зроблено:

- **`runKubescapeStdin`** замінює stdin-pipe (`scan -`) на запис у тимчасовий файл через `mkdtempSync` → `writeFileSync` → `kubescape scan <tmpfile>`, після чого `unlinkSync` + `rmdirSync` прибирають за собою.
- Додано `rmdirSync`, `mkdtempSync`, `writeFileSync`, `unlinkSync` до імпорту `node:fs`; `tmpdir` з `node:os`.
- CHANGELOG: новий запис `[1.13.51]` з поясненням фіксу, версія в `package.json` оновлена.
- `bun run lint` — всі `k8s`-маніфести пройшли kubescape, вихід `0`.

---
<END_OF_TRANSCRIPT>

## ADR Заміна stdin-сканування kubescape на тимчасовий файл

## Context and Problem Statement
`kubescape v4.0.8` не підтримує зчитування маніфесту зі stdin через аргумент `-`: CLI повертає `{"level":"fatal","msg":"no resources found to scan"}` і завершується з ненульовим кодом виходу. Функція `runKubescapeStdin` у `npm/rules/k8s/lint/lint.mjs` передавала зібраний `kubectl kustomize`-маніфест через stdin, що спричиняло падіння `bun run lint` на кроці `lint-k8s`.

## Considered Options
* Запис маніфесту у тимчасовий файл через `mkdtempSync` + `writeFileSync`, передача шляху до файлу в `kubescape scan <tmpfile>`, очищення після сканування (`unlinkSync` + `rmdirSync`).
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Запис маніфесту у тимчасовий файл", because це єдиний підхід, підтверджений у transcript як такий, що дає `exit 0` із `kubescape v4.0.8` (`kubectl kustomize <dir> > /tmp/built.yaml && kubescape scan /tmp/built.yaml --severity-threshold high` → `All controls passed`).

### Consequences
* Good, because `bun run lint` → `lint-k8s` завершується з кодом `0`; усі k8s-маніфести проходять kubescape без помилок — підтверджено виводом `[✓] … All controls passed (kubescape)` для кожного кореневого каталогу.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Змінений файл: `npm/rules/k8s/lint/lint.mjs` (функція `runKubescapeStdin`, рядки ≈196–209 після патчу)
- Додані імпорти: `mkdtempSync`, `writeFileSync`, `unlinkSync`, `rmdirSync` з `node:fs`; `tmpdir` з `node:os`
- Версія пакету: `1.13.50` → `1.13.51`; запис у `npm/CHANGELOG.md`
- Команда для відтворення проблеми: `echo "apiVersion: v1..." | kubescape scan -` (kubescape v4.0.8)
- Підтверджений робочий варіант: `kubectl kustomize <dir> > /tmp/built.yaml && kubescape scan /tmp/built.yaml --severity-threshold high --exceptions .kubescape-exceptions.json`
- Коміт виправлення: `6e79e10`
