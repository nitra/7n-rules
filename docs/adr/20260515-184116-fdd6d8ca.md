---
session: fdd6d8ca-82b9-476a-902d-380eb68d9de8
captured: 2026-05-15T18:41:16+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/fdd6d8ca-82b9-476a-902d-380eb68d9de8.jsonl
---

## Runbook n-fix — автоматична перевірка і виправлення проєкту

**Контекст:** У монорепо `nitra/cursor` накопичується набір архітектурних правил у `.cursor/rules/`. Щоб переконатися, що проєкт відповідає цим правилам, і виправити відхилення, команда запускає скіл `/n-fix`.

**Рішення/Процедура/Факт:**
1. Запустити скіл через `/n-fix` або викликом `.cursor/skills/n-fix/SKILL.md`.
2. Скіл виконує діагностику: `npx @nitra/cursor check` — перевіряє всі 16 активних правил (ADR-хуки, `settings.json`, `.gitignore`, lint-конфіги тощо).
3. За результатами перевірки скіл виправляє порушення відповідно до правил з `.cursor/rules/`.
4. Після виправлень запускається повторна перевірка для підтвердження чистого стану.

Ключові перевірки в сесії:
- `.claude/hooks/capture-decisions.sh` і `normalize-decisions.sh` збігаються з канонічними
- `.claude/settings.json` містить Stop-hook із `bun run lint-conftest`
- `.gitignore` покриває необхідні патерни

**Обґрунтування:** `npx @nitra/cursor check` є єдиним авторитетним джерелом стану відповідності: він перевіряє `check-*.mjs`-скрипти, що відповідають кожному правилу, і дає чіткий ✅/❌-статус перед будь-якими змінами.

**Розглянуті альтернативи:** не обговорювалися

**Зачіпає:** `.cursor/rules/` (16 правил), `.cursor/skills/n-fix/SKILL.md`, `.claude/hooks/`, `.claude/settings.json`, `.gitignore`
