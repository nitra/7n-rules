---
session: 069b3ac7-bc49-4c0a-b402-41a02c2900c5
captured: 2026-06-10T17:08:20+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/069b3ac7-bc49-4c0a-b402-41a02c2900c5.jsonl
---

=== js-lint-ci head ===
---
description: CI конфігурація лінтингу JavaScript в GitHub Actions
globs: "**/{.oxlintrc.json,eslint.config.js,.jscpd.json,knip.json,package.json},.github/workflows/*.yml,**/*.{js,mjs,cjs,jsx,ts,tsx}"
alwaysApply: false
version: '1.14'
---

=== vue rule? ===
n-vue
=== all rule dirs ===
conftest/ dev-dep/ feedback/ ga/ js-bun-db/ js-bun-redis/ js-lint/ js-lint-ci/ js-mssql/ js-run/ n-adr/ npm-module/ scripts/ security/ style-lint/ text/ n-vue/
[assistant]
[thinking]

[assistant]
Добре. Є кілька кандидатів, де може жити це правило. Найлогічніший — або розширити `js-run` (він про backend JS), або зробити спільний JS-норм-розділ, який стосується всіх (і backend, і frontend). Але розглянемо ще `n-vue` — якщо frontend == Vue.

**Пропозиція: додати правило в `js-run.mdc`** з ремаркою, що воно стосується всіх JS-файлів проекту (а не тільки backend), плюс аналогічний рядок у `n-vue.mdc`.

Або — якщо хочеш одне місце для обох — новий блок в `js-lint.mdc`, бо він already покриває `**/*.{js,mjs,cjs}` і і backend і frontend за globs.

Ось де конкретно я б дописав (варіант — у `js-lint.mdc`):

```markdown
## Розширення файлів

Нові JS-файли створюй із явним розширенням:
- `.mjs` — для ESM
- `.cjs` — для CommonJS

Використання `.js` для **нових** файлів заборонено: без явного розширення Node.js/Bun мусить читати `package.json#type`, щоб вирішити як інтерпретувати файл — це крихко й неявно.

Існуючі `.js`-файли **не** переіменовуй (крім явного рефакторингу).
```

Де тебе це більше влаштовує: `js-lint`, `js-run`, чи обидва?
[user]
js-lint.mdc
[assistant]
Читаю повний вміст файлу, щоб вставити блок у правильне місце.
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/js-lint/js-lint.mdc"}
[user]
[tool_result] 1	---
2	description: Перевірка JavaScript коду
3	globs: "**/{.oxlintrc.json,eslint.config.js,.jscpd.json,knip.json,package.json},**/*.{js,mjs,cjs,jsx,ts,tsx}"
4	alwaysApply: false
5	version: '1.29'
6	---
7
8	**oxlint**, **ESLint**, **jscpd**, **knip**. У скрипті **`lint-js`** і в CI — **`bunx oxlint`** + **`bunx eslint`** (ESLint — лише тоді, коли присутній `eslint.config.js`). Після запуску — **`knip`** (невикористаний код). **`bunx jscpd`** — для дублювання, лише якщо присутній `.jscpd.json`.
9
10	## Вимоги до конфігу
11
11	## Вимоги до конфігу
12
13	Перевіряй в `package.json`:
14
15	- `scripts.lint-js` — скрипт для запуску лінтера. Починається з `bunx oxlint`. Після `bunx oxlint` йде `&&`, а далі — лише якщо існує `eslint.config.js`. Тільки `&&`, без `||` і `||true`.
16	- `scripts.lint` — агрегований скрипт, який включає `lint-js`.
17
18	## Порядок запуску інструментів
19
20	1. `bunx oxlint` (завжди)
21
22	2. `bunx eslint` — лише якщо існує `eslint.config.js`
23
24	3. `bunx knip` (завжди)
25
26	4. `bunx jscpd` — лише якщо існує `.jscpd.json`
27
28	## Правила oxlint
29
30	Конфіг — `.oxlintrc.json`. Для backend-пакетів встановлюй `"env": { "node": true }`.
31
32	## Правила ESLint
33
34	Конфіг — `eslint.config.js`. Наявний лише тоді, коли є специфічні правила, відсутні в oxlint.
35
36	## Правила knip
37
38	Конфіг — `knip.json`. Включай лише якщо `package.json` містить `scripts.knip`. Значення: `{ "entry": ["src/index.{js,mjs,cjs,ts}!"], "project": ["src/**/*.{js,mjs,cjs,ts}!"] }`.
37
38	## Правила knip
39
40	Конфіг — `knip.json`. Включай лише якщо `package.json` містить `scripts.knip`. Значення: `{ "entry": ["src/index.{js,mjs,cjs,ts}!"], "project": ["src/**/*.{js,mjs,cjs,ts}!"] }`.
41
42	## Правила jscpd
43
44	Конфіг — `.jscpd.json`. Є опційним.
45
46	## Заборонені залежності
47
48	- `mocha`, `jasmine` — заміна `vitest`
49
50	## Посилання
51
52	- [oxlint](https://oxc.rs/docs/guide/usage/linter)
53
54	- [ESLint](https://eslint.org/)
55
56	- [knip](https://knip.dev/)
[assistant]
[tool: Edit] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/js-lint/js-lint.mdc","old_string":"## Заборонені залежності","new_string":"## Розширення файлів\n\nНові JS-файли створюй із явним розширенням:\n- `.mjs` — для ESM\n- `.cjs` — для CommonJS\n\nВикористання `.js` для **нових** файлів заборонено: без явного розширення Node.js/Bun мусить читати `package.json#type`, щоб вирішити як інтерпретувати файл.\n\nІснуючі `.js`-файли **не** переіменовуй (крім явного рефакторингу).\n\n## Заборонені залежності"}
[user]
[tool_result] The file was edited successfully.
[assistant]
Зроблено. Додав новий розділ `## Розширення файлів` у `js-lint/js-lint.mdc` перед `## Заборонені залежності`.

Що зафіксовано:
- нові JS-файли — `.mjs` (ESM) або `.cjs` (CJS), `.js` заборонено
- мотивація: явне розширення усуває залежність від `package.json#type`
- існуючі `.js` не чіпати
---

Підсумую це рішення:

## ADR Розширення нових JS-файлів: `.mjs`/`.cjs` замість `.js`

## Context and Problem Statement
При створенні нових JavaScript-файлів без явного розширення Node.js/Bun мусить читати `package.json#type` для визначення формату модуля (ESM vs CJS). Це крихко й неявно — поведінка файлу залежить від контексту пакету, а не від самого файлу.

## Considered Options
* Явне розширення `.mjs`/`.cjs` (без залежності від `package.json#type`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Явне розширення `.mjs`/`.cjs`", because це усуває неявну залежність від `package.json#type` при інтерпретації файлу.

### Consequences
* Good, because файл самодокументований — формат модуля видно з розширення без читання `package.json`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Зміна: `npm/rules/js-lint/js-lint.mdc` — новий розділ `## Розширення файлів`
- Варіант реалізації (a): інструкція в `.mdc` без git-aware перевірки (`fix.mjs` не змінювався)
- Стосується backend і frontend; існуючі `.js`-файли не переіменовуються
---
