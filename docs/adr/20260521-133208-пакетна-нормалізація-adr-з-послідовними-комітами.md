---
session: ea881c04-dcf0-4753-a405-2366550a0911
captured: 2026-05-21T13:32:08+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/ea881c04-dcf0-4753-a405-2366550a0911.jsonl
---

## ADR Пакетна нормалізація ADR з послідовними комітами

## Context and Problem Statement
У `docs/adr/` накопичилося 66 чернеток (файли з `session:` у frontmatter). Скрипт `.claude/hooks/normalize-decisions.sh` має порогові захисти (`ADR_NORMALIZE_THRESHOLD`, `ADR_NORMALIZE_MIN_INTERVAL_HOURS`), які унеможливлювали ручний запуск між автоматичними прогонами.

## Considered Options
* Запустити нормалізацію одним прогоном і закомітити всі зміни разом
* Запускати батчі по 10, комітити після кожного батчу й продовжувати до вичерпання чернеток
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Запускати батчі по 10, комітити після кожного батчу й продовжувати", because користувач явно обрав "Закомітити й продовжити" після перегляду першого батчу.

### Consequences
* Good, because transcript фіксує очікувану користь: кожен батч дає атомарний коміт (`adr: normalize batch`), що дозволяє переглядати та відкочувати зміни по частинах.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Команда ручного запуску: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0 bash .claude/hooks/normalize-decisions.sh`
- Dry-run перед реальним прогоном: `ADR_NORMALIZE_DRY=1` — дає змогу переглянути план (merge-into, rewrite, delete) до застосування змін
- Перший батч: 14 файлів змінено, `+134 / −864` рядки, коміт `7c62d53`
- Після першого батчу лишилося 61 чернетка; другий батч запущено у фоні (`btdj4w9mu`)
- Коміт-команда: `git add docs/adr/ && git commit -m "adr: normalize batch"`
