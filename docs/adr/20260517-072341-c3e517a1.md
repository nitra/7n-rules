---
session: c3e517a1-5154-44e2-9ce0-9ca5f8359ffb
captured: 2026-05-17T07:23:41+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/c3e517a1-5154-44e2-9ce0-9ca5f8359ffb.jsonl
---

Важливе відкриття: VS Code canonical values вже є у policy-скриптах (наприклад `graphql/policy/vscode_extensions/` перевіряє `"graphql.vscode-graphql"`). Тобто видаливши блоки з .mdc, я не втратив перевірки — вони є в Rego.

Але частина VS Code блоків не має відповідних policy-концернів → вони справді зникли. Це важливо для порівняння.

---

## Порівняння двох підходів

### Поточна архітектура (база)
```
.mdc          → AI-контекст (що писати)
fix/check.mjs → JS-перевірки (cross-file, FS)
policy/*.rego → Rego-перевірки (конкретні файли)
```
Канон зараз: **розсіяний** — часом в .mdc, часом в .rego, часом в check.mjs як рядок.

---

### Підхід A: `template/` як джерело істини

```
rules/<id>/
├── template/
│   ├── .vscode/extensions.json   # canonical snippet
│   └── package.json.json         # canonical fragment
├── fix/check.mjs   → читає template/, порівнює
└── policy/*.rego   → отримує template як --data
```

| | |
|---|---|
| **Плюси** | Канон в одному місці; AI бачить JSON а не JS-код; змінити канон = відредагувати JSON |
| **Мінуси** | Треба оновити всі check.mjs і rego (великий рефактор); потрібна нова утиліта template-loader; conftest --data-file інтеграція в runner |

---

### Підхід B: check.mjs / policy — вже є канон

Зберігаємо поточну архітектуру. Додаємо **відсутні** policy-концерни там, де VS Code блоки зникли без відповідного Rego.

```
rules/<id>/
├── policy/vscode_extensions/   ← ДОдати де немає
│   ├── vscode_extensions.rego
│   └── target.json
```

| | |
|---|---|
| **Плюси** | Нульовий рефактор; нова інфраструктура не потрібна; відповідає поточній архітектурі |
| **Мінуси** | Канон залишається у .rego рядках, а не у JSON-файлах; AI не отримує контекст з .rego при редагуванні .vscode/extensions.json (але policy вже є і без цього) |

---

### Моя рекомендація

**Підхід B зараз** — бо конкретна проблема ("VS Code блоки зникли") вирішується додаванням кількох policy-концернів, а не великим рефактором. Підхід A правильний концептуально, але потребує змін у всіх 26 правилах + runner-логіці.

**Конкретні дії для B:** знайти правила, де видалив VS Code блоки але policy/vscode_extensions не існує → додати Rego-концерни. Хочеш щоб я зробив такий аудит?
