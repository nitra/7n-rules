---
session: fcae811c-3bfd-41bb-8f03-01b3e0c66990
captured: 2026-06-14T21:53:26+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/fcae811c-3bfd-41bb-8f03-01b3e0c66990.jsonl
---

## ADR Оркестратор ADR-нормалізації на локальній моделі

## Context and Problem Statement
Поточна ADR-нормалізація реалізована як bash-hook `normalize-decisions.sh` (498 рядків), що запускається через Claude-hook і викликає LLM-провайдера хмари. Постало питання: чи можна і як перевести цей процес на локальну модель (`omlx/...`) та побудувати якісний оркестратор.

## Considered Options
* Залишити поточний bash-hook із хмарним LLM-викликом
* Побудувати новий оркестратор за патерном `npm/scripts/lib/fix/orchestrator.mjs` з локальним `omlx`-провайдером через `npm/lib/models.mjs`

## Decision Outcome
Chosen option: "Оркестратор за патерном `fix/orchestrator.mjs` з локальним `omlx`-провайдером", because transcript визначає `fix/orchestrator.mjs` як «канонічний референс патерну, який варто наслідувати», а модельна інфраструктура (`npm/lib/models.mjs`) вже підтримує маршрутизацію `omlx/<model>` → прямий HTTP до локального omlx-сервера без хмарного посередника.

### Consequences
* Good, because transcript фіксує очікувану користь: наявна `models.mjs`-інфраструктура (`N_LOCAL_MIN_MODEL=omlx/...`) дозволяє перемикнутися на локальну модель без зміни оркестраторного коду.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Поточний hook: `.claude/hooks/normalize-decisions.sh` (498 рядків)
- Референсний патерн: `npm/scripts/lib/fix/orchestrator.mjs` (задокументований у `npm/scripts/lib/fix/docs/orchestrator.md`, docgen-модель `omlx/gemma-4-e4b-it-OptiQ-4bit`)
- Модельна маршрутизація: `npm/lib/models.mjs` — префікс `omlx/<model>` → `npm/lib/omlx.mjs` (локальний HTTP), решта → хмара
- Змінна середовища: `N_LOCAL_MIN_MODEL=omlx/gemma-4-e4b-it-OptiQ-4bit` (з пам'яті сесії: `~/.zshenv`)
- Супутній skill: `.cursor/skills/n-adr-normalize/SKILL.md` — worktree-only, ручний запуск одного батчу чернеток
