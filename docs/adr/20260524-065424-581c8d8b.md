---
session: 581c8d8b-12a3-4ec3-9b5e-c2c686d3e935
captured: 2026-05-24T06:54:24+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/581c8d8b-12a3-4ec3-9b5e-c2c686d3e935.jsonl
---

Готово. Ось що зроблено:

| Файл | Зміна |
|---|---|
| `npm/rules/js-lint/js-lint.mdc` | +секція «Структура спільних модулів», версія `1.23 → 1.24` |
| `.cursor/rules/n-js-lint.mdc` | дзеркальне оновлення, версія `1.23 → 1.24` |
| `npm/package.json` | `1.13.89 → 1.13.90` |
| `npm/CHANGELOG.md` | новий запис `[1.13.90]` |

Обидва `fix changelog` і `fix js-lint` завершились **без зауважень**.
---

<анотація до коміту>
Наступна секція додана до `js-lint.mdc`:

## Структура спільних модулів

Коли кілька js-файлів мають спільну логіку, яку виносять в окремий каталог, — використовуй таку конвенцію іменування:

- **`utils/`** — низькорівневі, **чисті, generic helpers** без бізнес-логіки і без залежностей від домену проєкту. Те, що могло б жити в окремому npm-пакеті:
`formatDate()`, `chunk(array, size)`, `retry(fn, opts)`, `deepMerge()`, `parseDuration('5m')`, `sleep(ms)`

- **`lib/`** — внутрішні **модулі/підсистеми** твого проєкту, які знають про домен. Більші за utils, часто з власним state, конфігом або side effects:
`lib/gmail-client.js`, `lib/circuit-breaker.js`, `lib/skill-loader.js`, `lib/safety/dry-run.js`

> **Правило одного погляду:** якби цей файл міг бути в lodash/date-fns — це `utils/`. Якщо він прив'язаний до твого домену або має side effects — це `lib/`.
</анотація>
