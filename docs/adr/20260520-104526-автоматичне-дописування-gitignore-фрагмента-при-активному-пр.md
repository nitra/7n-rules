---
session: 748f86db-97db-4b20-81dd-f9fe88d716b5
captured: 2026-05-20T10:45:26+03:00
transcript: /Users/vitalii/.cursor/projects/Users-vitalii-www-nitra-cursor/agent-transcripts/748f86db-97db-4b20-81dd-f9fe88d716b5/748f86db-97db-4b20-81dd-f9fe88d716b5.jsonl
---

## ADR Автоматичне дописування `.gitignore`-фрагмента при активному правилі `adr`

## Context and Problem Statement
Артефакти ADR Stop-hook'ів (`.claude/hooks/*.log`, `.claude/hooks/.normalize-state`, `.claude/hooks/.normalize.lock`) потрапляли у git-трекінг у проєктах, що використовують правило `adr`. Не існувало ні канонічного переліку рядків для ігнорування, ні механізму їх автоматичного додавання.

## Considered Options
* Канонічний файл-фрагмент у пакеті + автодопис під час `npx @nitra/cursor`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Канонічний файл-фрагмент у пакеті + автодопис під час `npx @nitra/cursor`", because користувач явно попросив додати записи в «глобальний шаблон gitignore», а реалізація показала, що `sync-claude-config.mjs` вже є точкою синхронізації конфігів — розширити її на `.gitignore` природно і не вимагає ручних дій від споживача.

### Consequences
* Good, because після запуску `npx @nitra/cursor` усі відсутні рядки з фрагмента з'являються в `.gitignore` споживача автоматично, без ризику дублювання (існуючі рядки не перезаписуються).
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Новий файл: `npm/rules/adr/fix/hooks/template/.gitignore.snippet`
- Змінено: `npm/scripts/sync-claude-config.mjs` — функція `syncClaudeConfig` дописує фрагмент, коли правило `adr` увімкнене
- Змінено: `npm/bin/n-cursor.js` — виводить повідомлення про дописаний `.gitignore`
- Змінено: `npm/rules/adr/adr.mdc` — документація правила оновлена з посиланням на канонічний фрагмент
- Версія пакета: `1.13.63` → `1.13.64`

---

## ADR Базові рядки (`node_modules/`, `dist/`, `*.secret`) у ADR `.gitignore`-сніпеті

## Context and Problem Statement
Початковий варіант `npm/rules/adr/fix/hooks/template/.gitignore.snippet` містив лише ADR-специфічні артефакти Stop-hook'ів. Користувач попросив також включити базові рядки з кореневого `.gitignore` репозиторію (рядки 1–5: `node_modules/`, `dist/`, `*.secret`), щоб сніпет охоплював загальнопроєктні записи.

## Considered Options
* Додати базові рядки з кореневого `.gitignore` (`node_modules/`, `dist/`, `*.secret`) на початок сніпету
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Додати базові рядки з кореневого `.gitignore` на початок сніпету", because користувач явно вказав на `@.gitignore (1-5)` як джерело, яке треба включити до шаблону.

### Consequences
* Good, because `npx @nitra/cursor` тепер також забезпечує наявність `node_modules/`, `dist/` і `*.secret` у `.gitignore` споживача, що зменшує кількість ручних кроків при bootstrap нового проєкту.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Фінальний вміст `npm/rules/adr/fix/hooks/template/.gitignore.snippet`: `node_modules/`, `dist/`, `*.secret`, потім секція `# @nitra/cursor (adr)` з `.claude/hooks/*.log`, `.claude/hooks/.normalize-state`, `.claude/hooks/.normalize.lock`
- Оновлено тестові fixtures у `npm/scripts/sync-claude-config.test.mjs` для відповідності новому вмісту сніпету
