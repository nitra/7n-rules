---
session: ea881c04-dcf0-4753-a405-2366550a0911
captured: 2026-05-21T14:19:33+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/ea881c04-dcf0-4753-a405-2366550a0911.jsonl
---

## ADR Ітеративна пакетна нормалізація ADR-чернеток із комітом після кожного батчу

## Context and Problem Statement
У репозиторії накопичилось 66 ADR-чернеток із фронтматером `session:`. Скрипт `normalize-decisions.sh` обробляє їх батчами по 10 через LLM, але за замовчуванням обмежений порогом кількості й мінімальним інтервалом між запусками, що блокує ручний запуск.

## Considered Options
* Ручний запуск з `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0`, коміт після кожного батчу, продовження до вичерпання чернеток
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Ручний запуск із комітом після кожного батчу і продовженням", because користувач явно обрав «Закомітити й продовжити» після першого батчу, і цей цикл повторювався до батчу 5 включно.

### Consequences
* Good, because transcript фіксує очікувану користь: кожен батч з 10 чернеток обробляється окремим LLM-викликом (~7 хв), результат видно через `git diff` до коміту, і прогрес зберігається інкрементально (66 → 43 чернеток за 5 батчів).
* Bad, because transcript фіксує один збій: батч 3 повернув некоректний JSON від LLM і вимагав повторного прогону; автоматичного retry в скрипті немає.

## More Information
- Скрипт: `.claude/hooks/normalize-decisions.sh`
- Лог: `.claude/hooks/normalize-decisions.log`
- Змінні обходу: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0`
- Dry-run перед реальним прогоном: `ADR_NORMALIZE_DRY=1 bash .claude/hooks/normalize-decisions.sh`
- Коміти: `7c62d53`, `c45b8c9`, `f3ddb9e`, `bb28aaf` (батчі 1–4); батч 5 на момент завершення transcript ще не закомічений
- Коміт із трейлером `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` заблоковано auto mode classifier за порушення Content Integrity / Impersonation; наступні коміти виконувались без цього трейлера
