---
session: bb3047db-5332-4649-a713-3f1cde68927a
captured: 2026-05-30T06:45:09+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/bb3047db-5332-4649-a713-3f1cde68927a.jsonl
---

## ADR Ручна верифікація мутантів замість очікування Stryker

## Context and Problem Statement
`bun run coverage` запустив Stryker для npm-воркспейсу, але той завис на понад 50 хвилин: incremental-кеш став недійсним (`0 of 25053 mutant result(s) are reused`), паралельно два старих `bun -e fixSurvivedMutants` процеси (PID 90242, 91321) з'їдали CPU з Wed09AM — понад 3000 хвилин кожен. COVERAGE.md не оновився.

## Considered Options
* Дочекатися завершення Stryker-прогону (ще кілька годин)
* Зупинити Stryker, вбити зависші процеси та верифікувати 4 мутантів вручну через `vitest run -t <test-name>`

## Decision Outcome
Chosen option: "Зупинити Stryker, вбити зависші процеси та верифікувати вручну", because Stryker не просувався (sandbox-SeNDf9 не змінювався, `incremental.json` mtime = 1779980366), а старі bun-процеси блокували CPU через нескінченний self-import.

### Consequences
* Good, because transcript фіксує очікувану користь: усі 4 мутанти (`opts.fix → true`, `opts.fix → false`, `{}`, `fix: true`) підтверджено вбитими за ~10 хвилин замість 50+.
* Bad, because COVERAGE.md автоматично не оновився; наступний `bun run coverage` потрібний для оновлення рядка у звіті (очікується 141/141 = 100% для `coverage.mjs`).

## More Information
- Процеси вбито: `kill 90242 91321` (Wed09AM, самоімпорт `fixSurvivedMutants`)
- Stryker-дерево зупинено: `kill 33888 33890 42076 42105`
- Ручний цикл: `Edit coverage.mjs` (мутант) → `bunx vitest run ... -t <назва>` → `Edit` (відновлення) × 4

---

## ADR Тестування мутантів через spy withLock та .toString() стрілочної функції

## Context and Problem Statement
У `npm/rules/test/coverage/coverage.mjs` вижили 4 мутанти Stryker: два на рядку 189 (`if (opts.fix)` → `if (true)` / `if (false)`) і два на рядку 211 (`{ fix: false }` → `{}` / `{ fix: true }`). Наявні тести не охоплювали гілку виклику `fixSurvivedMutants` та дефолтне значення `fix` у `runCoverageCli`.

## Considered Options
* Тестувати через шпигунство на виклику `withLock` і перевірку аргументів `runCoverageSteps`
* Тестувати через `.toString()` другої стрілочної функції, яку передає `runCoverageCli` у `withLock`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Обидва: spy на `withLock.mock.calls` для L189 та `.toString()` другої стрілочної функції для L211", because мутанти на L189 перевіряються через стан `fixSurvivedMutantsCalled` (не `true`/`false` у виклику), а мутанти на L211 — через інспекцію тексту джерела замість mock-аргументів, які недоступні безпосередньо без перехоплення.

### Consequences
* Good, because transcript фіксує очікувану користь: усі 4 мутанти провалюють відповідний тест; `57 passed` після фікса (було 54 до нових тестів).
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл тестів: `npm/rules/test/coverage/tests/coverage.test.mjs`
- Нові describe-блоки: `runCoverageSteps — opts.fix gate (L189: вмикає/вимикає fixSurvivedMutants)` та перевірка `2-ї стрілки` (`FIX_TOKEN_RE`, `FALSE_TOKEN_RE`, `FIX_TRUE_RE`)
- Супровідний bug-fix у `npm/scripts/tests/auto-rules.test.mjs:282`: `root: process.cwd()` → `root: dir` (блокував Stryker dry-run)
- Версія bumped: `1.30.0` → `1.30.1`, commit `d42384b`
