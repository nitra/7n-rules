---
session: a8526b61-7bda-406e-b2f5-ce0c3c39d7e9
captured: 2026-06-10T13:36:47+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/a8526b61-7bda-406e-b2f5-ce0c3c39d7e9.jsonl
---

## ADR Переробка викликів до локальних моделей: прямий HTTP до omlx замість pi

## Context and Problem Statement
У монорепо є два транспортних шари для звернень до LLM-моделей: `pi` (CLI/SDK) та omlx (`http://localhost:8000`). Виникло питання — чи варто всі виклики до локальних моделей переключити на прямий HTTP до omlx, а `pi` залишити виключно як шар для хмарних провайдерів.

## Considered Options
* Переробити всі виклики до локальних моделей на прямий HTTP до `http://localhost:8000` (omlx), `pi` — тільки для хмарних
* Залишити поточну архітектуру без змін (всі виклики через `pi`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Переробити виклики до локальних моделей на прямий HTTP до omlx", because саме це запитав користувач як бажаний напрямок рефакторингу — `pi` залишається шаром для хмарних провайдерів, omlx (`http://localhost:8000`) бере на себе локальні моделі.

### Consequences
* Good, because розділення відповідальності між локальним (omlx) і хмарним (pi) транспортом стає явним на рівні коду.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Transcript зафіксував запит на дослідження всіх місць використання `pi` CLI/SDK та `http://localhost:8000` у монорепо `/Users/vitalii/www/nitra/cursor`. Агент-дослідник зібрав повну таблицю файлів з викликами, але детальний результат не потрапив у фінальний контекст. Для реалізації — дивитись файли, знайдені агентом через пошук по `pi` CLI-spawn та `omlx`/`localhost:8000` імпортах.
