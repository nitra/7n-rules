---
session: 04b2495e-073f-4746-b20e-f759aced6c90
captured: 2026-05-09T07:33:14+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/04b2495e-073f-4746-b20e-f759aced6c90.jsonl
---

## Runbook n-fix: усунення порушень правила `n-changelog`

**Контекст:** `npx @nitra/cursor check` повертав `❌` для правила `changelog` — були відсутні `CHANGELOG.md` у кореневому workspace `n-cursor` (`1.0.0`) та у `demo/`, хоча `npm/CHANGELOG.md` вже існував і був актуальний.

**Рішення/Процедура/Факт:**

1. Прочитати `npm/CHANGELOG.md` та `git diff dev..HEAD` щоб зрозуміти обсяг змін.
2. Збумпити версію кореневого `package.json`: `1.0.0 → 1.0.1`.
3. Створити `CHANGELOG.md` в корені з записом `[1.0.1] - 2026-05-09` (Added/Changed/Removed по зводу змін гілки відносно `dev`).
4. Створити `demo/CHANGELOG.md` з початковим записом `[0.0.0] - 2026-05-09` (package `private: true`, змін не було).
5. Запустити `oxfmt .` — форматування зачепило `.n-cursor.json`, `npm/scripts/auto-skills.mjs`, `npm/scripts/check-hasura.mjs`, `npm/scripts/utils/bun-sql-scan.mjs`, `npm/scripts/utils/conn-file-rules.mjs`.
6. Перевірити нові файли через `npx cspell CHANGELOG.md demo/CHANGELOG.md` — прибрати технічні терміни (`conftest`) з тексту CHANGELOG, щоб не додавати нових `cspell`-зауважень поверх 680 пре-існуючих.
7. `npx @nitra/cursor check` → **14/14 правил без зауважень**.

**Обґрунтування:** Правило `n-changelog` вимагає власного `CHANGELOG.md` у кожному workspace з `package.json.workspaces`, плюс кореневому пакеті, плюс `npm/`. Workspace `demo` (`private: true`) та кореневий пакет не мали цих файлів, бо додавались поступово й вимога поширилась на них пізніше.

**Розглянуті альтернативи:** Не обговорювались. Єдиний шлях — створити відсутні файли та збумпити версію, бо правило не допускає виключень для `private`-пакетів.

**Зачіпає:** `package.json` (root version), `CHANGELOG.md` (новий), `demo/CHANGELOG.md` (новий), `npm/CHANGELOG.md` (вже існував, не змінювався).

---

## Knowledge cspell: 680 пре-існуючих зауважень для технічних термінів

**Контекст:** `bun run lint-text` при перевірці всього репо видає ~680 `cspell`-зауважень у 91 файлі (терміни `rego`, `conftest` у `package.json:17,21,22` та інших місцях). Ці помилки існували до сесії `/n-fix` і не є її наслідком.

**Рішення/Процедура/Факт:** Під час `/n-fix` нові файли (`CHANGELOG.md`, `demo/CHANGELOG.md`) перевірялись окремо через `npx cspell <files>` і виправлялись до нуля зауважень. Загальний `bun run lint-text` залишається «брудним» через пре-існуючі порушення.

**Обґрунтування:** Розчистка 680 зауважень — окрема задача: потрібно або додати технічні терміни (`rego`, `conftest` та інші) у `.cspell.json#words` або до словника `@nitra/cspell-dict`, або позначити конкретні рядки `// cspell:ignore`. Робити це в рамках `/n-fix` виходило за скоуп.

**Розглянуті альтернативи:** Не обговорювались.

**Зачіпає:** `.cspell.json`, `@nitra/cspell-dict` (зовнішній пакет), `package.json` (root) — потенційно зачеплені при виправленні.
