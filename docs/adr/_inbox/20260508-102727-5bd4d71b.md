---
session: 5bd4d71b-a69d-4c8f-a170-2609d26b437d
captured: 2026-05-08T10:27:27+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/5bd4d71b-a69d-4c8f-a170-2609d26b437d.jsonl
---

Done.

What was done:

- **`npm/mdc/rego.mdc`** — розширено до v1.1: додано секцію VS Code з `tsandall.opa`, `format-on-save`, поясненням `opa.checkOnSave`; оновлено секцію перевірки з описом preflight → `opa check --strict` → `regal lint`.
- **`npm/scripts/lint-rego.mjs`** — перероблено: preflight перевіряє **і `opa`, і `regal`** (з підказкою `brew install`), далі послідовно `opa check --strict <targets>` і `regal lint <targets>`; таргети — `npm/policy/`.
- **`npm/package.json`** — bump до 1.8.206.
- **`npm/CHANGELOG.md`** — додано запис 1.8.206 з описом змін.

`bun run lint-rego` проходить чисто (4 файли, нема порушень).

---

<system-reminder>
The following deferred tools are now available via ToolSearch. Their schemas are NOT loaded — calling them directly will fail with InputValidationError. Use ToolSearch with query "select:<name>[,<name>...]" to load tool schemas before calling them:
AskUserQuestion
CronCreate
CronDelete
CronList
EnterPlanMode
EnterWorktree
ExitPlanMode
ExitWorktree
Monitor
NotebookEdit
PushNotification
RemoteTrigger
TaskOutput
TaskStop
TodoWrite
WebFetch
WebSearch
mcp__claude_ai_Gmail__authenticate
mcp__claude_ai_Gmail__complete_authentication
mcp__claude_ai_Google_Calendar__authenticate
mcp__claude_ai_Google_Calendar__complete_authentication
mcp__claude_ai_Google_Drive__authenticate
mcp__claude_ai_Google_Drive__complete_authentication
</system-reminder>

## ADR Інструментарій для Rego у VS Code та `lint-rego` скрипт

**Контекст:** Проєкт використовує OPA/Rego для policy-файлів, але `rego.mdc` (v1.0) містив лише посилання на `npx @nitra/cursor check rego` без опису VS Code-інтеграції та без документації лінтингового ланцюжка.

**Рішення/Процедура/Факт:**

- `npm/mdc/rego.mdc` оновлено до v1.1: додано секцію VS Code з рекомендацією розширення `tsandall.opa` (від автора OPA), конфігурацію `format-on-save` через `opa fmt` у `.vscode/settings.json`, та пояснення автоматичної діагностики через `opa.checkOnSave`.
- `npm/scripts/lint-rego.mjs` перероблено: preflight перевіряє наявність **і `opa`, і `regal`** у `PATH` (з підказкою `brew install opa regal` при відсутності), далі послідовно запускає `opa check --strict <targets>` (компіляція з типами та strict-режимом) та `regal lint <targets>` (статичний лінтер Styra Regal).
- Таргет лінтингу — `npm/policy/`; розширення через `LINT_TARGETS` у скрипті.
- `npm/package.json` bumped до 1.8.206, `npm/CHANGELOG.md` оновлено.

**Обґрунтування:** `opa check --strict` ловить компіляційні помилки, мертвий код та незадекларовані змінні, тоді як `regal` ловить semantic-порушення `rego.v1`, неявні set-rules та style-відхилення — два інструменти доповнюють один одного. Розширення `tsandall.opa` забезпечує LSP-досвід (hover, go-to-definition, live diagnostics) прямо в редакторі без запуску CLI вручну. Встановлення `opa` і `regal` лише через `PATH` (не в `dependencies`) — аналогічно до підходу з `shellcheck` у `text.mdc`.

**Розглянуті альтернативи:** Не обговорювалися; підхід з preflight + двома послідовними інструментами обрано за аналогією з `lint-ga.mjs`.

**Зачіпає:** `npm/mdc/rego.mdc`, `npm/scripts/lint-rego.mjs`, `npm/package.json`, `npm/CHANGELOG.md`, `.vscode/extensions.json` (рекомендація `tsandall.opa`), `.vscode/settings.json` (налаштування форматування).
