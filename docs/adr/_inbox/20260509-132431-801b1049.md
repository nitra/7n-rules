---
session: 801b1049-fcbb-4bda-ada1-38afc3bf62ad
captured: 2026-05-09T13:24:31+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/801b1049-fcbb-4bda-ada1-38afc3bf62ad.jsonl
---

## ADR HPA і PDB через Kustomize Component (`components/`), заборона локальних `base/hpa.yaml` і `base/pdb.yaml`

**Контекст:** `base/` (dev) не потребує автомасштабування, тому HPA і PDB у `base/hpa.yaml` / `base/pdb.yaml` захаращували dev-конфіг і вимагали `$patch: delete` в overlay-ях для видалення — крихкий шаблон, що ускладнює розуміння дерева.

**Рішення/Процедура/Факт:** Введено канонічну структуру: один Kustomize Component (фіксована директорія `components/`, sibling до `base/`) на сервіс містить `kustomization.yaml` (`apiVersion: kustomize.config.k8s.io/v1alpha1, kind: Component`), `hpa.yaml` і `pdb.yaml` з dev-like значеннями. `base/kustomization.yaml` **не** підключає `components/`. Прод-overlays (`ua/`, `ru/`) підключають `components: [- ../components]` і накладають JSON6902-патчі для реальних `minReplicas`/`maxReplicas`/`minAvailable`. Локальні `base/hpa.yaml` і `base/pdb.yaml` — заборонені на рівні `check-k8s.mjs` file-existence check. Змінені файли: `npm/mdc/k8s.mdc` (переписано розділ HPA/PDB, нові приклади), `npm/scripts/check-k8s.mjs` (додано `validateComponentsForBaseDeployment`, `verifyK8sBaseKustomizeHasNoHpaPdb`, константа `COMPONENTS_DIR='components'`; видалено мертві функції `patchTextDeclaresHpaStrategicDelete`, `kustomizationDeclaresHpaStrategicDelete`, `verifyK8sBaseKustomizeHpaDeletedWhenInherited`), `npm/tests/check-k8s-schema.test.mjs` (замінено тести старої моделі, додано `describe('validateComponentsForBaseDeployment')`), `npm/package.json` (→ `1.8.219`), `npm/CHANGELOG.md`.

**Обґрунтування:** Kustomize Component дозволяє overlay-ям вибірково підключати HPA/PDB лише там, де це потрібно (прод), без примусового `$patch: delete` у кожному dev-overlay. Це робить `base/` чистим і явним: якщо ресурс не включено — він відсутній, а не прихований патчем. Фіксована назва `components/` (замість `scale/`, `hpa-component/`) зменшує варіативність і спрощує повідомлення про помилки й документацію.

**Розглянуті альтернативи:** `$patch: delete` HPA/PDB в base через стратегічний merge (попередня схема) — відкинуто як крихку; окремі директорії `scale/`, `hpa-component/` — відкинуто на користь єдиної канонічної назви `components/`.

**Зачіпає:** `npm/mdc/k8s.mdc`, `npm/scripts/check-k8s.mjs` (публічний API: `validateComponentsForBaseDeployment`, `COMPONENTS_DIR`; видалено: `patchTextDeclaresHpaStrategicDelete`, `kustomizationDeclaresHpaStrategicDelete`), `npm/tests/check-k8s-schema.test.mjs`, `npm/package.json`, `npm/CHANGELOG.md`; усі репозиторії, що використовують `@nitra/cursor` правило `k8s` і мають `<pkg>/k8s/base/hpa.yaml` або `base/pdb.yaml`.
