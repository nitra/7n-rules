---
session: f49b8938-ed7d-4ac5-a6a6-11c28a1a3808
captured: 2026-05-09T12:06:19+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/f49b8938-ed7d-4ac5-a6a6-11c28a1a3808.jsonl
---

## Knowledge `Map.keys()` / `new Set(…)` не мають `.toSorted()` — потрібен spread

**Контекст:** У `npm/scripts/check-k8s.mjs` під час конвертації image-replace patches у блок `images:` для kustomization.yaml двічі викликався `.toSorted()` безпосередньо на результаті `Map.keys()` та `new Set(opIndices)`. Це призводило до runtime-помилок `…toSorted is not a function`, що блокували конвертацію файлів у `run/{auth,nexus-b2b,old-net-backend}/k8s/ru/kustomization.yaml`.

**Рішення/Процедура/Факт:** Два рядки у `npm/scripts/check-k8s.mjs` виправлено:
- рядок ~6252: `byPatch.keys().toSorted(…)` → `[...byPatch.keys()].toSorted(…)`
- рядок ~6299: `new Set(opIndices).toSorted(…)` → `[...new Set(opIndices)].toSorted(…)`
Обидва місця охоплено одним записом `### Fixed` у CHANGELOG (версія `1.8.214`).

**Обґрунтування:** `Map.keys()`, `Map.values()`, `Map.entries()` та конструктор `new Set(…)` повертають ітератори (`MapIterator`, `SetIterator`), а не масиви. Метод `.toSorted()` є лише на `Array.prototype`. Spread-оператор `[...iterator]` матеріалізує ітератор у масив і дає доступ до всіх Array-методів — мінімальна зміна без втрат продуктивності.

**Розглянуті альтернативи:** `Array.from(iterator).toSorted(…)` — семантично ідентичний підхід, але `[...]` коротший; не обговорювалися інші варіанти.

**Зачіпає:** `npm/scripts/check-k8s.mjs` (функції `applyConversionsToDoc` та `rewriteInlinePatchWithoutOps`), `npm/CHANGELOG.md`.
