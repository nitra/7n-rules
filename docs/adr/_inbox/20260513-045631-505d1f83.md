---
session: 505d1f83-a62d-4588-93a3-e884d625840b
captured: 2026-05-13T04:56:31+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/505d1f83-a62d-4588-93a3-e884d625840b.jsonl
---

exit: 0
[assistant]
lint-js чистий (exit 0). Три попередження усунуто:
- `npm/scripts/lint-ga.mjs:11,146` — `@nitra` в JSDoc → `\@nitra` (escaping inline tag).
- `npm/scripts/check-npm-module.mjs:369` — `// eslint-disable-next-line security/detect-non-literal-regexp` з поясненням (dynamic glob→regex — навмисно).

---

This session captured two significant decisions. Let me produce the ADR output.

## ADR Компактний npm-пакет: правила, перевірки та розподіл JS/Rego

**Контекст:** Пакет `@nitra/cursor` публікував усе, що потрапляло до `npm/` без явного whitelist, а тести, фікстури та dev-залежності могли непомітно виявитися у tarball. Потрібно було формалізувати правило «максимально компактний пакет» і забезпечити його автоматичну перевірку.

**Рішення/Процедура/Факт:**
1. **Правило `npm-module.mdc` (v1.10 → v1.11)** — додано три вимоги: (а) `"files"` whitelist обов'язковий у `npm/package.json`; (б) тести й фікстури поза будь-яким опублікованим шляхом (виняток — `*_test.rego`, які за конвенцією conftest лежать поруч із полісі у тому самому `package`); (в) лише runtime-залежності, `devDependencies` — у кореневий `package.json`.
2. **`npm/package.json`** — прибрано `devDependencies` (`@nitra/cursor` уже у кореневому `workspace:*`).
3. **`npm/policy/npm_module/npm_package_json/npm_package_json.rego`** — додано два deny: `files` обов'язковий і непорожній; `devDependencies` заборонені. Написано `npm_package_json_test.rego` (happy path + 7 негативних кейсів).
4. **`npm/scripts/check-npm-module.mjs`** — `checkPackageCompactness` видалено (мігровано у Rego); залишено тільки `checkNoTestsInPublishedFiles` (FS walk + oxc-parser AST — ці перевірки Rego не вміє). Нові exported helpers: `globToRegex`, `findTestFrameworkImport`, `classifyPublishedFileAsTest`.
5. **`npm/tests/check-npm-module.test.mjs`** — 14 нових тестів для glob-матчера, AST-сканера і класифікатора.
6. **`conftest.mdc` + `npm/CLAUDE.md` / `.claude-template/npm-CLAUDE.md`** — посилений STOP-блок «перед редагуванням `check-*.mjs` перевір rego-пакет»; додано приклад «`devDependencies` / `scripts.<name>` — пер-документно → rego».
7. **`knip.json`** — прибрано чотири hints: `graphql`, `vue`, `@nitra/cursor`, `vite`.
8. **`eslint.config.js`** — `.claude/worktrees/**` додано до `ignores`.
9. **`npm/policy/ga/workflow_common/workflow_common.rego`** — довгий `sprintf`-шаблон винесено у константу `forbidden_run_command_template` (regal `line-length`).

**Обґрунтування:** `"files"` без whitelist змушує npm пакувати майже все — тести/фікстури збільшують розмір і плутають користувачів. `devDependencies` у `npm/package.json` тягнуться при `npm install`, хоча потрібні лише для розробки пакета. Пер-документні структурні правила (форма полів, наявність/відсутність ключів верхнього рівня) — природна зона Rego; JS+AST виправданий лише там, де потрібен обхід файлової системи або синтаксичний аналіз коду.

**Розглянуті альтернативи:**
- Для rego-тестів (`*_test.rego`) обговорювались: (а) виключити через `"!**/*_test.rego"` у `files` (реалізовано спочатку, потім відкинуто); (б) фізично перенести у `npm/tests/policy/` (потребувало б переписати `lint-rego.mjs`); (в) вважати `policy/` з `*_test.rego` допустимим — **обрано**, бо conftest-конвенція передбачає тест поруч із пакетом.
- `checkPackageCompactness` у JS — обговорювалось залишити в JS, але мігровано у Rego відповідно до проєктної конвенції `conftest.mdc`.

**Зачіпає:** `npm/mdc/npm-module.mdc`, `.cursor/rules/n-npm-module.mdc`, `npm/scripts/check-npm-module.mjs`, `npm/tests/check-npm-module.test.mjs`, `npm/policy/npm_module/npm_package_json/npm_package_json.rego`, `npm/policy/npm_module/npm_package_json/npm_package_json_test.rego`, `npm/package.json`, `npm/CHANGELOG.md`, `.cursor/rules/conftest.mdc`, `npm/CLAUDE.md`, `npm/.claude-template/npm-CLAUDE.md`, `knip.json`, `eslint.config.js`, `npm/policy/ga/workflow_common/workflow_common.rego`, `npm/scripts/lint-ga.mjs`.
