---
session: 17e7fc53-4666-44dc-afce-5394aacc0141
captured: 2026-05-08T12:55:45+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/17e7fc53-4666-44dc-afce-5394aacc0141.jsonl
---

зміни у `<root>`: check вже падав до моїх правок (dev version bump потрібен для кореня, не npm-пакету — це окрема вимога). Це не регресія.
[assistant]
[tool: Bash] {"command":"git stash pop","description":"pop stash"}
[user]
[tool_result] On branch main: main: 1 times
On branch main
Your branch is up to date with 'origin/main'.

Changes not staged for commit:
(use "git add <file>" to restore changes to restore the content to staging)

modified:   npm/CHANGELOG.md
modified:   npm/mdc/js-bun-db.mdc
modified:   npm/package.json
modified:   npm/scripts/check-js-bun-db.mjs
modified:   npm/scripts/utils/bun-sql-scan.mjs
[assistant]
[thinking]

[assistant]
All five files are correctly modified. Let me verify the auto-rules regeneration step — the patch notes mention regenerating `bin/auto-rules.md` if needed.
[assistant]
[tool: Bash] {"command":"grep -n 'auto-rules' /Users/vitalii/www/nitra/cursor/npm/package.json | head -10","description":"Check if there is a generate script"}
[user]
[tool_result] (Bash completed with no output)
[assistant]
[tool: Bash] {"command":"head -20 /Users/vitalii/www/nitra/cursor/npm/bin/auto-rules.md","description":"Check auto-rules.md"}
[user]
[tool_result] # auto-rules.md vers. 0.5

# Format: one rule per line, syntax — «id - condition»

# Умова — довільний текст, описує ознаки проєкту, коли правило вмикається

# Порожні рядки та рядки з '#' — коментарі

js-bun-db - якщо в хоч одному package.json в секції dependencies присутній пакет pg, pg-format або mysql2 або є імпорт sql/SQL з Bun (приклад: import { sql } from "bun")
bun - якщо у проєкті використовується Bun (наявність bun.lockb, bunfig.toml або bun у package.json engines)
...
[assistant]
`bin/auto-rules.md` is a hand-maintained file, not generated — no regeneration step needed. The patch is fully applied.

Let me do a quick diff summary to confirm everything is in order:
[assistant]
[tool: Bash] {"command":"git diff --stat HEAD npm/","description":"git diff stat for npm/"}
[user]
[tool_result] npm/CHANGELOG.md                        | 10 ++++++++++
npm/mdc/js-bun-db.mdc                  | 56 ++++++++++++++++++++++++++++++++++
[assistant]
All five files changed as expected:

- `npm/mdc/js-bun-db.mdc` — bumped version to `1.6`, added `## pg-format: повне видалення, без шимів` section with idiom table, forbidden shim examples, and expanded "Не лишати" clause
- `npm/scripts/utils/bun-sql-scan.mjs` — added `templateQuasisText` import; new constants (`PG_FORMAT_PLACEHOLDER_RE`, `PG_FORMAT_SHIM_FUNC_NAMES`, `QUOTE_HELPER_NAMES`); added four functions: `isUnsafeCallNode`, `nodeContainsPgFormatPlaceholder`, `asNamedFunctionDecl`, `nodeContainsUnsafeCall`; exported `findPgFormatShimDefinitionInText` and `findPgFormatLikeQueryWrapperInText`
- `npm/scripts/check-js-bun-db.mjs` — wired both new scanners, expanded `counts` object, added two pass/fail message branches, updated JSDoc
- `npm/package.json` — `1.8.209` → `1.8.210`
- `npm/CHANGELOG.md` — new `[1.8.210]` entry

All 15 existing tests pass.

---

## ADR js-bun-db: заборона pg-format-шимів — AST-детектори v1.6

**Контекст:** При міграції з `pg-format` на Bun SQL у кодових базах виникав антипатерн — збереження `pg-format`-сумісного API під новим іменем (`format()`, `pgRead.query(text, params)`) з внутрішнім викликом `sql.unsafe(...)`. Такий шим зберігав injection-поверхню, від якої мала відбутись міграція, просто ховаючи її за «зручним» іменем.

**Рішення/Процедура/Факт:** У правило `js-bun-db.mdc` (v1.5 → v1.6) додано розділ «pg-format: повне видалення, без шимів» з таблицею ідіом міграції та прикладами забороненого коду. У `bun-sql-scan.mjs` реалізовано два нові AST-детектори: `findPgFormatShimDefinitionInText` (виявляє функції з іменами `format`/`pgFormat`/`sqlFormat`/`pgFmt`, у тілі яких є `%L`/`%I`/`%s`, а також будь-які `quoteLiteral`/`quoteIdent`/`escapeLiteral`/`escapeIdent`) та `findPgFormatLikeQueryWrapperInText` (виявляє об'єктний метод `query(text, params)` з `*.unsafe(...)` у тілі). Обидва детектори спрацьовують лише у файлах з `import { sql|SQL } from 'bun'`. Оркестратор `check-js-bun-db.mjs` підключено до обох детекторів з відповідними повідомленнями `fail` і `pass`. Версія пакету `1.8.209` → `1.8.210`.

**Обґрунтування:** Шим `format()` → `sql.unsafe(format(...))` є логічно еквівалентним прямому `sql.unsafe` з конкатенацією рядків — injection-вектор не усунено. Явний AST-детектор в `check-js-bun-db` дозволяє автоматично виявляти такий технічний борг на CI, а не покладатись на code-review. Детектор навмисно прив'язаний до наявності `import sql from 'bun'`, щоб не генерувати хибнопозитивні спрацьовування для форматерів дат чи URL у не-SQL коді.

**Розглянуті альтернативи:** Не обговорювалися; підхід (AST-детектор у межах існуючого `check-js-bun-db`) був заданий у патчі.

**Зачіпає:** `npm/mdc/js-bun-db.mdc`, `npm/scripts/utils/bun-sql-scan.mjs` (експорти `findPgFormatShimDefinitionInText`, `findPgFormatLikeQueryWrapperInText`), `npm/scripts/check-js-bun-db.mjs`, `npm/package.json`, `npm/CHANGELOG.md`
