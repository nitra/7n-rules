---
session: d943f92b-e895-4110-8ae0-338380a03c95
captured: 2026-06-06T19:34:19+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/d943f92b-e895-4110-8ae0-338380a03c95.jsonl
---

Дивлюся на три вже існуючі файли і знаходжу, що основне рішення вже задокументовано тричі. Ось мій аналіз:

---

## Що вже є (і перекривається)

Три файли покривають **одне й те саме рішення** — `sym ≥ 4` routing:
- `260606-1654-...` — перший запис (з першої половини сесії)
- `260606-1734-...` — розширений, з емпіричним підтвердженням sym=4–7
- `260606-1932-...` — ще один, з цієї відновленої сесії

Відхилення A і B вже у "Considered Options" у всіх трьох. **Дублювання є, нового ADR для цього не потрібно.**

---

## Де є реальний gap — що варто додати

Я бачу **одне рішення**, яке ніде не зафіксоване:

**Виключення `npm/reports/**` і `npm/bin/**` з docgen** — це не тривіальний config. За ним стоїть:
- Stryker sandboxes надували 938 → 241 файл (x4 inflation)
- `npm/bin/n-cursor.js` — бандл, не джерельний код
- Без цього exclusion tier-аудит та batch дають хибні дані

Але чесно: це дуже маленьке рішення. Сумнівно чи варто ADR. Скоріше — коментар у `docgen-ignore.mjs`.

---

## Що точно **не** варто як ADR

- Розміщення `docgen-batch.mjs` у `npm/skills/docgen/js/` — implementation detail
- Порядок (cloud → local) у batch — impl. detail
- Повний прогін 58 файлів — операційне, не архітектурне

---

## Мій висновок

Три існуючі ADR — **перебір для одного рішення**. Я б **не додавав** новий ADR з цієї сесії, а замість цього **злив** три в один (найповніший — `1932`), видалив `1654` і `1734`.

Хочеш так зробити — очистити дублікати і залишити один канонічний?
