---
session: 45a1997b-a862-4bad-aa42-299f0ed8f886
captured: 2026-06-20T07:07:01+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/45a1997b-a862-4bad-aa42-299f0ed8f886.jsonl
---

Враховуючи, що `lint-ga`, `lint-text`, `lint-js`, `lint-style` мусять лишитись у package.json через жорсткі CI/policy-вимоги — і вони залишаться — агрегат `"lint"` у B не стає порожнім: він просто делегує замість переліку. Рекомендація: **B** (мінімальна ламкість, задовольняє CI, `bun run lint` як зручний аліас). Але якщо пріоритет — «прибрати подвійне», кажи **A**, і я переписую Rego.
[user]
B
[assistant]
[thinking]

[assistant]
Прийнято. Фіналізуємо:

**Цільовий стан `package.json`:**
- `"lint"` → `"n-cursor lint --full"`
- Видаляємо: `"lint-doc-files"`, `"lint-python"`, `"lint-rego"`, `"lint-security"`, standalone `"oxfmt"`
- Лишаємо: `"lint-js"`, `"lint-style"`, `"lint-ga"`, `"lint-text"` (вимагаються CI/Rego-правилами)

**Rego `bun.package_json.rego`** — міняємо вимогу змісту агрегату: замість перерахування `&& bun run lint-*` → `"lint"` мусить мати значення `"n-cursor lint --full"`.

Перш ніж міняти Rego, прочитаю template/snippet щоб точно знати структуру і не зламати інші правила.
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== bun rule template/package.json files ===\"\nls -1 npm/rules/bun/template/ 2>/dev/null\necho\ncat npm/rules/bun/template/package.json.deny.json 2>/dev/null\necho\n# Шукаємо snippet якщо є\ncat npm/rules/bun/template/package.json.snippet.json 2>/dev/null"}
[user]
[tool_result] === bun rule template/package.json files ===
npm/rules/bun/template/package.json.deny.json
npm/rules/bun/template/package.json.snippet.json

{"name":"{{.Name}}","version":"{{.Version}}","private":{{.Private}},"workspaces":["npm","demo"],"type":"module","scripts":{"start":"bun ./npm/bin/n-cursor.js","test":"vitest run","lint-js":"bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints","lint-style":"npx stylelint
[assistant]
[thinking]

[assistant]
The snippet is exactly `package.json`'s script area. I need to:

1. Update `bun/template/package.json.snippet.json` to remove obsolete `lint-*` and update `"lint"` value
2. Update `bun/policy/package_json/package_json.rego` to match the new `"lint"` constraint (exact value instead of pattern)
3. Update `package.json` itself

First let me read the full current state of the template:
[assistant]
[tool: Bash] {"command":"cat npm/rules/bun/template/package.json.snippet.json"}
[user]
[tool_result] {"name":"{{.Name}}","version":"{{.Version}}","private":{{.Private}},"workspaces":["npm","demo"],"type":"module","scripts":{"start":"bun ./npm/bin/n-cursor.js","test":"vitest run","lint-js":"bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints","lint-style":"npx stylelint **/*.{css,scss,vue} --fix && npx stylelint \"**/*.{css,scss,vue}\"","lint-ga":"bun run npm/rules/ga/lint/lint.mjs","lint-text":"bun run lint-text-core && bun run lint-text-extra","lint-text-core":"...(cut)","lint-text-extra":"...(cut)","oxfmt":"oxfmt .","lint":"bun run lint-doc-files && bun run lint-ga && bun run lint-js && bun run lint-rego && bun run lint-text && bun run lint-style && bun run lint-python && bun run lint-security && oxfmt .","lint-doc-files":"...(cut)","lint-rego":"...(cut)","lint-python":"...(cut)","lint-security":"...(cut)","postlint":"...(cut)"}...}
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/template/package.json.snippet.json"}
[user]
[tool_result] 1	{"name":"{{.Name}}","version":"{{.Version}}","private":{{.Private}},"workspaces":["npm","demo"],"type":"module","scripts":{"start":"bun ./npm/bin/n-cursor.js","test":"vitest run","lint-js":"bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints","lint-style":"npx stylelint **/*.{css,scss,vue} --fix && npx stylelint \"**/*.{css,scss,vue}\"","lint-ga":"bun run npm/rules/ga/lint/lint.mjs","lint-text":"bun run lint-text-core && bun run lint-text-extra","lint-text-core":"cspell **/* --no-progress --gitignore && shellcheck --external-sources $(git ls-files '*.sh') && markdownlint **/*.md --ignore node_modules","lint-text-extra":"v8r --cache && dotenv-linter .env .env.*","oxfmt":"oxfmt .","lint":"bun run lint-doc-files && bun run lint-ga && bun run lint-js && bun run lint-rego && bun run lint-text && bun run lint-style && bun run lint-python && bun run lint-security && oxfmt .","lint-doc-files":"bun run npm/rules/doc-files/lint/lint.mjs","lint-rego":"bun run npm/rules/rego/lint/lint.mjs && opa check npm/","lint-python":"bun run npm/rules/python/lint/lint.mjs","lint-security":"trufflehog filesystem . --no-update --exclude-paths .trufflehog-exclude --fail","postlint":"n-cursor sync","scripts_end":"..."},"dependencies":{...},"devDependencies":{...},"version_end":"..."}
[assistant]
[thinking]

[assistant]
Now I have the complete template. The plan:

1. **`package.json.snippet.json`**: Remove `lint-doc-files`, `lint-rego`, `lint-python`, `lint-security`, `oxfmt` scripts. Update `lint` to `"n-cursor lint --full"`.

2. **`package.json`** (root): Same removals and update `"lint"`.

3. **`bun.package_json.rego`**: Update the validation logic — instead of checking that `lint` = concatenated list of `lint-*` + `oxfmt .`, check that `lint` = `"n-cursor lint --full"`.

4. **Delete ADR `відокремлення-oxfmt-від-lint-ланцюжка.md`**.

5. **Update ADR `уніфікація-lint-bun-run-lint-в-n-cursor-lint.md`** if needed.

Let me start:
[assistant]
[tool: Write] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/template/package.json.snippet.json","content":"{\"name\":\"{{.Name}}\",\"version\":\"{{.Version}}\",\"private\":{{.Private}},\"workspaces\":[\"npm\",\"demo\"],\"type\":\"module\",\"scripts\":{\"start\":\"bun ./npm/bin/n-cursor.js\",\"test\":\"vitest run\",\"lint-js\":\"bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints\",\"lint-style\":\"npx stylelint **/*.{css,scss,vue} --fix && npx stylelint \\\"**/*.{css,scss,vue}\\\"\",\"lint-ga\":\"bun run npm/rules/ga/lint/lint.mjs\",\"lint-text\":\"bun run lint-text-core && bun run lint-text-extra\",\"lint-text-core\":\"cspell **/* --no-progress --gitignore && shellcheck --external-sources $(git ls-files '*.sh') && markdownlint **/*.md --ignore node_modules\",\"lint-text-extra\":\"v8r --cache && dotenv-linter .env .env.*\",\"lint\":\"n-cursor lint --full\",\"postlint\":\"n-cursor sync\",\"scripts_end\":\"...\"},\"dependencies\":{...},\"devDependencies\":{...},\"version_end\":\"...\"}"}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/npm/rules/bun/template/package.json.snippet.json has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.rego"}
[user]
[tool_result] 1	# Перевірка кореневого `package.json` для bun (bun.mdc).
2	#
3	# Канон надходить через --data: { "template": { "deny": ... } }
4	# Структура --data сформована з template/package.json.deny.json
[assistant]
[tool: Bash] {"command":"wc -l npm/rules/bun/policy/package_json/package_json.rego"}
[user]
[tool_result] 97 npm/rules/bun/policy/package_json/package_json.rego"}
[user]
[tool_result] 97 npm/rules/bun/policy/package_json/package_json.rego
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.rego","offset":40,"limit":60}
[user]
[tool_result] 40
41	# ── lint-скрипти ────────────────────────────────────────────────────────────────
42	# Правило: якщо існують `lint-*` скрипти, повинен бути агрегатний `lint`-скрипт,
43	# що через `bun run <name>` викликає їх УСІХ і закінчується на `&& oxfmt .`.
44	#
45	# TODO: Після переходу на `n-cursor lint --full` видалити це правило і замінити
46	# простішим: lint == "n-cursor lint --full".
47	#
48	input_scripts := input.scripts
49	lint_script := input_scripts.lint
50
51	lint_prefixed_scripts := {k |
52	   some k, _ in input_scripts
53	   startswith(k, "lint-")
54	}
55
54	lint_prefixed_scripts := {k |
55	   some k, _ in input_scripts
56	   startswith(k, "lint-")
57	}
58
59	deny[msg] {
60	   count(lint_prefixed_scripts) > 0
61	   not input_scripts["lint"]
62	   msg := sprintf(lint_aggregate_missing_template, [lint_prefixed_scripts])
63	}
64
65	deny[msg] {
66	   count(lint_prefixed_scripts) > 0
67	   lint_script
68	   not endswith(lint_script, "&& oxfmt .")
69	   msg := sprintf(lint_aggregate_wrong_template, [lint_prefixed_scripts])
70	}
[assistant]
[thinking]

[assistant]
The rego has a clear TODO note in the right place (lines 45–46). I need to:
1. Replace the lint validation block (lines 40–70) to just check `lint == "n-cursor lint --full"`
2. Read the full file first to see what's after line 70
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.rego","offset":68,"limit":30}
[user]
[tool_result] 68	   not endswith(lint_script, "&& oxfmt .")
69	   msg := sprintf(lint_aggregate_wrong_template, [lint_prefixed_scripts])
70	}
71
72	deny[msg] {
73	   count(lint_prefixed_scripts) > 0
74	   lint_script
75	   endswith(lint_script, "&& oxfmt .")
76	   not contains(lint_script, "&& oxfmt .")
77	   msg := "bug: impossible; OPA lint check reached never-state"
78	}
79
80	# ── повідомлення-шаблони ────────────────────────────────────────────────────────
81	80	lint_aggregate_missing_template := "lint-* scripts exist but aggregate `lint` script is missing. Declare: %v"
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.rego","offset":78,"limit":20}
[user]
[tool_result] 78	}
79
80	# ── повідомлення-шаблони ────────────────────────────────────────────────────────
81	lint_aggregate_missing_template := "lint-* scripts exist but aggregate `lint` script is missing. Declare: %v"
82	lint_aggregate_wrong_template := "aggregate `lint` script exists but does not end with `&& oxfmt .`. Add all lint-* scripts: %v"
83
84	# ── postlint ────────────────────────────────────────────────────────────────────
85	# postlint мусить бути "n-cursor sync".
86	#
87	deny[msg] {
88	   not input_scripts["postlint"]
89	   msg := postlint_missing_msg
90	}
91
92	deny[msg] {
93	   input_scripts["postlint"]
94		 input_scripts.postlint != "n-cursor sync"
93	   input_scripts["postlint"]
94		 input_scripts.postlint != "n-cursor sync"
95	   msg := postlint_wrong_msg
96	}
97
98	# ── повідомлення ────────────────────────────────────────────────────────────────
99
100	postlint_missing_msg := "postlint script is missing. Add: \"postlint\": \"n-cursor sync\""
101	postlint_wrong_msg := "postlint script is wrong. Expected: \"postlint\": \"n-cursor sync\""
[assistant]
[thinking]

[assistant]
Now I understand the full structure. I'll replace the lint block (lines 41-82) with a simplified check: `lint` must equal `"n-cursor lint --full"`.
[assistant]
[tool: Edit] {"replace_all":false,"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.rego","old_string":"# ── lint-скрипти ────────────────────────────────────────────────────────────────\n# Правило: якщо існують `lint-*` скрипти, повинен бути агрегатний `lint`-скрипт,\n# що через `bun run <name>` викликає їх УСІХ і закінчується на `&& oxfmt .`.\n#\n# TODO: Після переходу на `n-cursor lint --full` видалити це правило і замінити\n# простішим: lint == \"n-cursor lint --full\".\n#\ninput_scripts := input.scripts\nlint_script := input_scripts.lint\n\nlint_prefixed_scripts := {\n   some k, _ in input_scripts\n   startswith(k, \"lint-\")\n}\n\ndeny[msg] {\n   count(lint_prefixed_scripts) > 0\n   not input_scripts[\"lint\"]\n   msg := sprintf(lint_aggregate_missing_template, [lint_prefixed_scripts])\n}\n\ndeny[msg] {\n   count(lint_prefixed_scripts) > 0\n   lint_script\n   not endswith(lint_script, \"&& oxfmt .\")\n   msg := sprintf(lint_aggregate_wrong_template, [lint_prefixed_scripts])\n}\n\ndeny[msg] {\n   count(lint_prefixed_scripts) > 0\n   lint_script\n   endswith(lint_script, \"&& oxfmt .\")\n   not contains(lint_script, \"&& oxfmt .\")\n   msg := \"bug: impossible; OPA lint check reached never-state\"\n}\n\n# ── повідомлення-шаблони ────────────────────────────────────────────────────────\nlint_aggregate_missing_template := \"lint-* scripts exist but aggregate `lint` script is missing. Declare: %v\"\nlint_aggregate_wrong_template := \"aggregate `lint` script exists but does not end with `&& oxfmt .`. Add all lint-* scripts: %v\"","new_string":"# ── lint-скрипти ────────────────────────────────────────────────────────────────\n# `lint` мусить делегувати в оркестратор: \"n-cursor lint --full\".\n#\ninput_scripts := input.scripts\nlint_script := input_scripts.lint\n\ndeny[msg] {\n   not input_scripts[\"lint\"]\n   msg := lint_missing_msg\n}\n\ndeny[msg] {\n   input_scripts[\"lint\"]\n   lint_script != \"n-cursor lint --full\"\n   msg := sprintf(lint_wrong_template, [lint_script])\n}\n\n# ── повідомлення-шаблони ────────────────────────────────────────────────────────\nlint_missing_msg := \"lint script is missing. Add: \\\"lint\\\": \\\"n-cursor lint --full\\\"\"\nlint_wrong_template := \"lint script is wrong (%v). Expected: \\\"lint\\\": \\\"n-cursor lint --full\\\"\""}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.rego has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[thinking]

[assistant]
Now let me update `package.json` itself. Let me first see its current state:
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/package.json"}
[user]
[tool_result] 1	{
2	  "name": "mono",
2	  "name": "mono",
3	  "version": "1.0.0",
4	  "private": true,
5	  "workspaces": [
6	    "npm",
7	    "demo"
8	  ],
9	  "type": "module",
10	  "scripts": {
11	    "start": "bun ./npm/bin/n-cursor.js",
12	    "test": "vitest run",
13	    "lint-js": "bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints",
14	    "lint-style": "npx stylelint **/*.{css,scss,vue} --fix && npx stylelint \"**/*.{css,scss,vue}\"",
15	    "lint-ga": "bun run npm/rules/ga/lint/lint.mjs",
16	    "lint-text": "bun run lint-text-core && bun run lint-text-extra",
17	    "lint-text-core": "cspell **/* --no-progress --gitignore && shellcheck --external-sources $(git ls-files '*.sh') && markdownlint **/*.md --ignore node_modules",
18	    "lint-text-extra": "v8r --cache && dotenv-linter .env .env.*",
19	    "oxfmt": "oxfmt .",
20	    "lint": "bun run lint-security",
20	    "lint": "bun run lint-security",
21	    "postlint": "n-cursor sync"
22	  },
23	  "dependencies": {
24	  "dependencies": {
25	    "@nitra/cursor": "workspace:*"
26	  },
27	  "devDependencies": {
28	    "vitest": "*"
29	  }
30	}
[assistant]
[thinking]

[assistant]
The current `package.json` is in a half-written state: `"lint": "bun run lint-security"` and missing `lint-security`, `lint-doc-files`, etc., but still has `oxfmt`. Let me update it to the target state: remove `oxfmt`, set `"lint": "n-cursor lint --full"`.
[assistant]
[tool: Edit] {"replace_all":false,"file_path":"/Users/vitalii/www/nitra/cursor/package.json","old_string":"    \"oxfmt\": \"oxfmt .\",\n    \"lint\": \"bun run lint-security\",\n    \"postlint\": \"n-cursor sync\"","new_string":"    \"lint\": \"n-cursor lint --full\",\n    \"postlint\": \"n-cursor sync\""}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/package.json has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[thinking]

[assistant]
Now delete the ADR file about oxfmt separation, and update the existing ADR about lint unification if needed:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\nrm -f \"docs/adr/відокремлення-oxfmt-від-lint-ланцюжка.md\"\nls docs/adr/ | grep -i oxfmt 2>/dev/null || echo \"ADR видалено\""}
[user]
[tool_result] ADR видалено
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/docs/adr/уніфікація-lint-bun-run-lint-в-n-cursor-lint.md"}
[user]
[tool_result] 1	---
2	type: ADR
3	title: "Уніфікація lint-інфраструктури: прибирання кореневого bun run lint на користь n-cursor lint"
[assistant]
[thinking]

[assistant]
[tool: Bash] {"command":"wc -l 'docs/adr/уніфікація-lint-bun-run-lint-в-n-cursor-lint.md'"}
[user]
[tool_result] 65 docs/adr/уніфікація-lint-bun-run-lint-в-n-cursor-lint.md
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/docs/adr/уніфікація-lint-bun-run-lint-в-n-cursor-lint.md","offset":20,"limit":46}
[user]
[tool_result] 20
21	## Considered Options
22
23	* **A) Єдиний аліас** (`"lint": "n-cursor lint --full"`) — `bun run lint` делегує в оркестратор.
24	* **B) Повне видалення** — `bun run lint` зникає; єдина точка входу — `n-cursor lint --full`.
25
26	## Decision Outcome
27
26	## Decision Outcome
27
28	Chosen option: **A (аліас)**, because:
29	- Consumer repos мають `bun run lint` у `postcommit`/Makefile; breaking change зайвий.
29	- Consumer repos мають `bun run lint` у `postcommit`/Makefile; breaking change зайвий.
30	- Rego правило оновлюється на `lint == "n-cursor lint --full"` замість ланцюга.
30	- Rego правило оновлюється на `lint == "n-cursor lint --full"` замість ланцюга.
31	- `bun run lint` лишається зручним аліасом без дублювання логіки.
31	- `bun run lint` лишається зручним аліасом без дублювання логіки.
32
32
33	### Consequences
33	### Consequences
34
34
35	35	* Good, because консумери можуть зберегти `bun run lint` звичку без нових знань.
35	* Good, because консумери можуть зберегти `bun run lint` звичку без нових знань.
36	36	* Good, because дублювання логіки прибрано — правила живуть у `n-cursor`.
37	37	* Good, because `postlint: n-cursor sync` лишається незмінним.
38	38	* Bad, because лишається формальний `bun run lint` (але без власної логіки).
39	39
40	40	## More Information
41	41
42	42	- `npm/rules/bun/policy/package_json/package_json.rego` — оновлено вимогу.
43	43	- `npm/rules/bun/template/package.json.snippet.json` — оновлено шаблон.
44	44	- `package.json` (кореневий) — `"lint": "n-cursor lint --full"`.
45	45	- `npm/rules/lint/js/orchestrate.mjs` — oxfmt інтегровано як fix-крок.
46	46	- `npm/rules/python/js/lint.mjs` + `meta.json` — python інтегровано.
47	47	- `npm/rules/python/lint/lint.mjs` — додано `readOnly` параметр.
48	48
49	49	### Scripts видалено з `package.json` і `snippet.json`
50	49	### Scripts видалено з `package.json` і `snippet.json`
50	50
51	50
52	51	- `lint-doc-files`
53	52	- `lint-rego`
54	53	- `lint-python`
55	54	- `lint-security`
56	55	- `oxfmt` (standalone)
57	56
58	57	### Scripts що залишились (потрібні CI/Rego)
59	58
60	59	- `lint-js` — вимагається `js-lint` Rego
61	60	- `lint-style` — вимагається `style-lint` Rego
62	61	- `lint-ga` — вимагається `lint-ga.yml` CI
63	62	- `lint-text` — вимагається `lint-text.yml` CI
64	63
65	64
66	65
67	66
[assistant]
[thinking]

[assistant]
The ADR "уніфікація-lint" is already accurate and complete — it matches exactly what we decided and implemented. No edits needed.

Now let's check if there are Rego tests that test the old `lint_aggregate` logic that I need to update:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== Rego tests for bun package_json ===\"\nls -1 npm/rules/bun/policy/package_json/\necho\ncat npm/rules/bun/policy/package_json/*.test.rego 2>/dev/null || grep -rn \"oxfmt\\|lint_aggregate\\|lint-prefixed\" npm/rules/bun/policy/ 2>/dev/null"}
[user]
[tool_result] === Rego tests for bun package_json ===
package_json.rego
package_json.test.rego

[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.test.rego"}
[user]
[tool_result] 1	package bun_test
2
3	import rego.v1
4
5	import data.bun
6
7	# Утиліта: базова конфігурація (пасс без помилок)
8	8	base_input := {
8	base_input := {
9	9	  "scripts": {
9	  "scripts": {
10	10	    "start": "bun ./npm/bin/n-cursor.js",
11	11	    "test": "vitest run",
12	12	    "lint-js": "bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints",
13	13	    "lint-style": "npx stylelint ...",
14	14	    "lint-ga": "bun run npm/rules/ga/lint/lint.mjs",
15	15	    "lint-text": "bun run lint-text-core && bun run lint-text-extra",
16	16	    "lint": "bun run lint-doc-files && bun run lint-ga && ...",
17	16	    "lint": "bun run lint-doc-files && bun run lint-ga && ...",
17	17	    "postlint": "n-cursor sync"
18	18	  },
19	19	  "name": "foo",
20	20	  ...
21	21	}
[assistant]
[tool: Bash] {"command":"wc -l npm/rules/bun/policy/package_json/package_json.test.rego"}
[user]
[tool_result] 67 npm/rules/bun/policy/package_json/package_json.test.rego
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.test.rego"}
[user]
[tool_result] 1	package bun_test
2
3	import rego.v1
4
5	import data.bun
6
7	# Утиліта: базова конфігурація (пасс без помилок)
8	base_input := {
9	  "scripts": {
10	    "start": "bun ./npm/bin/n-cursor.js",
11	    "test": "vitest run",
12	    "lint-js": "bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints",
13	    "lint-style": "npx stylelint ...",
14	    "lint-ga": "bun run npm/rules/ga/lint/lint.mjs",
15	    "lint-text": "bun run lint-text-core && bun run lint-text-extra",
16	    "lint": "bun run lint-doc-files && bun run lint-ga && ...",
17	    "postlint": "n-cursor sync"
18	  },
19	  "name": "foo",
20	  "version": "1.0.0",
21	  "private": true,
22	  "workspaces": ["npm", "demo"],
23	  "type": "module"
24	}
25
26	# 1. Happy path — no deny
27	test_base_ok {
28	   count(bun.deny) == 0 with input as base_input
29	}
30
31	# 2. Missing lint script
32	test_lint_missing {
33	   d := bun.deny with input as object.remove(base_input, ["scripts"]) | {"scripts": object.remove(base_input.scripts, ["lint"])}
34	   count(d) > 0
35	   some msg in d; contains(msg, "lint")
35	   some msg in d; contains(msg, "lint")
36	}
36	}
37
37
38	# 3. Wrong lint script (missing oxfmt)
38	# 3. Wrong lint script (missing oxfmt)
39	test_lint_wrong {
39	test_lint_wrong {
40	   wrong := object.remove(base_input, ["scripts"]) | {"scripts": object.union(base_input.scripts, {"lint": "bun run lint-js"})}
40	   wrong := object.remove(base_input, ["scripts"]) | {"scripts": object.union(base_input.scripts, {"lint": "bun run lint-js"})}
41	   d := bun.deny with input as wrong
41	   d := bun.deny with input as wrong
42	   count(d) > 0
42	   count(d) > 0
43	   some msg in d; contains(msg, "oxfmt")
43	   some msg in d; contains(msg, "oxfmt")
44	}
44	}
45
45
46	# 4. postlint missing
46	# 4. postlint missing
47	test_postlint_missing {
47	test_postlint_missing {
48	   d := bun.deny with input as object.remove(base_input, ["scripts"]) | {"scripts": object.remove(base_input.scripts, ["postlint"])}
48	   d := bun.deny with input as object.remove(base_input, ["scripts"]) | {"scripts": object.remove(base_input.scripts, ["postlint"])}
49	   count(d) > 0
49	   count(d) > 0
50	   some msg in d; contains(msg, "postlint")
50	   some msg in d; contains(msg, "postlint")
51	}
51	}
52
52
53	# 5. postlint wrong value
53	# 5. postlint wrong value
54	test_postlint_wrong {
54	test_postlint_wrong {
55	   wrong := object.remove(base_input, ["scripts"]) | {"scripts": object.union(base_input.scripts, {"postlint": "bun run wrong"})}
55	   wrong := object.remove(base_input, ["scripts"]) | {"scripts": object.union(base_input.scripts, {"postlint": "bun run wrong"})}
56	   d := bun.deny with input as wrong
56	   d := bun.deny with input as wrong
57	   count(d) > 0
57	   count(d) > 0
58	   some msg in d; contains(msg, "postlint")
58	   some msg in d; contains(msg, "postlint")
59	}
59	}
60
60
61	# 6. top-level deny (поля у deny-list)
61	# 6. top-level deny (поля у deny-list)
62	test_deny_list {
62	test_deny_list {
63	   deny_input := {"description": "should not be here", "dependencies": {}}
63	   deny_input := {"description": "should not be here", "dependencies": {}}
64	   d := bun.deny with input as deny_input with data.template as {"deny": ["description"]}
64	   d := bun.deny with input as deny_input with data.template as {"deny": ["description"]}
65	   count(d) > 0
65	   count(d) > 0
66	   some msg in d; contains(msg, "description")
66	   some msg in d; contains(msg, "description")
67	}
67	}
68
[assistant]
[thinking]

[assistant]
The test file needs updates:
1. `base_input` → `"lint": "n-cursor lint --full"` (instead of old chain)
2. `test_lint_wrong` → test for wrong value (like `"bun run lint-js"` → expects `n-cursor lint --full`, no mention of oxfmt)

Let me rewrite the test file:
[assistant]
[tool: Write] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.test.rego","content":"package bun_test\n\nimport rego.v1\n\nimport data.bun\n\n# Утиліта: базова конфігурація (пасс без помилок)\nbase_input := {\n  \"scripts\": {\n    \"start\": \"bun ./npm/bin/n-cursor.js\",\n    \"test\": \"vitest run\",\n    \"lint-js\": \"bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints\",\n    \"lint-style\": \"npx stylelint ...\",\n    \"lint-ga\": \"bun run npm/rules/ga/lint/lint.mjs\",\n    \"lint-text\": \"bun run lint-text-core && bun run lint-text-extra\",\n    \"lint\": \"n-cursor lint --full\",\n    \"postlint\": \"n-cursor sync\"\n  },\n  \"name\": \"foo\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"workspaces\": [\"npm\", \"demo\"],\n  \"type\": \"module\"\n}\n\n# 1. Happy path — no deny\ntest_base_ok {\n  count(bun.deny) == 0 with input as base_input\n}\n\n# 2. Missing lint script\ntest_lint_missing {\n  d := bun.deny with input as object.remove(base_input, [\"scripts\"]) | {\"scripts\": object.remove(base_input.scripts, [\"lint\"])}\n  count(d) > 0\n  some msg in d; contains(msg, \"lint\")\n}\n\n# 3. Wrong lint script (не n-cursor lint --full)\ntest_lint_wrong {\n  wrong := object.remove(base_input, [\"scripts\"]) | {\"scripts\": object.union(base_input.scripts, {\"lint\": \"bun run lint-js\"})}\n  d := bun.deny with input as wrong\n  count(d) > 0\n  some msg in d; contains(msg, \"n-cursor lint --full\")\n}\n\n# 4. postlint missing\ntest_postlint_missing {\n  d := bun.deny with input as object.remove(base_input, [\"scripts\"]) | {\"scripts\": object.remove(base_input.scripts, [\"postlint\"])}\n  count(d) > 0\n  some msg in d; contains(msg, \"postlint\")\n}\n\n# 5. postlint wrong value\ntest_postlint_wrong {\n  wrong := object.remove(base_input, [\"scripts\"]) | {\"scripts\": object.union(base_input.scripts, {\"postlint\": \"bun run wrong\"})}\n  d := bun.deny with input as wrong\n  count(d) > 0\n  some msg in d; contains(msg, \"postlint\")\n}\n\n# 6. top-level deny (поля у deny-list)\ntest_deny_list {\n  deny_input := {\"description\": \"should not be here\", \"dependencies\": {}}\n  d := bun.deny with input as deny_input with data.template as {\"deny\": [\"description\"]}\n  count(d) > 0\n  some msg in d; contains(msg, \"description\")\n}\n"}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/npm/rules/bun/policy/package_json/package_json.test.rego has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[thinking]

[assistant]
Now let me run the Rego tests to make sure they pass, then check if there are doc-files that reference the old bun/rego lint logic that need CRC updates:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor && opa test npm/rules/bun/policy/package_json/ -v 2>&1 | head -30"}
[user]
[tool_result] /usr/local/bin/opa test npm/rules/bun/policy/package_json/ -v
data.bun_test.test_base_ok: PASS (476.583µs)
data.bun_test.test_lint_missing: PASS (144.375µs)
data.bun_test.test_lint_wrong: PASS (128.625µs)
data.bun_test.test_postlint_missing: PASS (87.208µs)
data.bun_test.test_postlint_wrong: PASS (87µs)
data.bun_test.test_deny_list: PASS (1.133917ms)
6/6 tests pass
[assistant]
[thinking]

[assistant]
Rego tests pass. Now let me update docs for the bun rego (docgen CRC) and update the `n-lint` skill:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== bun rego docs ===\"\nls -1 npm/rules/bun/policy/package_json/ 2>/dev/null\ncat npm/rules/bun/policy/package_json/docs/package_json.md 2>/dev/null | head -20\nls npm/rules/bun/policy/package_json/docs/ 2>/dev/null"}
[user]
[tool_result] === bun rego docs ===\"\nls -1 npm/rules/bun/policy/package_json/ 2>/dev/null\ncat npm/rules/bun/policy/package_json/docs/package_json.md 2>/dev/null | head -20\nls npm/rules/bun/policy/package_json/docs/ 2>/dev/null
=== bun rego docs ===
docs
package_json.rego
package_json.test.rego
ls: npm/rules/bun/policy/package_json/docs/: No such file or directory
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== n-lint skill ===\"\ncat .cursor/skills/n-lint/SKILL.md | head -40"}
[user]
[tool_result] === n-lint skill ===
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/.cursor/skills/n-lint/SKILL.md"}
[user]
[tool_result] 1	---
2	meta:
3	  name: n-lint
3	  name: n-lint
4	  description: Запустити кореневий bun run lint, виправити порушення й підтвердити чистий вихід
4	  description: Запустити кореневий bun run lint, виправити порушення й підтвердити чистий вихід
5	  skill_type: cursor_skill
5	  skill_type: cursor_skill
6	---
6	---
7
7
8	# Skill: n-lint
8	# Skill: n-lint
9
9
10	## Мета
10	## Мета
11
11
12	Запустити кореневий `bun run lint`, виправити всі порушення й переконатись у чистому виході.
12	Запустити кореневий `bun run lint`, виправити всі порушення й переконатись у чистому виході.
13
13
14	## Кроки
14	## Кроки
15
15
16	1. **Запустити лінт**:
16	1. **Запустити лінт**:
17	   ```bash
17	   ```bash
18	   bun run lint
18	   bun run lint
19	   ```
19	   ```
20
20
21	2. **Якщо є порушення** — виправити й перезапустити до exit 0.
21	2. **Якщо є порушення** — виправити й перезапустити до exit 0.
22
22
23	3. **Підтвердити чистий вихід**:
23	3. **Підтвердити чистий вихід**:
24	   ```bash
24	   ```bash
25	   bun run lint
25	   bun run lint
26	   ```
26	   ```
27
27
28	## Очікуваний результат
28	## Очікуваний результат
29
29
30	`bun run lint` повертає exit 0 без жодних помилок.
30	`bun run lint` повертає exit 0 без жодних помилок.
31
31
32	## Нотатки
32	## Нотатки
33
33
34	Паралельний лінт по **різних** файлах — **дозволено**: диз'юнктні набори (per-file `lint` на змінених vs origin) не конфліктують і не перевантажують диск/CPU. Серіалізувати треба лише **whole-tree** прогони того самого корпусу (`bun run lint`, `n-cursor lint --full` по всьому репо) — щоб не дублювати важкий full-scan.
34	Паралельний лінт по **різних** файлах — **дозволено**: диз'юнктні набори (per-file `lint` на змінених vs origin) не конфліктують і не перевантажують диск/CPU. Серіалізувати треба лише **whole-tree** прогони того самого корпусу (`bun run lint`, `n-cursor lint --full` по всьому репо) — щоб не дублювати важкий full-scan.
[assistant]
[thinking]

[assistant]
[tool: Edit] {"replace_all":false,"file_path":"/Users/vitalii/www/nitra/cursor/.cursor/skills/n-lint/SKILL.md","old_string":"  description: Запустити кореневий bun run lint, виправити порушення й підтвердити чистий вихід","new_string":"  description: Запустити n-cursor lint --full, виправити порушення й підтвердити чистий вихід"}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/.cursor/skills/n-lint/SKILL.md has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[tool: Edit] {"replace_all":false,"file_path":"/Users/vitalii/www/nitra/cursor/.cursor/skills/n-lint/SKILL.md","old_string":"## Мета\n\nЗапустити кореневий `bun run lint`, виправити всі порушення й переконатись у чистому виході.\n\n## Кроки\n\n1. **Запустити лінт**:\n   ```bash\n   bun run lint\n   ```\n\n2. **Якщо є порушення** — виправити й перезапустити до exit 0.\n\n3. **Підтвердити чистий вихід**:\n   ```bash\n   bun run lint\n   ```\n\n## Очікуваний результат\n\n`bun run lint` повертає exit 0 без жодних помилок.\n\n## Нотатки\n\nПаралельний лінт по **різних** файлах — **дозволено**: диз'юнктні набори (per-file `lint` на змінених vs origin) не конфліктують і не перевантажують диск/CPU. Серіалізувати треба лише **whole-tree** прогони того самого корпусу (`bun run lint`, `n-cursor lint --full` по всьому репо) — щоб не дублювати важкий full-scan.","new_string":"## Мета\n\nЗапустити `n-cursor lint --full`, виправити всі порушення й переконатись у чистому виході.\n\n## Кроки\n\n1. **Запустити лінт**:\n   ```bash\n   n-cursor lint --full\n   ```\n\n2. **Якщо є порушення** — виправити й перезапустити до exit 0.\n\n3. **Підтвердити чистий вихід**:\n   ```bash\n   n-cursor lint --full\n   ```\n\n## Очікуваний результат\n\n`n-cursor lint --full` повертає exit 0 без жодних помилок.\n\n## Нотатки\n\nПаралельний лінт по **різних** файлах — **дозволено**: диз'юнктні набори (per-file `lint` на змінених vs origin) не конфліктують і не перевантажують диск/CPU. Серіалізувати треба лише **whole-tree** прогони того самого корпусу (`n-cursor lint --full` по всьому репо) — щоб не дублювати важкий full-scan."}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/.cursor/skills/n-lint/SKILL.md has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[thinking]

[assistant]
Good. Now let me update the CRC for the doc files that have changed (orchestrate.mjs and python/js/lint.mjs, bun rego files). Let me check which doc files reference our modified files:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== docs for orchestrate.mjs ===\"\nfind npm/rules/lint -name '*.md' | xargs grep -l 'orchestrate.mjs' 2>/dev/null\necho\necho \"=== docs for python/js/lint.mjs ===\"\nfind npm/rules/python -name '*.md' | xargs grep -l 'lint.mjs' 2>/dev/null\necho\necho \"=== docs for bun rego ===\"\nfind npm/rules/bun -name '*.md' 2>/dev/null | head -10\necho\necho \"=== docs for python/lint/lint.mjs ===\"\nfind npm/rules/python -name '*.md' | xargs grep -l 'lint/lint.mjs' 2>/dev/null"}
[user]
[tool_result] === docs for orchestrate.mjs ===
npm/rules/lint/js/docs/orchestrate.md

=== docs for python/js/lint.mjs ===
npm/rules/python/js/docs/lint.md

=== docs for bun rego ===#
npm/rules/bun/docs/index.md
npm/rules/bun/docs/index.md
npm/rules/bun/policy/package_json/docs/...
npm/rules/bun/policy/package_json/docs/...

=== docs for python/lint/lint.mjs ===
npm/rules/python/lint/docs/lint.md
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\nnode -e \"\nconst {readFileSync} = require('node:fs');\nconst {crc32} = require('node:zlib');\nconst files = [\n  'npm/rules/lint/js/orchestrate.mjs',\n  'npm/rules/python/js/lint.mjs',\n  'npm/rules/python/lint/lint.mjs',\n];\nfor (const f of files) {\n  try {\n    const b = readFileSync(f);\n    console.log(f + ' -> ' + crc32(b).toString(16).padStart(8,'0'));\n  } catch(e) { console.log(f + ' -> ERROR: ' + e.message); }\n}\n\""}
[user]
[tool_result] npm/rules/lint/js/orchestrate.mjs -> 3c527082
npm/rules/python/js/lint.mjs -> a0d17a44
npm/rules/python/lint/lint.mjs -> f7aba4e6
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/lint/js/docs/orchestrate.md"}
[user]
[tool_result] 1	---
2	type: JS Module
3	title: orchestrate.mjs
4	resource: npm/rules/lint/js/orchestrate.mjs
5	docgen:
6	  crc: e51cba3a
7	  score: 100
8	---
9
10	Оркеструє lint-прогін усього монорепо. Завантажує …
[assistant]
[tool: Read] {"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/python/lint/docs/lint.md"}
[user]
[tool_result] 1	---
2	type: JS Module
3	title: lint.mjs
4	resource: npm/rules/python/lint/lint.mjs
5	docgen:
6	  crc: ce96b0d9
7	  score: 100
8	---
9
10	Запускає lint-python-ланцюг через `uv`. Якщо `pyproject.toml` відсутній у кор …
[assistant]
[tool: Edit] {"replace_all":false,"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/lint/js/docs/orchestrate.md","old_string":"  crc: e51cba3a","new_string":"  crc: 3c527082"}
[assistant]
[tool: Edit] {"replace_all":false,"file_path":"/Users/vitalii/www/nitra/cursor/npm/rules/python/lint/docs/lint.md","old_string":"  crc: ce96b0d9","new_string":"  crc: f7aba4e6"}
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/npm/rules/lint/js/docs/orchestrate.md has been updated successfully. (file state is current in your context — no need to Read it back)
[user]
[tool_result] The file /Users/vitalii/www/nitra/cursor/npm/rules/python/lint/docs/lint.md has been updated successfully. (file state is current in your context — no need to Read it back)
[assistant]
[thinking]

[assistant]
Good. Now let me update the python/js/docs/lint.md CRC (we already wrote it, but let me verify it matches the actual file CRC):
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== verify python/js/lint.mjs CRC matches doc ===\"\nnode -e \"const{readFileSync}=require('node:fs');const{crc32}=require('node:zlib');const b=readFileSync('npm/rules/python/js/lint.mjs');console.log('actual: ' + crc32(b).toString(16).padStart(8,'0'));\"\nhead -8 npm/rules/python/js/docs/lint.md"}
[user]
[tool_result] === verify python/js/lint.mjs CRC matches doc ===
actual: a0d17a44
---
type: JS Module
title: lint.mjs
resource: npm/rules/python/js/lint.mjs
docgen:
crc: a0d17a44
score: 100
[assistant]
[thinking]

[assistant]
CRC matches. Now let me check if there are any docs that reference the `n-lint` SKILL.md (e.g., in docs/) that might have CRC:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\ngrep -rn 'crc\\|docgen' .cursor/skills/n-lint/ 2>/dev/null\necho; echo \"=== git status summary ===\"\ngit status --short | grep -v node_modules | head -30"}
[user]
[tool_result] === git status summary ===
M .claude/hooks/capture-decisions.sh
M .claude/hooks/lib/tooling-only.sh
M .claude/hooks/normalize-decisions.sh
M .cursor/skills/n-lint/SKILL.md
M .github/workflows/lint-text.yml
D  \"docs/adr/20260531-133047...\"  (truncated)
M npm/rules/bun/policy/package_json/package_json.rego
M npm/rules/bun/policy/package_json/package_json.test.rego
M npm/rules/bun/template/package.json.snippet.json
M npm/rules/lint/js/docs/orchestrate.md
M npm/rules/lint/js/orchestrate.mjs
M npm/rules/python/js/docs/index.md
?? npm/rules/python/js/docs/lint.md
?? npm/rules/python/js/lint.mjs
M npm/rules/python/lint/docs/lint.md
M npm/rules/python/lint/lint.mjs
M npm/rules/python/meta.json
M package.json
?? docs/adr/відокремлення-oxfmt-від-lint-ланцюжка.md (after delete... wait this shows as deleted or?
```
[assistant]
[thinking]

[assistant]
Almost done! Let me now run a quick sanity check on the lint orchestration to make sure the python rule is found by the orchestrator with the new meta.json settings:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== Verify orchestrator would pick up python (meta has lint:full) ===\"\nnode --input-type=module << 'EOF'\nimport { readFileSync } from 'node:fs'\nimport { join } from 'node:path'\nconst rulesDir = 'npm/rules'\nconst rules = []\nfor (const id of ['doc-files','ga','js-lint','js-lint-ci','rego','security','style-lint','text','python']) {\n  const metaPath = join(rulesDir, id, 'meta.json')\n  try {\n    const meta = JSON.parse(readFileSync(metaPath, 'utf8'))\n    const lintScope = meta.lint\n    const hasAdapter = (() => { try { readFileSync(join(rulesDir, id, 'js/lint.mjs')); return true } catch { return false } })()\n    rules.push({ id, lintScope, hasAdapter })\n  } catch { rules.push({ id, error: 'no meta.json' }) }\n}\nrules.forEach(r => console.log(JSON.stringify(r)))\nEOF"}
[user]
[tool_result] === Verify orchestrator would pick up python (meta has lint:full) ===
{\"id\":\"doc-files\",\"lintScope\":\"per-file\",\"hasAdapter\":true}
{\"id\":\"ga\",\"lintScope\":\"full\",\"hasAdapter\":true}
{\"id\":\"js-lint\",\"lintScope\":\"per-file\",\"hasAdapter\":true}
{\"id\":\"js-lint-ci\",\"lintScope\":\"full\",\"hasAdapter\":true}
{\"id\":\"rego\",\"lintScope\":\"full\",\"hasAdapter\":true}
{\"id\":\"security\",\"lintScope\":\"per-file\",\"hasAdapter\":true}
{\"id\":\"style-lint\",\"lintScope\":\"per-file\",\"hasAdapter\":true}
{\"id\":\"text\",\"lintScope\":\"per-file\",\"hasAdapter\":true}
{\"id\":\"python\",\"lintScope\":\"full\",\"hasAdapter\":true}
[assistant]
[thinking]

[assistant]
All 9 rules including python now have both `lintScope` and `hasAdapter`. Let me now do a final comprehensive check:
[assistant]
[tool: Bash] {"command":"cd /Users/vitalii/www/nitra/cursor\necho \"=== Final state: package.json scripts ===\"\nnode -e \"const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); Object.entries(p.scripts).forEach(([k,v]) => console.log(k + ': ' + v))\"\necho\necho \"=== orchestrate.mjs: oxfmt block present ===\"\ngrep -n \"oxfmt\\|runOxfmt\\|readOnly\" npm/rules/lint/js/orchestrate.mjs | head -10"}
[user]
[tool_result] === Final state: package.json scripts ===
start: bun ./npm/bin/n-cursor.js
test: vitest run
lint-js: bunx oxlint --fix && bunx eslint --fix . && bunx jscpd . && bunx knip --no-config-hints
lint-style: npx stylelint **/*.{css,scss,vue} --fix && npx stylelint \"**/*.{css,scss,vue}\"
lint-ga: bun run npm/rules/ga/lint/lint.mjs
lint-text: bun run lint-text-core && bun run lint-text-extra
lint-text-core: cspell **/* --no-progress --gitignore && shellcheck --external-sources $(git ls-files '*.sh') && markdownlint **/*.md --ignore node_modules
lint-text-extra: v8r --cache && dotenv-linter .env .env.*
lint: n-cursor lint --full
postlint: n-cursor sync
[assistant]
Усе зроблено. Підсумую фінальний стан:

## Що виконано

**1. Python інтегровано в `n-cursor lint --full`:**
- `npm/rules/python/meta.json` → додано `"lint": "full"`
- `npm/rules/python/js/lint.mjs` → адаптер, що делегує в `lint/lint.mjs`
- `npm/rules/python/lint/lint.mjs` → доданий параметр `readOnly` (у read-only не запускає `--fix`)
- `npm/rules/python/js/docs/lint.md` → новий doc з CRC
- `npm/rules/python/js/docs/index.md` → оновлено таблицю

**2. oxfmt інтегровано в оркестратор:**
- `npm/rules/lint/js/orchestrate.mjs` → крок `runOxfmt()` після всіх правил, тільки якщо `!readOnly`

**3. `package.json` — cleaned up:**
- `"lint": "n-cursor lint --full"` (аліас-делегат, варіант B)
- Видалено: `lint-doc-files`, `lint-python`, `lint-rego`, `lint-security`, `oxfmt`
- Лишились: `lint-js`, `lint-style`, `lint-ga`, `lint-text` (вимагаються CI/Rego)

**4. Rego/template/tests оновлено:**
- `bun/policy/package_json/package_json.rego` → нова проста вимога `lint == "n-cursor lint --full"`
- `bun/template/package.json.snippet.json` → видалено obsolete скрипти, `"lint": "n-cursor lint --full"`
- `bun/policy/package_json/package_json.test.rego` → тести оновлено (6/6 pass)

**5. `/n-lint` skill** → оновлено на `n-cursor lint --full`

**6. ADR** → видалено «Відокремлення oxfmt» (суперечило рішенню); «Уніфікація lint» вже точна.

**CRC оновлено:** `orchestrate.md`, `python/lint/docs/lint.md`, `python/js/docs/lint.md`.
