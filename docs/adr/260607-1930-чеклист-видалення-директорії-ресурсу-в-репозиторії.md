---
session: 8cd8d81f-2872-41c6-a29c-5e1ea6518ad1
captured: 2026-06-07T19:30:02+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/8cd8d81f-2872-41c6-a29c-5e1ea6518ad1.jsonl
---

Директорія `benchmarks/` **поки не готова до видалення** — є два причини:

1. **`package.json` скрипт**: `"benchmark": "bun benchmarks/runner-comparison/run.mjs"` — видалення зламає цей скрипт.
2. **Uncommitted changes**: є незакомічені зміни у `benchmarks/runner-comparison/demo/src/docs/promise-pool.md` та `benchmarks/tool-free/run.mjs`.

Якщо спайки більше не потрібні, план такий:
- прибрати `"benchmark"` зі скриптів у `package.json`
- видалити директорію
- скомітити

Хочеш це зробити?
[user]
так, можемо видалити. для цього скрипту я зроблю change file потім сам
[assistant]
[tool: Bash] {"command":"cd /Users/vitaliytv/www/nitra/cursor && rm -rf benchmarks/","description":"Delete benchmarks directory"}
[user]
[tool_result]
[assistant]
[tool: Edit] {"file_path":"/Users/vitaliytv/www/nitra/cursor/package.json","old_string":"\"benchmark\": \"bun benchmarks/runner-comparison/run.mjs\"","new_string":""}
[user]
[tool_result] The file was edited successfully.
[assistant]
[tool: Read] {"file_path":"/Users/vitaliytv/www/nitra/cursor/package.json","limit":30}
[user]
[tool_result]      1	{
2	  "name": "@nitra/cursor",
3	  "version": "4.1.0",
4	  "description": "Nitra Cursor — AI-assisted development toolkit",
5	  "scripts": {

6	    "test": "bun run --cwd npm test",
7	    "lint": "bun run --cwd npm lint",

8	    "postinstall": "bun run --cwd npm postinstall",
9	    "prepare": "cd npm && bun run prepare"
10	  },
11	  "workspaces": [
12	    "npm",
12	    "demo"
13	  ],
14	  "devDependencies": {
15	     ...
16	  }
17	}

[assistant]
Директорію `benchmarks/` видалено, скрипт `"benchmark"` прибрано з `package.json`.
[user]
хочу цей процес (визначення чи ресурс/директорія можна видалити) задокументувати як окремий adr. Як він у нас зараз виглядає?
[assistant]

---

## ADR Чеклист видалення директорії / ресурсу в репозиторії

## Context and Problem Statement
Під час сесії виникло питання: чи безпечно видалити директорію `benchmarks/`, яка містила спайки (`runner-comparison`, `tool-free`). Потрібно було перевірити залежності перед видаленням, щоб не зламати конфіг та не втратити незафіксовані зміни.

## Considered Options
* Перевірити залежності (скрипти, конфіги, CI) і незафіксовані зміни перед видаленням
* Видалити без перевірки
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Перевірити залежності та uncommitted changes перед видаленням", because видалення без перевірки зламало б скрипт `"benchmark"` у `package.json` і потенційно втратило б незафіксовані зміни у `benchmarks/runner-comparison/demo/src/docs/promise-pool.md` та `benchmarks/tool-free/run.mjs`.

**Процес чеклисту:**
1. `ls <dir>` — перевірити структуру
2. `git log --all -- <dir>` — переконатись у git-історії
3. `git diff HEAD -- <dir>` — знайти uncommitted changes
4. `grep -r "<dir>" ... -l | grep -v <dir>` — знайти зовнішні посилання (скрипти, конфіги, CI)
5. Усунути посилання (`package.json`, CI) → видалити директорію → скомітити

### Consequences
* Good, because перевірка виявила активний скрипт `"benchmark": "bun benchmarks/runner-comparison/run.mjs"` у `package.json`, який прибрали до видалення.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Видалені файли: `benchmarks/runner-comparison/`, `benchmarks/tool-free/`
- Змінений файл: `package.json` — прибрано рядок `"benchmark": "bun benchmarks/runner-comparison/run.mjs"`
- Команди чеклисту: `ls`, `git log --oneline --all -- <dir>`, `git diff HEAD -- <dir>`, `grep -r "<dir>" --include="*.json" --include="*.yml" -l`
- Change-файл буде додано окремо користувачем (`n-cursor change`)
