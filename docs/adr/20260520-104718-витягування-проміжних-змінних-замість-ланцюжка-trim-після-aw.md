---
session: 9fa638b6-798e-444b-ba69-148c3e1518fa
captured: 2026-05-20T10:47:18+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/9fa638b6-798e-444b-ba69-148c3e1518fa.jsonl
---

## ADR Витягування проміжних змінних замість ланцюжка `.trim()` після `await`

## Context and Problem Statement
ESLint-правило `unicorn/no-await-expression-member` забороняє звертатися до властивостей/методів напряму з `await`-виразу. У `npm/rules/changelog/fix/consistency/check.mjs` (рядки ~161–162) та `npm/scripts/sync-claude-config.test.mjs` (рядок ~289) такий доступ порушував правило і блокував `bun run lint`.

## Considered Options
* Витягти результат `await`-виразу в проміжну змінну, а потім викликати метод на ній
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Витягти результат у проміжну змінну", because це мінімальна зміна, яка задовольняє `unicorn/no-await-expression-member` без зміни логіки.

### Consequences
* Good, because `bun run lint` виходить без порушень по цьому правилу.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Файли: `npm/rules/changelog/fix/consistency/check.mjs` (змінні `originMainRaw` / `headRaw`), `npm/scripts/sync-claude-config.test.mjs` (змінна `gitignoreContent`). Правило: `unicorn/no-await-expression-member`.

---

## ADR Заміна вкладеного тернарного оператора на `let` + `if`

## Context and Problem Statement
ESLint-правило `sonarjs/no-nested-conditional` забороняє вкладені тернарні оператори. У `npm/scripts/sync-claude-config.mjs` (~рядок 442) вираз для `prefix` використовував вкладений тернарний оператор, що спричиняло порушення при `bun run lint`.

## Considered Options
* Переписати на `let prefix = ''` із наступним `if`-блоком
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "`let` + `if`", because це усуває вкладеність умов і задовольняє `sonarjs/no-nested-conditional` без зміни поведінки.

### Consequences
* Good, because код стає лінійно читабельним, `bun run lint` виходить з кодом 0.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Файл: `npm/scripts/sync-claude-config.mjs` (~рядок 442). Правило: `sonarjs/no-nested-conditional`.

---

## ADR Заміна неологізму `автодопис` на `автоматичне дописування`

## Context and Problem Statement
Слово `автодопис` (у формі `автодопису`) з'явилося в `npm/CHANGELOG.md` (рядок 15) і не було розпізнане `cspell` як словникове слово, що давало помилку `Unknown word (автодопису)` і блокувало `bun run lint`. Слово було вжито лише в одному місці репозиторію.

## Considered Options
* Замінити на описовий відповідник `автоматичного дописування`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Замінити на `автоматичного дописування`", because слово є одноразовим неологізмом, не вживається більше ніде в репо, і простіше переформулювати, ніж додавати виключення в словник `cspell`.

### Consequences
* Good, because `cspell` більше не фіксує `Unknown word`, `bun run lint` виходить з кодом 0.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Файл: `npm/CHANGELOG.md`, рядок 15. Команда перевірки: `bun run lint-text`. Інструмент: `cspell`.
