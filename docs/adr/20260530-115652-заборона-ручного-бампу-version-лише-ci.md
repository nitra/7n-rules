---
type: ADR
title: "Заборона ручного бампу `version` — лише CI"
---

# Заборона ручного бампу `version` — лише CI

**Status:** Accepted
**Date:** 2026-05-30

## Context and Problem Statement

У монорепо `@nitra/cursor` паралельні feature-гілки конфліктували на полі `version` у `package.json`. Субагенти (під час виконання `n-fix`, `n-coverage-fix` тощо), читаючи правило `n-changelog.mdc` v3.0, самостійно бампали `version`. Перевірка `consistency.mjs` давала pass при ручному bump, тому коміт проходив і паралельні гілки конфліктували. Перевірка `checkDirtyNpmRequiresVersionBump` у `npm-module` додатково форсувала ручний bump — падала за його відсутності. Потрібно гарантувати, що `version` змінює лише CI-крок `release.mjs` через агрегацію `.changes/*.md`.

## Considered Options

- Лише документація: прибрати legacy-рядок з правила, нічого не змінювати у `consistency.mjs`. Ручний bump лишається тихо прохідним.
- Документація + check активно відхиляє будь-який дрейф `version` поза CI: зміна `consistency.mjs` так, що `Vbase ≠ Vcurrent` → `fail` на будь-якій гілці, навіть із change-файлом.
- Окремий write-time guard / хук як незалежний механізм блокування.

## Decision Outcome

Chosen option: "Документація + check активно відхиляє будь-який дрейф `version`", because наявний pre-commit hook `npm-changelog` вже запускає `check changelog`, тому новий `fail` у `consistency.mjs` автоматично блокує коміт без окремого механізму. Legacy-лазівка у правилі видаляється одночасно. Заборона однорідна для всіх гілок, включно з `main`. CI-реліз-коміт не гейтиться changelog-workflow, тому release-коміт не блокується. Підхід 3 (окремий guard) відхилено як надлишковий.

### Consequences

- Good, because `consistency.mjs` перевіряє drift до change-file check — ручний bump не проходить навіть за наявності change-файлу.
- Good, because правило `n-changelog.mdc` оновлено до v3.1 — прибрано legacy-рядок, STOP-блок став категоричним; синхронізовано `.cursor/rules/n-changelog.mdc`.
- Bad, because у `npm-module` є перевірка `checkDirtyNpmRequiresVersionBump` (в `package_structure.mjs`), яка активно форсує ручний bump — залишилась незавершеною (Task 6) через конкурентну активність у тому ж робочому дереві.

## More Information

Змінені файли:
- `npm/rules/changelog/js/consistency.mjs`
- `npm/rules/changelog/js/tests/consistency/tests/check.test.mjs`
- `npm/rules/changelog/changelog.mdc`
- `.cursor/rules/n-changelog.mdc`

Commits на гілці `feat/ci-only-version-bump`: `c3fc7f0`, `fb849cf`, `ee0cf8a` (логіка + тести), `11ff6f2` (правило changelog v3.1).

CI-bump: `npm/rules/release/release.mjs`; workflow `.github/workflows/npm-publish.yml` не запускає `check changelog`, тому release-коміт не блокується.

Незавершена задача Task 6: прибрати `checkDirtyNpmRequiresVersionBump` з `npm/rules/npm-module/js/package_structure.mjs` і секцію "Build версія" з `npm/rules/npm-module/npm-module.mdc`.

Специфікація: `docs/superpowers/specs/2026-05-30-ci-only-version-bump-design.md`.
План: `docs/superpowers/plans/2026-05-30-ci-only-version-bump.md`.

## Update 2026-05-30

Префлайт-контекст: hook-файл `hk.pkl` — локальний pre-commit hook, що запускає лише `check changelog`, без жодного бампу версії. Саме він автоматично блокує коміт із незаконним bump через новий `fail` у `consistency.mjs`, без окремого механізму (Підхід 3 відхилено: pre-commit вже дає той самий ефект).

## Update 2026-05-30

Деталі реалізації: у `consistency.mjs` видалено функцію `verifyChangelogEntry` та її імпорт; функція `checkPublishedWorkspacePendingGitChanges` змінена так, що `Vbase ≠ Vcurrent` → `fail` незалежно від наявності change-файлу. Команда для розробника в повідомленні про помилку: `n-cursor change …` (поклади change-файл і відкоти version до базового).

## Update 2026-05-30

Деталі реалізації рішення з transcript:

- `npm/rules/changelog/js/consistency.mjs` — переписані `checkLocalOnlyChangedWorkspace`, `checkPublishedWorkspace`, `checkPublishedWorkspacePendingGitChanges`; version-drift перевіряється до check-файлу; прибрано `verifyChangelogEntry`.
- `npm/rules/changelog/js/tests/consistency/tests/check.test.mjs` — bump→pass кейси переведені на fail; додані явні «ручний bump → fail» сценарії; 110 unit-тестів green.
- `npm/rules/changelog/changelog.mdc` + `.cursor/rules/n-changelog.mdc` — прибрано legacy-лазівку, STOP категоричний, `version: '3.0' → '3.1'`.
- `npm/rules/npm-module/js/package_structure.mjs` — видалено `checkDirtyNpmRequiresVersionBump` і всі допоміжні git-функції.
- `npm/rules/npm-module/npm-module.mdc` + дзеркало — секція «Build версія» переписана на change-файл-флоу, `version: '1.13' → '1.14'`.
- `.cursor/rules/scripts.mdc` — STOP-блок секції «Завершення задачі» переписано, `version: '1.11' → '1.12'`.
- `npm/skills/llm-patch/SKILL.md` + `.cursor/skills/n-llm-patch/SKILL.md` — зразковий output «bump version (minor)» замінено на `npx @nitra/cursor change …`.
- Change-файл: `npm/.changes/1780200000000-cibump.md`.
- Специфікація: `docs/superpowers/specs/2026-05-30-ci-only-version-bump-design.md`.
- Гілка реалізації: `feat/ci-only-version-bump-2` (worktree `/Users/vitaliytv/www/nitra/cursor-ci-bump`).

Побічний ефект: у worktree з «брудною» базою (concurrent release підняв локальну версію до 1.36.0 при опублікованій 1.34.1) `fix changelog` виходить з помилкою — це очікувана поведінка нової перевірки.

## Update 2026-06-01

**Конкретний інцидент у worktree `main-fix` (сесія /n-fix):** `npx @nitra/cursor fix changelog` виявив розходження: `npm/package.json` має версію `3.2.0`, npm-реєстр — `3.2.3`. Агент спробував ручний bump до `3.2.3`, а потім `git merge main --no-edit` для усунення drift — отримав конфлікти у `npm/scripts/dispatcher/lib/commands.mjs`. Обидва кроки скасовано: `git merge --abort` + `git checkout -- npm/package.json`. Репо лишилось у чистому стані; `fix changelog` продовжує повертати `❌` до повноцінного release-flow через CI.

Change-файл поточної сесії: `npm/.changes/1780300299314-c5d303.md` (bump: patch, js-lint e18e правила).

**Preflight worktree перед інцидентом:** скіл `/n-fix` виявив `git rev-parse --show-toplevel = /Users/vitalii/www/nitra/cursor` (основне дерево) → перейшов у `.worktrees/main-fix/` (worktree вже існував за конвенцією `<current-branch>-fix`).

## Update 2026-06-07

Під час підготовки breaking change для `@nitra/cursor` підтверджено той самий інваріант: версію `4.0.0` не редагують напряму в `npm/package.json`, а фіксують через changeset.

Transcript facts:
- команда: `n-cursor change -t major -w npm "bump to v4: breaking changes in model classification API"`;
- створений файл: `.changesets/1749296099946-npm.md`;
- frontmatter changeset: `bump: major`, `workspace: npm`;
- фактичний bump версії має виконати CI-flow.
