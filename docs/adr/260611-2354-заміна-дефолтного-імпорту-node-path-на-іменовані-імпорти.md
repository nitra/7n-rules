---
session: dcbd14cd-85ea-4d0d-9118-6a0f6fc7c58a
captured: 2026-06-11T23:54:44+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/dcbd14cd-85ea-4d0d-9118-6a0f6fc7c58a.jsonl
---

## ADR Заміна дефолтного імпорту `node:path` на іменовані імпорти

## Context and Problem Statement
У `docgen-scan.mjs` (в обох модулях `doc-files` та `doc-aggregate`) використовувався `import path from 'node:path'` з inline-коментарем `// eslint-disable-next-line unicorn/import-style`. Правило `unicorn/import-style` в `@nitra/eslint-config` вимагає `named: true` для `node:path`, забороняючи дефолтний імпорт.

## Considered Options
* Рефакторинг на іменовані імпорти (`join`, `dirname`, `basename`, `extname`, `relative`, `resolve`, `sep`, `isAbsolute`, `posix`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Рефакторинг на іменовані імпорти", because це єдиний спосіб задовольнити `unicorn/import-style: named:true` без залишення inline-suppress коментаря. Обидва файли (`npm/skills/doc-files/js/docgen-scan.mjs` і `npm/skills/doc-aggregate/js/docgen-scan.mjs`) були повністю переписані з усіма `path.xxx` викликами замінені відповідними іменованими функціями.

### Consequences
* Good, because transcript фіксує очікувану користь: `bunx eslint npm/skills/doc-aggregate/js/docgen-scan.mjs npm/skills/doc-files/js/docgen-scan.mjs` вийшов без помилок після рефакторингу.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Змінені файли: `npm/skills/doc-files/js/docgen-scan.mjs`, `npm/skills/doc-aggregate/js/docgen-scan.mjs`. Правило: `unicorn/import-style` з `@nitra/eslint-config/index.js:306`.

---

## ADR Перенос inline `eslint-disable` в file-level overrides `eslint.config.js`

## Context and Problem Statement
По всьому проєкту було ~50 розкиданих inline `// eslint-disable-next-line` коментарів для кількох повторюваних ситуацій: `n/no-process-exit` у CLI entry-points (`fix.mjs`), `no-unsanitized/method` у динамічних `import()` з filesystem-whitelist'у, `sonarjs/no-hardcoded-ip` у k8s manifest/тест файлах, `@microsoft/sdl/no-insecure-url` + `sonarjs/no-clear-text-protocols` у hasura-тестах, `sonarjs/slow-regex` у capacitor, `security/detect-non-literal-regexp` у `npm-module`. Inline suppress-коментарі дублюються й не дають глобального picture навіщо виняток.

## Considered Options
* Перенести в файлові overrides в `eslint.config.js` (glob-патерни + вимкнені правила)
* Перенести в upstream `@nitra/eslint-config` (глобальний патч для всіх проєктів)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Перенести в файлові overrides в `eslint.config.js`", because на момент реалізації inline-видалення репо `@nitra/eslint-config` ще не аналізувалося; user запропонував глобальний підхід лише після того, як overrides вже були додані до `eslint.config.js`. Фінального рішення щодо upstream-патчу в transcript не прийнято.

### Consequences
* Good, because transcript фіксує очікувану користь: `grep -rn "eslint-disable" npm/**/*.mjs` після видалення повертає порожній вивід — всі inline suppress-коментарі прибрані.
* Bad, because якщо `@nitra/eslint-config` буде патчено глобально — дублюючі overrides у `eslint.config.js` треба буде прибирати окремим кроком; transcript не містить підтвердженого плану цього кроку.

## More Information
Файл: `eslint.config.js`. Glob-патерни в overrides: `npm/rules/*/fix.mjs`, `npm/scripts/utils/with-lock.mjs`, `npm/scripts/lint-cli.mjs`, `npm/scripts/lib/run-rule.mjs`, `npm/tests/fix-mjs-contract.test.mjs`, `npm/rules/test/coverage/coverage.mjs`, `npm/rules/k8s/js/**`, `npm/rules/hasura/js/tests/**`, `npm/rules/capacitor/js/platforms.mjs`, `npm/rules/npm-module/js/package_structure.mjs`. Масове видалення через `sed -i '' '/eslint-disable-next-line …/d'`. Виняток: `demo/src/auto-imports.d.ts` залишений із `/* eslint-disable */` — auto-generated файл.

---

## ADR Питання про upstream-патч `@nitra/eslint-config` (відкрите)

## Context and Problem Statement
Після того як inline `eslint-disable` коментарі були замінені file-level overrides у `eslint.config.js`, user запропонував розглянути внесення патчу безпосередньо до `@nitra/eslint-config`, щоб ті ж самі glob-виключення діяли глобально для всіх проєктів на базі цього конфігу.

## Considered Options
* Додати glob-overrides до `@nitra/eslint-config/npm/index.js` (один патч — всі проєкти)
* Залишити overrides у кожному `eslint.config.js` проєкту окремо
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: рішення в transcript не прийнято — сесія перервана на аналізі `@nitra/eslint-config/npm/index.js` (рядки unicorn-правил), до жодного запису/коміту в `eslint-config`-репо не дійшло.

### Consequences
* Good, because Neutral, because transcript не містить підтвердження наслідку.
* Bad, because Neutral, because transcript не містить підтвердження наслідку.

## More Information
Репо `@nitra/eslint-config` знаходиться в `/Users/vitaliytv/www/nitra/eslint-config/`. Аналізувався файл `npm/index.js`. На момент перериву сесії зміни до нього не вносились.
