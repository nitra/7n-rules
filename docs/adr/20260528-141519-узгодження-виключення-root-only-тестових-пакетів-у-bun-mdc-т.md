---
session: 79c7096f-4041-4649-b052-230c590e9d52
captured: 2026-05-28T14:15:19+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/79c7096f-4041-4649-b052-230c590e9d52.jsonl
---

## ADR Узгодження виключення root-only тестових пакетів у `bun.mdc` та `package_json.rego`

## Context and Problem Statement
У правилі `bun` (`@nitra/cursor`) існував розсинхрон між текстовим описом у `bun.mdc` і rego-полісі `package_json.rego`: `bun.mdc` описував виняток для `vitest`, `@vitest/coverage-v8`, `@stryker-mutator/vitest-runner` у root `devDependencies` лише як dog food-виключення для самого `@nitra/cursor`, тоді як `package_json.rego` фактично дозволяв ці пакети у root `devDependencies` будь-якого проєкту-споживача. Це призводило до хибних помилок: запуск `bun run coverage` у зовнішньому репо `ai` падав із підказкою додати ці пакети, попри те що `bun.mdc` забороняв їх у root.

## Considered Options
* Узгодити `bun.mdc` і `package_json.rego` так, щоб виняток стосувався будь-якого кореневого `package.json` (не лише `@nitra/cursor`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Узгодити `bun.mdc` і `package_json.rego` — дозволити `vitest`, `@vitest/coverage-v8`, `@stryker-mutator/vitest-runner` у root `devDependencies` як загальний виняток", because `package_json.rego` вже фактично реалізував цей дозвіл, а `bun.mdc` необхідно привести у відповідність; виняток обґрунтований тим, що опублікований workspace `npm/` не має `devDependencies` згідно з `npm-module.mdc`, тому ці пакети можуть жити лише у кореневому `package.json`.

### Consequences
* Good, because проєкти-споживачі (як репо `ai`) отримують узгоджені правила й `bun run coverage` більше не генерує хибних помилок про відсутній `vitest`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файли: `npm/rules/bun/bun.mdc`, `npm/rules/bun/policy/package_json/package_json.rego`, `npm/rules/bun/policy/package_json/package_json_test.rego`
- Пакети-винятки: `vitest`, `@vitest/coverage-v8`, `@stryker-mutator/vitest-runner`
- Правило `npm-module.mdc` забороняє `devDependencies` у published workspace `npm/` — це первинна причина, чому тестові peer-залежності мають бути у root
- Контекст з `test.mdc` (v2.4): правило `test` керує `stryker.config.mjs` і `vitest.config.js`
