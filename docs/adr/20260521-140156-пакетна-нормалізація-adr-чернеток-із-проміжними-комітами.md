---
session: ea881c04-dcf0-4753-a405-2366550a0911
captured: 2026-05-21T14:01:56+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/ea881c04-dcf0-4753-a405-2366550a0911.jsonl
---

## ADR Пакетна нормалізація ADR-чернеток із проміжними комітами

## Context and Problem Statement
У репозиторії накопичилось 66 ADR-чернеток (файли з `session:` у frontmatter). Скрипт `.claude/hooks/normalize-decisions.sh` має вбудований throttle (`ADR_NORMALIZE_MIN_INTERVAL_HOURS=21600`), що унеможливлює послідовний прогон кількох батчів без ручного обходу.

## Considered Options
* Обробити всі чернетки за один великий прогін
* Обробляти батчами по 10 із проміжним `git commit` після кожного батчу та перевіркою через dry-run перед першим реальним прогоном
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Обробляти батчами по 10 із проміжним `git commit` після кожного батчу", because скрипт сам визначає batch size=10 (найстаріші чернетки), а користувач на питання "що робити далі?" вибрав "Закомітити й продовжити" — кожен результат одразу зберігається в git, не чекаючи завершення всіх батчів.

### Consequences
* Good, because transcript фіксує очікувану користь: кожен батч атомарно закомічується (`7c62d53`, `c45b8c9`), тому часткові результати не втрачаються при збої LLM (батч 3 повернув хибний JSON — retry не загубив попередні commit).
* Bad, because throttle обходиться через `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0`, тобто захист від надто частих LLM-викликів вимкнено на весь час сесії; transcript не містить підтвердження наслідку щодо вартості/ліміту API.

## More Information
- Скрипт: `.claude/hooks/normalize-decisions.sh`
- Лог: `.claude/hooks/normalize-decisions.log`
- Dry-run прапор: `ADR_NORMALIZE_DRY=1`
- Обхід throttle: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0`
- Після 2 успішних батчів лишилося 58 чернеток; батч 3 завершився з помилкою JSON і запущено retry
- Commit-повідомлення: `adr: normalize batch` з `Co-Authored-By: Claude Opus 4.7 (1M context)`
