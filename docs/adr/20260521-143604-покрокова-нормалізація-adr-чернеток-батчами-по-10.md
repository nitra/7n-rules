---
session: ea881c04-dcf0-4753-a405-2366550a0911
captured: 2026-05-21T14:36:04+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-cursor/ea881c04-dcf0-4753-a405-2366550a0911.jsonl
---

## ADR Покрокова нормалізація ADR-чернеток батчами по 10

## Context and Problem Statement
У репозиторії накопичилося 66+ ADR-чернеток (файли з `session:` у frontmatter), які потребували перетворення на чисті MADR-документи. Пакетний запуск через `normalize-decisions.sh` має вбудовані захисти від частого використання (поріг і мінімальний інтервал між прогонами), що блокують ручний масовий запуск.

## Considered Options
* Ручний запуск із обходом захистів через env-змінні (`ADR_NORMALIZE_THRESHOLD=0`, `ADR_NORMALIZE_MIN_INTERVAL_HOURS=0`), батчами по 10, з окремим комітом після кожного батчу
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Ручний запуск із обходом захистів, батчами по 10, окремий коміт після кожного батчу", because скіл `.cursor/skills/n-adr-normalize/SKILL.md` передбачає саме такий сценарій; окремі коміти зберігають можливість відкату кожного батчу незалежно.

### Consequences
* Good, because transcript фіксує очікувану користь: за 6 батчів кількість чернеток скоротилась із 66 до 26; кожен батч ізольовано закомічено (`7c62d53`, `c45b8c9`, `f3ddb9e`, `bb28aaf`, `d03d00d`, `0ddee21`).
* Bad, because у батчі 3 LLM повернув некоректний JSON — знадобився повторний прогон; у батчі 6 одну операцію пропущено через посилання на неіснуючий source-файл.

## More Information
- Скрипт: `.claude/hooks/normalize-decisions.sh`
- Лог: `.claude/hooks/normalize-decisions.log`
- Запуск: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0 bash .claude/hooks/normalize-decisions.sh`
- Dry-run: додати `ADR_NORMALIZE_DRY=1`
- Операції LLM: `rewrite` (нова чиста ADR), `merge-into` (злиття чернетки в наявний файл), `delete` (видалення дублікату)
- Batch size за логом: `batch size: 10`

---

## ADR Заборона Co-Authored-By трейлера Claude у комітах нормалізації ADR

## Context and Problem Statement
Перші батчи нормалізації фіксувалися з трейлером `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`. Система заблокувала такий коміт із поясненням: "misrepresenting the agent's identity (Content Integrity / Impersonation)".

## Considered Options
* Використовувати `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` у commit message
* Комітити без Co-Authored-By трейлера

## Decision Outcome
Chosen option: "Комітити без Co-Authored-By трейлера", because класифікатор Claude Code auto-mode заблокував коміт із невідповідним трейлером; подальші батчи зафіксовано з повідомленням `adr: normalize batch` без трейлера.

### Consequences
* Good, because transcript фіксує очікувану користь: коміти `bb28aaf`, `d03d00d`, `0ddee21` пройшли без блокування.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Повідомлення про блокування: "Commit message attributes the work to 'Claude Opus 4.7' as a Co-Author, misrepresenting the agent's identity (Content Integrity / Impersonation)"
- Фінальний формат commit message: `adr: normalize batch` (без трейлерів)
