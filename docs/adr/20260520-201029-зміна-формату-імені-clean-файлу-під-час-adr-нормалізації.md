---
session: 38b10f20-1ef3-4398-8bd4-bc25bbe3f37f
captured: 2026-05-20T20:10:29+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/38b10f20-1ef3-4398-8bd4-bc25bbe3f37f.jsonl
---

Generating ADR content from transcript...

## ADR Зміна формату імені clean-файлу під час ADR-нормалізації

## Context and Problem Statement
Операція `rewrite` у `normalize-decisions.sh` перейменовувала чернетку з іменем `YYYYMMDD-HHMMSS-<sid>.md` на bare `<slug>.md`, тобто timestamp повністю зникав. Через це назва clean-файлу не відображала час сесії, не збігалася з чернеткою і `docs/adr/` не сортувався хронологічно.

## Considered Options
* Зберігати `YYYYMMDD-HHMMSS-`-префікс чернетки в імені clean-файлу (`<timestamp>-<slug>.md`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Зберігати `YYYYMMDD-HHMMSS-`-префікс чернетки в імені clean-файлу", because користувач явно сформулював вимогу: «залишай дату та час на початку файлу, щоб назва файлів рідше змінювалась».

### Consequences
* Good, because ім'я clean-файлу прив'язане до timestamp сесії і стабільне між ревізіями — slug змінює тільки суфікс, а не ціле ім'я.
* Good, because `docs/adr/` сортується хронологічно за `ls`, оскільки всі файли (draft і clean) мають спільний часовий префікс.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Змінені файли: `npm/.claude-template/hooks/normalize-decisions.sh` (нова `case`-гілка в `rewrite`), `.claude/hooks/normalize-decisions.sh` (project-копія, синкнута `bun ./npm/bin/n-cursor.js`), `npm/rules/adr/adr.mdc` (bump `v2.0`→`v2.1`, опис clean-імені, таблиця `rewrite`, дерево каталогу, абзац про `slug`), `npm/skills/adr-normalize/SKILL.md`, `npm/CHANGELOG.md`, `npm/package.json`.
- Логіка перейменування: draft `YYYYMMDD-HHMMSS-*` → `YYYYMMDD-HHMMSS-<slug>.md`; draft без префікса → fallback на bare `<slug>.md`; колізії — детермінований суфікс `-2`/`-3` на повному новому імені.
- Верифікація: `npx @nitra/cursor check adr changelog` — 2/2 ✅; `shellcheck` хука — OK; функціональний `case`-тест трьох патернів імені — коректно.
- Версія `npm/package.json`: `1.13.58` → `1.13.59` (розробка на `m2`); `1.13.59` → `1.13.66` (після rebase на `main`, що просунувся до `1.13.65`).
- Інтеграція: `git rebase main` на `m2`, резолв конфліктів (`package.json`, `CHANGELOG.md`, `n-security.mdc`), fast-forward `main`, push `origin/main` і force-push `origin/m2`. Фінальний коміт `2250454 adr`.
