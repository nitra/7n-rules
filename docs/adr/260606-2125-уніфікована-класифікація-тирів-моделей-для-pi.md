---
session: 38aa0305-a12b-4078-9085-ce03884efdd6
captured: 2026-06-06T21:25:01+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/38aa0305-a12b-4078-9085-ce03884efdd6.jsonl
---

this is the end of the transcript.

---

Проаналізуй цей transcript і надай ADR.

## ADR Уніфікована класифікація тирів моделей для pi

## Context and Problem Statement
Кілька скілів `@nitra/cursor` викликають LLM через `pi`, і кожен мав власні env vars для вибору моделі (`N_CURSOR_FIX_MODEL_HAIKU`, `N_CURSOR_FIX_MODEL_SONNET`). На різних машинах `pi` може мати різний default-провайдер (anthropic, google тощо), тому відсутність API-ключа в одного провайдера призводила до непрозорих помилок (`pi exit 1: No API key found for anthropic`) без підказки що робити.

## Considered Options
* Залишити per-skill env vars (`N_CURSOR_FIX_MODEL_HAIKU`, `N_CURSOR_FIX_MODEL_SONNET`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Глобальна шестирівнева класифікація тирів моделей", because user прямо продиктував класифікацію і вимогу що вона має бути спільною для всіх скілів, а не лише для `fix`.

### Consequences
* Good, because transcript фіксує очікувану користь: кожен скіл імпортує потрібний тир (`CLOUD_MIN`, `CLOUD_AVG`) замість власних env vars; конфіг налаштовується один раз у середовищі.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- `npm/lib/models.mjs` — новий модуль із 6 константами: `LOCAL_MIN`, `LOCAL_AVG`, `LOCAL_MAX`, `CLOUD_MIN`, `CLOUD_AVG`, `CLOUD_MAX`; читає `N_LOCAL_MIN_MODEL`, …, `N_CLOUD_MAX_MODEL` з `env`
- `npm/skills/fix/js/llm-worker.mjs` — оновлено: дефолти `MODEL = CLOUD_MIN`, `MODEL_HEAVY = CLOUD_AVG`; при відсутньому ключі видає повідомлення з підказкою встановити відповідний тир env var
- Env var формат значень: `provider/model-id` (наприклад `ollama/gemma3:4b`, `openai/gpt-5.4-mini`)
- Ескалація в оркестраторі: після 2 невдач на одному правилі переходить з `MODEL` на `MODEL_HEAVY`
- Commit: `85009988 ✨ feat(npm): global model tier classification — N_LOCAL_*/N_CLOUD_* env vars`
