---
session: f210f394-3b4a-4cae-a1e4-6ac768a3a7d5
captured: 2026-06-03T06:24:29+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/f210f394-3b4a-4cae-a1e4-6ac768a3a7d5.jsonl
---

Прочитав `ci4.mdc` цілком. Є конкретні відповідності і конкретні пробіли.

## Що збігається

Промпти у SKILL.md виконують **базові** вимоги ci4:
- ✓ Мова — українська (code identifiers залишаються ASCII)
- ✓ «Чистий Markdown, жодних HTML-обгорток (`<div>`/`<span>`/класів)» — токен-ефективність
- ✓ Контекстна незалежність (явно повторювати назви, заборона «як вище», «ця функція»)
- ✓ Rebuild Test як явна мета

## Що відсутнє або розходиться

**1. `<details>`/`<summary>` для змішаної аудиторії** — головний механізм ci4 (рядки 234–267) для поділу «менеджерська проза зверху / інженерні деталі всередині». У SKILL.md він ніде не згаданий. Промпт каже «жодних HTML-обгорток» — суб-агент легко прочитає це як заборону `<details>`, хоча ci4 явно каже, що `<details>` — **семантичний** елемент (не презентаційний), виняток дозволений.

**2. Структура Tier 2 (ARCHITECTURE.md) і Tier 3 (доменні доки)** не відображає arc42-скелет і Diátaxis, які ci4 рекомендує. Зокрема: немає вимоги писати manager-prose зверху, інженерні деталі — під `<details open=false>`, та прив'язувати ADR-посилання у форматі `[slug]`.

**3. Tier 3 — доменні доки** — це найближчий аналог `docs/explanation/`, але промпт не вказує субагенту використовувати `<details>` для Ops/Manager/Engineer-блоків.

## Висновок

Документація буде коректна за формою (Markdown, мова, контекстна незалежність), але **не буде ci4-сумісна** в частині `<details>`-collapsible-блоків для змішаної аудиторії. Для Tier 1 (файлові доки) — це менш критично (суто технічна аудиторія). Для Tier 2 і Tier 3 — помітний пропуск.

Хочете, щоб я оновив промпти у SKILL.md: додав вимогу `<details>/<summary>` для Engineer/Ops блоків у Tier 2 і Tier 3, і прибрав двозначність у Tier 1 («жодних HTML-обгорток» → явно дозволити `<details>`)?
