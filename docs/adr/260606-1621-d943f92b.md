---
session: d943f92b-e895-4110-8ae0-338380a03c95
captured: 2026-06-06T16:21:09+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/d943f92b-e895-4110-8ae0-338380a03c95.jsonl
---

Всі 4 пари готові. Ось повна порівняльна оцінка:

---

## sym=4 — `k8s-tree.mjs` (90/100 від скорера)

**Різниця: невелика, некритична**

Локальна версія правильно описує кешування і основну поведінку. Але:
- Огляд: "для подальшої обробки" — розмито
- Гарантії: стверджує ігнорування `.git` — **неправда**, ігнорується лише `.github/`
- Не відображає що ключ кешу будується за сортованим списком аргументів

Для споживача API критичної помилки немає, розробник зрозуміє призначення модуля.

---

## sym=5 — `lint.mjs` (80/100)

**Різниця: помірна, є одна неправда**

- Крок 4 Поведінки: "якщо команда завершується з помилкою через відсутність `uv`" — **семантична помилка**. `uv` перевіряється окремо на кроці 2, крок 4 просто виконує `zizmor`
- Гарантії: "`runLintGaCli` повертає `false` або `null`" — **неправда**, функція повертає exit code числом
- Описує `runStandardLint` lock (внутрішній механізм), не потрапляє у Публічний API

Помірно критично: один хибний інваріант у Гарантіях може ввести в оману.

---

## sym=6 — `workflows.mjs` (100/100 від скорера, **насправді найгірший**)

**Різниця: критична — детермінований скорер сліпий**

Локальна версія отримала 100/100 від score_a.mjs, але це найгірший документ:
- Поведінка — **6 внутрішніх функцій** лістяться як поведінка: `checkShellcheckInstalled`, `checkGaWorkflowFiles`, `runAllGaRego`, `checkApplyWorkflow`, `checkMegalinter`, `check` — витік імплементації замість опису поведінки
- Публічний API включає `lint-ga` (CLI-команда, не JS export) і `runAllGaRego` (internal)
- Гарантії: "Кешує результати перевірок у межах одного прогону workflow" — **хибна гарантія**, модуль не кешує
- Скорер А пропустив все це бо перевіряв лише Гарантії на витоки, не Поведінку і Публічний API

**Це підриває score_a.mjs як гейт.** sym ≥ 6 → cloud обов'язково.

---

## sym=7 — `consistency.mjs` (80/100)

**Різниця: помірно-висока, є смислові прогалини**

- "MTB:" префікс у Поведінці — артефакт з тренувальних даних моделі
- Крок 6: "якщо поточна версія випереджає опубліковану, виникає помилка" — **неповно**: версія може бути вища за реєстр якщо є change-file; помилка тільки якщо git-base violation або відсутній change-file
- Не згадує різницю між `registry-published` і `local-only` моделями бази
- Не згадує `REGISTRY_DISABLED` flag
- Гарантії загалом правильні

Критично для розробника що намагається зрозуміти коли саме падає перевірка.

---

## Загальний висновок

| sym | Критичність | Характер помилок |
|:---:|---|---|
| 4 | низька | Неточні деталі, ігноруємо |
| 5 | помірна | Одна хибна гарантія, один неправильний крок |
| 6 | **висока** | Масовий витік internal names, хибна гарантія, скорер сліпий |
| 7 | помірно-висока | Смислові прогалини в умовах падіння |

**Поріг `sym ≥ 5` → cloud виглядає більш консервативним і правильним.** При `sym ≥ 4` локальна 4-ка ще справляється, але sym=5 вже дає брехливі гарантії. При `sym ≥ 6` ризик хибних 100% балів від детермінованого скорера при реально поганій документації.
