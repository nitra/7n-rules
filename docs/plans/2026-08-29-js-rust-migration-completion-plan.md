# План завершення міграції JS → Rust

**Створено:** 2026-08-29. **Оновлено:** 2026-08-30 — усі числа переміряні
заново, не перенесені; додано третю колію (дистрибуція) після появи
`nitra/oci-dist`.

Цей документ — не переказ реєстру `2026-08-05-open-questions-register.md`, а
відповідь на одне питання: **що лишилось і в якому порядку це брати**.
Обґрунтування кожного окремого рішення живе в реєстрі; сюди винесено лише
залишок, згрупований так, щоб його можна було роздати паралельно.

## Де ми зараз

Порахунок відтворюваний. JS-фіксери:

```bash
git ls-files 'npm/rules/**/fix-*.mjs' 'plugins/*/rules/**/fix-*.mjs' | grep -v '/tests/'
```

| | 2026-08-29 | 2026-08-30 (перезібрано) |
|---|---:|---:|
| JS-фіксерів у репо (без тек `tests/`) | 69 | **11** |
| з них борг «спершу парність» | 27 | **0** |
| ще не портовано | 42 | **0** |
| LLM-воркери (не T0-фікси) | — | **4** |
| задокументовані винятки з названою причиною | — | **7** |
| нативних фіксів (`NATIVE_FIXES`, `rules-core`) | 14 | **32** |
| wasm-гостей | 6 | 6 |
| детекторів, що ще експортують `lint` | 6 | 6 |

Детект-половина міграції закрита (§2.24) і **не зрушила** за цей час: ті самі
шість `main.mjs` експортують `lint`, структурно виключені й задокументовані.
Усе, що нижче про концерни — **fix-половина**.

Головне: **залишок fix-міграції — нуль**, і борг «спершу парність» — теж
нуль. Одинадцять JS-фіксерів, що лишились, поділені рівно на дві категорії з
названою причиною на кожен (розділ «Залишок» нижче). Це вже не програма робіт.

Два попередні варіанти цієї таблиці (69→12, потім 42→2) були **хибні** —
обидва рахували гілки `detect` за гілки `fix`. Ця редакція зібрана з
машинно звірюваної таблиці `FIX_STAYS_IN_JS` і доккоментарів гостей, не з
грепу; §2.99 реєстру описує механізм помилки й перевірку проти неї.

## Дві колії, не одна

Ключове, що варто тримати в голові при плануванні: концерни мігрують
**двома незалежними колiями**, і плутати їх дорого.

- **Ядро (`npm/rules/**`) → нативно в `crates/rules-core`.** Без wasm, без
  гостя, без бюджету розміру. Диспатч — `listNativeFixes()` →
  `nativeFixPattern`. Прецедент: §2.67.
- **Плагіни (`plugins/**`) → у wasm-гостя.** Бюджет 10 MiB (§2.55), rego
  безкоштовний через host-імпорт (§2.66).

Одна й та сама «родина» концернів (напр. `vscode_*`) розсипана по обох
колiях — це головна причина, чому її не можна взяти одним махом.

## Третя колія — дистрибуція (додано 2026-08-30)

Дві колії вище описують, **де виконується** концерн. Вони нічого не кажуть
про те, **як код доїжджає до консюмера** — бо доти це робив npm і питання не
стояло. Зріз 6 знімає npm, і дистрибуція стає третьою колією плану, зі своїм
порядком і своїми залежностями.

**Транспорт уже не наша задача.** `nitra/oci-dist` — окремий репозиторій, що
виніс і знеособив `r-plugin`'s `r-plugin-oci`/`r-plugin-package`; крейти
`oci-dist-package`/`oci-dist-oci` опубліковані `0.1.5` у kellnr `crates.7n.ai`.
Сім із десяти відкритих питань §12.4 зрізу 6 закриті саме ним: формат
артефакта, резолв без каталогу, lock із точними SHA-256, digest-first
цілісність, офлайн-кеш, креденшели з docker store, реєстр як частина
ідентичності. Журнал рішень — `nitra/oci-dist` →
`docs/specs/2026-08-30-oci-transport-unification.md`.

**Що лишається зробити саме тут** (коду під це в репозиторії ще немає):

| # | Робота | Форма |
|---|---|---|
| Д1 | `npm/scripts/lib/resolve-plugins.mjs` — резолв за npm-залежностями → резолв за lock + OCI reference. Автодетект за файловими сигналами лишається: він відповідає «які плагіни потрібні», не «де їх узяти» | JS, потім Rust |
| Д2 | Публікація кожного `crates/plugin-*` як власного OCI-артефакта | CI |
| Д3 | `npm/wasm-plugins/` видаляється; `builtin-pins.json` переродити в lock-формат `oci-dist`, не заводити другий | видалення |
| Д4 | Канонічний рядок виклику `bunx n-rules …` → `n-rules …` — у **14 шаблонах/сніпетах**, які лягають у чужі репозиторії, і в `bun/package_json`, який переписує чужі виклики на цей рядок | міграція консюмерів |

**Д4 — єдина робота плану з двобічним обмеженням у часі.** Перейти до неї
раніше зрізу 6 не можна (доти `n-rules` існує лише як npm-пакет, і `bunx` —
єдине, чим його запустити), забути після — це
лишити кожного консюмера з викликом каналу, якого більше немає. Тобто зріз 6
несе не лише свою реалізацію, а й міграцію вже переписаних чужих файлів.

**Масштаб Д4 переміряно 2026-08-30** (`grep -rl 'bunx n-rules'`): 62 файли в
репозиторії, з них **14 — консюмер-facing** шаблони й слот-сніпети, що
лягають у чужі CI-файли (`plugins/ci-github/**/template/*.snippet.yml` — 8,
`plugins/ci-azure/**/template/*.snippet.yml` — 3, `plugins/*/slots/ci/github/` — 3).
Д4 — не «один рядок», а ця чотирнадцятка плюс `bun/package_json`.

**Зачіпка за fix-колію.** `bun/package_json` — не просто зачеплений за Д4, він
**виробник** його боргу: канон обходить дерево консюмера й переписує знайдені
виклики скриптів на `bunx n-rules lint <surface>` (§2.92). Тобто кожен його
прогін до зрізу 6 додає в чужі репозиторії ще один виклик каналу, який
зникне. Це не блокер (раніше зрізу 6 змінити рядок нема на що), але означає,
що радіус Д4 **зростає з часом**, і відкладати зріз 6 не безкоштовно.

Тому й важелі бізнес-правила не рівноцінні: два з чотирьох (переписування
викликів; канонізація скрипта у форвардер) вшивають рядок запуску в чужий
репозиторій, detect-only — ні. Це не аргумент за detect-only сам по собі, але
ціна, якої розбір бізнес-правила спершу не бачив.

**Хвіст у споживача транспорту.** `nitra/r-plugin` на `main` пінить
`oci-dist-oci = "=0.1.1"` — версію **до** виправлення дефекту пінування
(мережевий fetch не звірявся з lock-digest; cache-miss при наявному піні був
гучною помилкою навіть онлайн). Підняти до `=0.1.5` варто **до** того, як
`7n-rules` підключиться, щоб обидва споживачі стояли на одному виправленому
шляху, а не розходились у поведінці пінування.

## Залишок fix-міграції — нуль. Fix-половину закрито

**Перезібрано 2026-08-30 з машинних джерел, не з грепу.** Попередні дві
редакції цього розділу помилялись однаково: список складався грепом згадок
`CONCERN_*` у гості, а рядок збігу приймався за гілку `fix`. Гілки `detect` і
`fix` живуть в ОДНОМУ `impl Guest` і розрізняються лише позицією — див. §2.99
реєстру, там же перевірка на два маркери, що дає межу однією командою.

Джерела правди, з яких зібрано цю редакцію:

- таблиця `FIX_STAYS_IN_JS`
  (`npm/scripts/lib/lint-surface/tests/wasm-plugin-parity.test.mjs:6351`) —
  **машинно звіряється** і з диском, і з живим маніфестом гостя;
- розділи «Портовано ЛИШЕ detect» доккоментарів гостей
  (`crates/plugin-ci-github/src/lib.rs:226`, `crates/plugin-ci-azure/src/lib.rs:508`);
- `NATIVE_FIXES` (`crates/rules-core/src/concerns/fix.rs`) для ядрової колії.

### Усі 11 JS-фіксерів, що лишились, — не борг

```bash
git ls-files 'npm/rules/**/fix-*.mjs' 'plugins/*/rules/**/fix-*.mjs' | grep -v '/tests/'
```

**LLM-воркери — 4.** `doc-files/check`, `test/coverage`, `text/cspell-fix`,
`js/eslint/fix-worker.mjs`. Не T0-фікси взагалі; ключ у `NATIVE_FIXES` створив
би фіктивний патерн, що ЗАТІНИВ би реальний шлях
(`crates/rules-core/src/concerns/fix.rs:41-45`).

**Задокументовані винятки — 7.**

| Канон | Причина | Де зафіксовано |
|---|---|---|
| `bun/package_json` | тип межі `source-file.content: string` при семантиці «бачити все дерево» | §2.92, `FIX_STAYS_IN_JS` |
| `js/eslint` | канон робить `eslint --fix` через programmatic API; гість гейтить обидва лінтери на `path:bunx` | §2.93, `FIX_STAYS_IN_JS` |
| `test/storybook-vitest-config` | хірургічний string-splice чужого `vitest.config.*` | §2.87, `FIX_STAYS_IN_JS` |
| `test/stryker_config` | fix потребує ПОВТОРНОГО планування по дереву; napi-міст будує `FixRequest::files` лише з полів `file` violations | зріз 1 контракту v3.1, `FIX_STAYS_IN_JS` |
| `ga/service_deploy_workflow` | портовано лише detect | `plugin-ci-github/src/lib.rs:226` |
| `ci_artifact/consume` | слот-граф і canonical-шаблони ЧУЖИХ плагінів поза `fs-read`/`repo-root@1`; порт інвертував би sandbox-модель | §2.98 |
| `azure-pipelines/service_deploy_pipeline` | немає host-каналу до реєстру ввімкнених правил; ЧАСТКОВИЙ порт був би ГІРШИМ за відсутність порту | `plugin-ci-azure/src/lib.rs:508-527` + тест «fix — порожній план» |

**Тобто борг «спершу парність» — нуль канонів, і залишок портування — нуль
концернів.** Fix-половина міграції закрита тим самим числом, яким закрито
`tauri/release` (§2.97): останній концерн, що мав рацію «не портуємо»,
пережиту власною причиною.

### Що це означає для формулювання «повна міграція на Rust»

Сім винятків — не хвіст, який колись доберуть. Кожен має названу причину, і
чотири з семи впираються в **контракт**, а не в брак часу:
`source-file.content: string`, `FixRequest::files` лише з `file`-полів
violations, відсутній host-імпорт до слот-графа, відсутній канал до реєстру
ввімкнених правил. Розблокує їх не хвиля портів, а зміни контракту — тобто
робота того ж класу, що мажор `4.0.0` (§2.84), який свого часу розблокував
бінарну родину `image-*`.

**Восьмий виняток особливий і має термін.** `js/eslint` тримається на драбині
«гість → канон»: гість гейтить обидва лінтери на `path:bunx` і на дереві без
`bunx` повертає порожній план, після чого спрацьовує JS-канон.

**Уточнено 2026-08-30 (попереднє формулювання було неточним).** Зріз 6 прибирає
не `bunx`, а `bunx n-rules` — сам `bunx` у JS-репозиторії, що користується bun,
лишається. Механізм ризику інший і вужчий: **сьогодні `bun` гарантовано
присутній скрізь, де взагалі виконується `n-rules`**, бо канонічний запуск і є
`bunx n-rules`. Після зрізу 6 `n-rules` — самостійний бінар, і JS-репозиторій на
npm/pnpm/yarn може ніколи не мати bun. Зникає не `bunx`, а **неявна гарантія
його присутності**.

Висновок від цього не змінюється, але масштаб більший, ніж один концерн:
`path:bunx` тримає і `ESLINT_TOOL` (`crates/plugin-lang-js/src/lib.rs:12359`), і
`JSCPD_TOOL` (`:12551`), а `plugin.toml:36` декларує ще й `path:bun`. Відвʼязати
треба клас, а не випадок, і ДО зрізу 6 — див. §2.99 (із цим самим уточненням) і
§2.100.


## Порядок, який я пропоную

**Перезібрано 2026-08-30.** Пункти 2-5 попередньої редакції (порт
`tauri/release`, порт `ci_artifact/consume`, рішення по `bun/package_json`,
зняття пʼятьох канонів) більше не існують як робота: перший **зроблено**
(§2.97), решта виявились не задачами, а вже ухваленими рішеннями (§2.98,
§2.99). Лишається:

1. **`r-plugin` → `oci-dist`** — PR відкрито (`git.7n.ai/nitra/r-plugin/pulls/1`),
   але на `=0.1.4`, не `0.1.5`: тег `v0.1.5` запушено, а публікації в kellnr
   не сталося — індекс віддає максимум `0.1.4` для обох крейтів. Виправлення
   пінування приїхало в `0.1.3`, тож ціль пункту досягнута; сам зламаний
   реліз-прогін `oci-dist` лишається окремою задачею.
2. **Відвʼязати `lang-js` від неявної гарантії bun** — єдина робота fix-колії з
   ТЕРМІНОМ. Не лише `fix_js_eslint`: `path:bunx` тримає і `ESLINT_TOOL`, і
   `JSCPD_TOOL`, плюс `path:bun` у `plugin.toml`. Після зрізу 6 присутність
   bun перестає бути гарантованою, а канон, що страхує гостя, планувалось
   зняти (§2.99, §2.100).
3. **Рішення власника по `bun/package_json`** — не порт (§2.92 закрила це),
   а вибір бізнес-правила: чи концерн і далі вироблятиме борг Д4.
4. **Дистрибуція Д1–Д4** — окремою послідовністю, темп і старт за рішенням
   власника. Усередині: Д2 (публікація артефактів) → Д1 (резолв) → Д3
   (видалення `wasm-plugins/`) → Д4 (міграція консюмерів). Д3 не можна
   робити раніше Д1, Д4 — раніше решти.

Паралелізувати тут уже майже нічого: fix-колія закрита, і залишок — це
рішення власника, одна точкова правка гостя й дистрибуція, яка сама по собі
послідовна. **Урок трьох паралельних хвиль 2026-08-30:** три з чотирьох
задач виявились неіснуючими, і субагенти це виявили ПЕРЕД тим, як щось
видалити, — бо бриф вимагав розвідки перед кодом. Дешевша перевірка, ніж
паралелізм, — звірка плану з машинними джерелами ПЕРЕД тим, як його роздавати.

## Що спочатку виглядало блокером, а виявилось ні

Варто памʼятати, бо це повторювалось:

- **Стеля розміру гостя** (§2.55) — була оцінкою під іншого гостя, без
  жодного зовнішнього обмеження під нею.
- **`regorus` у кожному гості** (§2.66) — виніс у хост, гість схуд на 41,8%,
  а новий гість дістає rego безкоштовно.
- **exec-tool як структурна перешкода** (§2.51 → §2.64) — одна з двох
  «перешкод» спиралась на хибну засновку про rollback у T0.
- **anchor/alias у YAML** (§2.62) — виявилось, що ламав лише flow-стиль, а
  висновок про anchor був артефактом фікстури з двома змінними.

Спільне в усіх чотирьох: **перевірка вимірюванням спростовувала припущення**.
Перед тим як оголошувати щось блокером, варто зміряти.

## CI вимкнено на час міграції (рішення власника 2026-08-30)

Усі **15** воркфлоу репозиторію переведені в `disabled_manually`
(`gh workflow list --all`). Причина — `main` був червоний сімнадцятьма
фейлами, з яких лише два виявились реальним дрейфом (виправлено: #578, #579),
а дванадцять були структурно непрохідними в CI: збірка одного wasm-гостя з
шести й відсутній `kubescape` (#580). Замість того щоб добивати гейт паралельно
з міграцією, гейт зупинено до її завершення.

**Що це коштує, поки триває:** нічого не заважає влити зламане в `main`.
Компенсація — цільові прогони перед кожним мержем (`cargo test -p <крейт>`,
`npx vitest run <файл>`), і вони мають бути НАЗВАНІ в описі PR, інакше
«перевірено» не перевіряється.

**Умова повернення:** після зрізу 6. До того ж моменту треба переглянути, чи
гейт узагалі має лишатись у теперішній формі — п'ять із його сюїтів
перевіряють склад резолву JS-канонів, а канонів після Д1-Д4 не буде.

## Відкриті питання, що впливають на план

- **Анти-дрейф-гейт на `%q`** (§2.68). Переміряно 2026-08-30
  (`grep -ro '%q'`): **82** входження — 63 у ядрі, 19 у плагінах (`lang-js` 7,
  `ci-azure`/`ci-github` по 3, решта lang-плагінів по 2). Було 76 на
  2026-08-29, тобто **число зростає** — нові `%q` продовжують зʼявлятись,
  поки гейта немає. Сьогодні вони безпечні (`conftest` — Go, `%q` легальний),
  але стають помилкою рантайму в момент переїзду концерну на regorus; це вже
  ловили тричі вручну. Гейт має бути привʼязаний до політик, **досяжних через
  regorus**, а не глобальною забороною — інакше він хибно червонить усі 82.
- **Бізнес-правило `bun/package_json`** — не технічне питання, рішення
  власника. Портування воно НЕ розблоковує (§2.92 — блокер у типі межі,
  не в правилі); воно визначає лише, чи концерн і далі вироблятиме борг Д4.
- **Доля `npm/rules/**` ядра, `schemas/`, `.claude-template`** — свідомо не
  вирішено (§12.4 п.7). Вшивання в бінар доречне для ядра, але рішення
  відкладене до реального старту зрізу 6.
- **Прибирання worktree — зроблено 2026-08-29** з дозволу власника: 73 ГБ
  build-кешів + 87 worktree (38 із залишковими змінами, звіреними як такі,
  що вже в `main`; 49 чистих). Дифи 38 заархівовані у
  `~/www/nitra/7n-rules-worktree-residue-20260829`. Лишились 6 із реально
  незданою роботою. За поточного темпу паралельних хвиль це
  повертатиметься — довгострокове рішення й далі `CARGO_TARGET_DIR` назовні
  для агентів, з обовʼязковим застереженням, бо саме ця змінна тричі
  коштувала годин на пошук неіснуючого бага.
