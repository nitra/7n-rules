# Review PR 263 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Провести доказовий read-only review PR #263 і повернути пріоритезований список зауважень.

**Architecture:** Review прив'язує кожне зауваження до конкретного diff path, тесту або публічного contract. Жодних коментарів чи змін у GitHub не надсилаються до окремого погодження.

**Tech Stack:** GitHub connector, GitHub CLI, Bun, Vitest, Composer manifest semantics.

## Global Constraints

- Не змінювати branch, PR description, labels, reviews або comments.
- Не оцінювати repo-wide CI debt як дефект #263, якщо finding не лежить у його diff.
- Кожне finding має містити severity, evidence і конкретний сценарій несправності.

---

### Task 1: Composer taze і manifest contract

**Files:**
- Review: `plugins/lang-php/taze/{composer-diff,provider}.mjs`
- Review: `plugins/lang-php/rules/php/composer_manifest/{main.mjs,concern.json,composer_manifest.mdc}`
- Test: відповідні `tests/*.test.mjs`.

- [ ] **Step 1: Прочитати diff та tests разом**

Run: `gh pr diff 263 --repo nitra/7n-rules -- plugins/lang-php/taze plugins/lang-php/rules/php/composer_manifest`

Expected: кожна заявлена Composer semantic має тестовий evidence.

- [ ] **Step 2: Перевірити edge cases**

Переглянути платформні packages (`php`, `ext-*`), package version constraints, dev requirements, sorted dependencies, license та відсутність Composer binary.

- [ ] **Step 3: Записати findings**

Формат кожного: `P0–P3 | file:line | scenario | observed behavior | required correction`.

### Task 2: Nested workspaces, coverage і doc-files

**Files:**
- Review: `npm/scripts/lib/resolve-plugins.mjs`
- Review: `plugins/lang-php/{coverage-provider,doc-files}/**/*.mjs`
- Test: їхні `tests/*.test.mjs`.

- [ ] **Step 1: Перевірити nested workspace boundary**

Встановити, що globs обмежують discovery глибиною 2 і не захоплюють `vendor/`, а root-only policy не застосовується до вкладених manifests.

- [ ] **Step 2: Перевірити parsing results**

Звірити Clover totals, Infection status mapping, невалідний XML, порожні files і docblock exports з тестами.

- [ ] **Step 3: Запустити targeted tests у read-only checkout**

Run: `bunx vitest run plugins/lang-php/coverage-provider/tests plugins/lang-php/doc-files/tests plugins/lang-php/taze/tests npm/scripts/lib/tests/resolve-plugins.test.mjs`

Expected: tests green або failure класифіковано як baseline.

### Task 3: Підсумувати review

**Files:**
- Modify: немає.

- [ ] **Step 1: Перевірити GitHub review threads і CI**

Run: `gh pr checks 263 --repo nitra/7n-rules && gh pr view 263 --repo nitra/7n-rules --comments`

Expected: знайдені external concerns відокремлені від власних findings.

- [ ] **Step 2: Повернути concise report**

Якщо defects не знайдено, прямо повідомити `No findings`; інакше відсортувати за severity та додати merge recommendation. Не публікувати review без явного дозволу.
