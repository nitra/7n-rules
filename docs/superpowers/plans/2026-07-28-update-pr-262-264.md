# Update PR 262 and 264 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Розв'язати конфлікти PR #262 і #264 з green `main`, не змінюючи їхній задум.

**Architecture:** Кожен PR оновлюється у власній гілці після merge CI debt. Конфлікти розв'язуються на користь актуального `main` для спільного tooling і на користь PR для ізольованої feature logic.

**Tech Stack:** GitHub CLI, Git, Bun, Vitest, `@7n/rules`.

## Global Constraints

- Не об'єднувати #262 і #264 та не додавати зміни #266.
- Не force-push без явного повідомлення автора гілки; prefer rebase + `--force-with-lease`.
- Фінальний PR status оцінювати лише після green CI cleanup PR.

---

### Task 1: Оновити PR #262

**Files:**
- Modify: лише файли конфлікту між `main` та `codex/git-reconcile-postrun-defects`.
- Test: `npm/skills/git-reconcile/js/tests/orchestrate.test.mjs`.

- [ ] **Step 1: Створити локальну гілку від PR head і отримати новий main**

Run: `git fetch origin main codex/git-reconcile-postrun-defects && git switch --create review/pr-262 origin/codex/git-reconcile-postrun-defects && git rebase origin/main`

Expected: або чистий rebase, або точний список conflict files.

- [ ] **Step 2: Розв'язати кожен конфлікт через тристоронню звірку**

Зберегти final `git-reconcile` behavior: patch-equivalent release entries, lockfile validation, CI baseline classification і safe worktree cleanup; прийняти актуальні зміни `main` поза цими concern-ами.

- [ ] **Step 3: Запустити focused verification**

Run: `bunx vitest run npm/skills/git-reconcile/js/tests/orchestrate.test.mjs && npx @7n/rules lint && npx @7n/rules lint changelog && git diff --check`

Expected: exit 0 до push.

- [ ] **Step 4: Оновити віддалену PR-гілку**

Run: `git push --force-with-lease origin HEAD:codex/git-reconcile-postrun-defects`

Expected: GitHub запускає CI на новому head.

### Task 2: Оновити PR #264

**Files:**
- Modify: лише файли конфлікту між `main` та `claude/doc-files-wave-batch-phase2`.
- Test: `npm/rules/doc-files/docgen-wave-batch/tests/docgen-wave-batch.test.mjs` і наявні `docgen-files-batch`, `docgen-gen`, `docgen-judge` suites.

- [ ] **Step 1: Створити локальну гілку від PR head і rebаse**

Run: `git fetch origin main claude/doc-files-wave-batch-phase2 && git switch --create review/pr-264 origin/claude/doc-files-wave-batch-phase2 && git rebase origin/main`

Expected: або чистий rebase, або точний список conflict files.

- [ ] **Step 2: Зберегти хвильову batch semantics**

Після розв'язання конфліктів перевірити: required-wave failure ізолюється per-file; `CRITIC_NONE_RE` не запускає refine; best-of-2 не виконує другий critique/refine; judge gate правильно маркує `degraded`.

- [ ] **Step 3: Запустити focused verification**

Run: `bunx vitest run npm/rules/doc-files/docgen-wave-batch/tests npm/rules/doc-files/docgen-files-batch/tests npm/rules/doc-files/docgen-gen/tests npm/rules/doc-files/docgen-judge/tests && npx @7n/rules lint && npx @7n/rules lint changelog && git diff --check`

Expected: усі suites і gates exit 0.

- [ ] **Step 4: Оновити віддалену PR-гілку**

Run: `git push --force-with-lease origin HEAD:claude/doc-files-wave-batch-phase2`

Expected: GitHub запускає CI на новому head.

### Task 3: Підтвердити merge readiness

**Files:**
- Modify: немає.

- [ ] **Step 1: Дочекатися terminal GitHub checks для #262 і #264**

Run: `gh pr checks 262 --repo nitra/7n-rules --watch && gh pr checks 264 --repo nitra/7n-rules --watch`

Expected: усі required checks success; merge state не `DIRTY`.

- [ ] **Step 2: Перевірити review threads**

Run: `gh api graphql -f query='query { repository(owner:"nitra", name:"7n-rules") { pullRequest(number:262) { reviewThreads(first:100) { nodes { isResolved } } } } }'`

Expected: немає unresolved threads; повторити для #264.
