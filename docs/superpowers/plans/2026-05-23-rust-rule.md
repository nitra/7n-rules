# Rust Rule (`lint-rust`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Додати правило `rust` у `@nitra/cursor` (`lint-rust` через `cargo fmt` + `cargo clippy`, CI workflow, VSCode-екстеншени) і звузити `tauri.mdc`, перенісши вимогу `rust-lang.rust-analyzer` у нове правило.

**Architecture:** Rego-heavy за зразком `style-lint`/`js-lint`: три rego-policy-пакети (`package_json`, `vscode_extensions`, `lint_rust_yml`), що читають канонічні літерали виключно через `data.template.*`. `js/tooling/check.mjs` — лише FS-existence + `runConftestBatch`-orchestrator. `js/applies/check.mjs` — applies-gate за наявністю `Cargo.toml` (root або workspace). `fix.mjs` — 20-рядковий entry-point, делегує до `runStandardRule` (де серіалізація через `withLock`).

**Tech Stack:** Node 24+, Bun 1.3+, Rego (`conftest`), `@nitra/cursor` package internals (`runConftestBatch`, `createCheckReporter`, `runStandardRule`).

**Spec:** [docs/superpowers/specs/2026-05-23-rust-rule-design.md](../specs/2026-05-23-rust-rule-design.md)

---

## Task 1: Scaffolding каталогу правила

**Files:**
- Create: `npm/rules/rust/auto.md`
- Create: `npm/rules/rust/fix.mjs`
- Create: `npm/rules/rust/utils/has-cargo-toml.mjs`
- Create: `npm/rules/rust/utils/has-cargo-toml.test.mjs`

- [ ] **Step 1: Створити `npm/rules/rust/auto.md`**

```
якщо в проекті є хоч один Cargo.toml
```

- [ ] **Step 2: Створити `npm/rules/rust/fix.mjs` (verbatim з abie/style-lint)**

```js
import { runStandardRule } from '../../scripts/utils/run-standard-rule.mjs'

/**
 * Запускає правило: applies → JS-concerns → policy → mdc-refs (через runStandardRule).
 * Library mode: викликається CLI orchestration через `import + run(ctx)`.
 * @param {import('../../scripts/utils/run-standard-rule.mjs').RuleContext} [ctx] контекст прогону (walkCache тощо)
 * @returns {Promise<number>} 0 — OK, 1 — порушення
 */
export function run(ctx) {
  return runStandardRule(import.meta.dirname, ctx)
}

if (import.meta.main) {
  // Standalone: bun rules/<id>/fix.mjs — повний еквівалент `npx @nitra/cursor fix <id>`
  // (config-loading + whitelist + summary). Дві ролі fix.mjs: library (run) + standalone (main).
  const { runRuleCli } = await import('../../scripts/utils/run-rule-cli.mjs')
  // eslint-disable-next-line unicorn/no-process-exit -- standalone entry-point має повертати exit-code для CI/IDE
  process.exit(await runRuleCli(import.meta.dirname))
}
```

- [ ] **Step 3: Написати failing test `npm/rules/rust/utils/has-cargo-toml.test.mjs`**

```js
import { describe, expect, test } from 'bun:test'
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'

import { hasCargoTomlInTree } from './has-cargo-toml.mjs'

const IGNORED_NAMES = new Set(['node_modules', '.git', '.next', '.turbo'])

describe('hasCargoTomlInTree', () => {
  /** @returns {string} абсолютний шлях до тимчасового кореня */
  function makeRoot() {
    return mkdtempSync(join(tmpdir(), 'rust-has-cargo-'))
  }

  test('повертає true при наявності Cargo.toml у корені', () => {
    const root = makeRoot()
    writeFileSync(join(root, 'Cargo.toml'), '[package]\nname="x"\n')
    try {
      expect(hasCargoTomlInTree(root, IGNORED_NAMES)).toBe(true)
    } finally {
      rmSync(root, { recursive: true, force: true })
    }
  })

  test('повертає true при Cargo.toml у workspace-підкаталозі', () => {
    const root = makeRoot()
    mkdirSync(join(root, 'src-tauri'), { recursive: true })
    writeFileSync(join(root, 'src-tauri', 'Cargo.toml'), '[package]\nname="t"\n')
    try {
      expect(hasCargoTomlInTree(root, IGNORED_NAMES)).toBe(true)
    } finally {
      rmSync(root, { recursive: true, force: true })
    }
  })

  test('повертає false при відсутності Cargo.toml', () => {
    const root = makeRoot()
    writeFileSync(join(root, 'package.json'), '{}')
    try {
      expect(hasCargoTomlInTree(root, IGNORED_NAMES)).toBe(false)
    } finally {
      rmSync(root, { recursive: true, force: true })
    }
  })

  test('ігнорує Cargo.toml у node_modules/', () => {
    const root = makeRoot()
    mkdirSync(join(root, 'node_modules', 'pkg'), { recursive: true })
    writeFileSync(join(root, 'node_modules', 'pkg', 'Cargo.toml'), '')
    try {
      expect(hasCargoTomlInTree(root, IGNORED_NAMES)).toBe(false)
    } finally {
      rmSync(root, { recursive: true, force: true })
    }
  })
})
```

- [ ] **Step 4: Запустити тест — переконатися, що falls**

Run: `cd npm && bun test rules/rust/utils/has-cargo-toml.test.mjs`
Expected: FAIL (`Cannot find module './has-cargo-toml.mjs'`)

- [ ] **Step 5: Створити `npm/rules/rust/utils/has-cargo-toml.mjs`**

```js
/**
 * Rule-level applies-walker правила rust: рекурсивно шукає Cargo.toml у
 * cwd або будь-якому workspace-підкаталозі. Пропускає `node_modules`, `.git`,
 * `.next`, `.turbo` за тим самим списком, що `npm/scripts/auto-rules.mjs`.
 * Утиліта rule-local, бо лише `rust` потребує "знайти Cargo.toml у дереві".
 */
import { readdirSync } from 'node:fs'
import { join } from 'node:path'

/**
 * Чи присутній хоч один Cargo.toml у дереві `root` (синхронно, з раннім return).
 * @param {string} root абсолютний шлях кореня
 * @param {Set<string>} ignoredDirNames імена директорій, в які НЕ заходимо
 * @returns {boolean} true, якщо знайдено Cargo.toml
 */
export function hasCargoTomlInTree(root, ignoredDirNames) {
  /**
   * @param {string} dir
   * @returns {boolean}
   */
  function walk(dir) {
    let entries
    try {
      entries = readdirSync(dir, { withFileTypes: true })
    } catch {
      return false
    }
    for (const entry of entries) {
      if (entry.isFile() && entry.name === 'Cargo.toml') return true
      if (entry.isDirectory() && !ignoredDirNames.has(entry.name)) {
        if (walk(join(dir, entry.name))) return true
      }
    }
    return false
  }
  return walk(root)
}
```

- [ ] **Step 6: Запустити тест — переконатися, що passes**

Run: `cd npm && bun test rules/rust/utils/has-cargo-toml.test.mjs`
Expected: PASS (4/4)

- [ ] **Step 7: Commit**

```bash
git add npm/rules/rust/
git commit -m "feat(rust): scaffold rule (fix.mjs, auto.md, has-cargo-toml walker)"
```

---

## Task 2: `policy/package_json/` концерн (substring у scripts.lint-rust)

**Files:**
- Create: `npm/rules/rust/policy/package_json/target.json`
- Create: `npm/rules/rust/policy/package_json/template/package.json.contains.json`
- Create: `npm/rules/rust/policy/package_json/package_json.rego`
- Create: `npm/rules/rust/policy/package_json/package_json_test.rego`

- [ ] **Step 1: Створити `target.json`**

```json
{
  "$schema": "https://unpkg.com/@nitra/cursor/schemas/target.json",
  "files": { "single": "package.json" }
}
```

- [ ] **Step 2: Створити `template/package.json.contains.json` (канон підрядків)**

```json
{
  "scripts": {
    "lint-rust": [
      "cargo fmt --all",
      "cargo clippy --fix --allow-staged --allow-dirty",
      "cargo clippy --all-targets --all-features -- -D warnings"
    ]
  }
}
```

- [ ] **Step 3: Написати failing test `package_json_test.rego`**

```rego
package rust.package_json_test

import data.rust.package_json
import rego.v1

template_data := {"contains": {"scripts": {"lint-rust": [
	"cargo fmt --all",
	"cargo clippy --fix --allow-staged --allow-dirty",
	"cargo clippy --all-targets --all-features -- -D warnings",
]}}}

valid_pkg := {"scripts": {"lint-rust": "cargo fmt --all && cargo clippy --fix --allow-staged --allow-dirty --all-targets --all-features && cargo clippy --all-targets --all-features -- -D warnings"}}

test_allow_canonical if {
	count(package_json.deny) == 0 with input as valid_pkg with data.template as template_data
}

test_deny_missing_lint_rust_script if {
	bad := json.patch(valid_pkg, [{"op": "remove", "path": "/scripts/lint-rust"}])
	count(package_json.deny) > 0 with input as bad with data.template as template_data
}

test_deny_missing_fmt_step if {
	bad := json.patch(valid_pkg, [{"op": "replace", "path": "/scripts/lint-rust", "value": "cargo clippy --fix --allow-staged --allow-dirty --all-targets --all-features && cargo clippy --all-targets --all-features -- -D warnings"}])
	some msg in package_json.deny with input as bad with data.template as template_data
	contains(msg, "cargo fmt --all")
}

test_deny_missing_clippy_fix if {
	bad := json.patch(valid_pkg, [{"op": "replace", "path": "/scripts/lint-rust", "value": "cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings"}])
	some msg in package_json.deny with input as bad with data.template as template_data
	contains(msg, "cargo clippy --fix")
}

test_deny_missing_d_warnings if {
	bad := json.patch(valid_pkg, [{"op": "replace", "path": "/scripts/lint-rust", "value": "cargo fmt --all && cargo clippy --fix --allow-staged --allow-dirty"}])
	some msg in package_json.deny with input as bad with data.template as template_data
	contains(msg, "-D warnings")
}

# Drift test: підміна data.template веде перевірку.
test_data_template_drives_contains if {
	some msg in package_json.deny with input as valid_pkg
		with data.template as {"contains": {"scripts": {"lint-rust": ["custom-cargo"]}}}
	contains(msg, "custom-cargo")
}
```

- [ ] **Step 4: Запустити failing rego тест**

Run: `cd npm && bun run lint-rego`
Expected: FAIL (`package rust.package_json не існує` або undefined `package_json.deny`)

- [ ] **Step 5: Створити `package_json.rego`**

```rego
# Перевірка `package.json` для правила rust (rust.mdc).
#
# Канон надходить через --data: { "template": { "contains": ... } }
# Структура --data сформована з template/package.json.contains.json.
# Перевіряємо substring-вимоги до scripts.lint-rust: усі три кроки
# (`cargo fmt`, `cargo clippy --fix`, фінальний `cargo clippy ... -D warnings`)
# мають бути присутніми у значенні скрипта.
package rust.package_json

import rego.v1

deny contains msg if {
	some script_name, needles in data.template.contains.scripts
	actual := object.get(object.get(input, "scripts", {}), script_name, "")
	some needle in needles
	not contains(actual, needle)
	msg := sprintf("package.json: scripts.%s має містити %q (rust.mdc)", [script_name, needle])
}
```

- [ ] **Step 6: Запустити rego тест — переконатися, що passes**

Run: `cd npm && bun run lint-rego`
Expected: усі тести `rust.package_json_test.*` — pass

- [ ] **Step 7: Commit**

```bash
git add npm/rules/rust/policy/package_json/
git commit -m "feat(rust): package_json concern with substring rego"
```

---

## Task 3: `policy/vscode_extensions/` концерн (subset-of recommendations)

**Files:**
- Create: `npm/rules/rust/policy/vscode_extensions/target.json`
- Create: `npm/rules/rust/policy/vscode_extensions/template/extensions.json.snippet.json`
- Create: `npm/rules/rust/policy/vscode_extensions/vscode_extensions.rego`
- Create: `npm/rules/rust/policy/vscode_extensions/vscode_extensions_test.rego`

- [ ] **Step 1: Створити `target.json`**

```json
{
  "$schema": "https://unpkg.com/@nitra/cursor/schemas/target.json",
  "files": { "single": ".vscode/extensions.json" }
}
```

- [ ] **Step 2: Створити `template/extensions.json.snippet.json`**

```json
{ "recommendations": ["rust-lang.rust-analyzer", "tamasfe.even-better-toml"] }
```

- [ ] **Step 3: Написати failing test `vscode_extensions_test.rego`**

```rego
package rust.vscode_extensions_test

import data.rust.vscode_extensions
import rego.v1

template_data := {"snippet": {"recommendations": ["rust-lang.rust-analyzer", "tamasfe.even-better-toml"]}}

test_allow_with_both_extensions if {
	cfg := {"recommendations": ["rust-lang.rust-analyzer", "tamasfe.even-better-toml"]}
	count(vscode_extensions.deny) == 0 with input as cfg with data.template as template_data
}

test_allow_with_additional_extensions if {
	cfg := {"recommendations": ["dbaeumer.vscode-eslint", "rust-lang.rust-analyzer", "tamasfe.even-better-toml", "oxc.oxc-vscode"]}
	count(vscode_extensions.deny) == 0 with input as cfg with data.template as template_data
}

test_deny_missing_rust_analyzer if {
	cfg := {"recommendations": ["tamasfe.even-better-toml"]}
	some msg in vscode_extensions.deny with input as cfg with data.template as template_data
	contains(msg, "rust-lang.rust-analyzer")
}

test_deny_missing_even_better_toml if {
	cfg := {"recommendations": ["rust-lang.rust-analyzer"]}
	some msg in vscode_extensions.deny with input as cfg with data.template as template_data
	contains(msg, "tamasfe.even-better-toml")
}

test_deny_no_recommendations_field if {
	count(vscode_extensions.deny) > 0 with input as {} with data.template as template_data
}

# Drift test.
test_data_template_drives_check if {
	some msg in vscode_extensions.deny with input as {"recommendations": []}
		with data.template as {"snippet": {"recommendations": ["custom.ext"]}}
	contains(msg, "custom.ext")
}
```

- [ ] **Step 4: Запустити failing test**

Run: `cd npm && bun run lint-rego`
Expected: FAIL (`package rust.vscode_extensions не існує`)

- [ ] **Step 5: Створити `vscode_extensions.rego`**

```rego
# Перевірка `.vscode/extensions.json` для правила rust (rust.mdc).
#
# Канон надходить через --data: { "template": { "snippet": ... } }
# Структура --data сформована з template/extensions.json.snippet.json.
# Semantics: subset-of для recommendations — кожен з канону має бути присутнім,
# інші екстеншени дозволені.
package rust.vscode_extensions

import rego.v1

deny contains msg if {
	some rec in data.template.snippet.recommendations
	not rec in {r | some r in object.get(input, "recommendations", [])}
	msg := sprintf(".vscode/extensions.json: recommendations має містити %q (rust.mdc)", [rec])
}
```

- [ ] **Step 6: Запустити rego тест**

Run: `cd npm && bun run lint-rego`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add npm/rules/rust/policy/vscode_extensions/
git commit -m "feat(rust): vscode_extensions concern (subset-of rego)"
```

---

## Task 4: `policy/lint_rust_yml/` концерн (CI workflow)

**Files:**
- Create: `npm/rules/rust/policy/lint_rust_yml/target.json`
- Create: `npm/rules/rust/policy/lint_rust_yml/template/lint-rust.yml.snippet.yml`
- Create: `npm/rules/rust/policy/lint_rust_yml/lint_rust_yml.rego`
- Create: `npm/rules/rust/policy/lint_rust_yml/lint_rust_yml_test.rego`

- [ ] **Step 1: Створити `target.json`**

```json
{
  "$schema": "https://unpkg.com/@nitra/cursor/schemas/target.json",
  "files": { "single": ".github/workflows/lint-rust.yml" }
}
```

- [ ] **Step 2: Створити `template/lint-rust.yml.snippet.yml`**

```yaml
name: Lint Rust

on:
  push:
    branches:
      - dev
      - main
    paths:
      - '**/*.rs'
      - '**/Cargo.toml'
      - '**/Cargo.lock'
      - '**/rustfmt.toml'
      - '**/clippy.toml'

  pull_request:
    branches:
      - dev
      - main

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Rustfmt
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
```

- [ ] **Step 3: Написати failing test `lint_rust_yml_test.rego`**

```rego
package rust.lint_rust_yml_test

import data.rust.lint_rust_yml
import rego.v1

template_data := {"snippet": {"jobs": {"lint": {"steps": [
	{"uses": "actions/checkout@v6"},
	{"uses": "dtolnay/rust-toolchain@stable"},
	{"uses": "Swatinem/rust-cache@v2"},
	{"run": "cargo fmt --all -- --check"},
	{"run": "cargo clippy --all-targets --all-features -- -D warnings"},
]}}}}

canonical_wf := {"jobs": {"lint": {"steps": [
	{"uses": "actions/checkout@v6"},
	{"uses": "dtolnay/rust-toolchain@stable"},
	{"uses": "Swatinem/rust-cache@v2"},
	{"run": "cargo fmt --all -- --check"},
	{"run": "cargo clippy --all-targets --all-features -- -D warnings"},
]}}}

test_allow_canonical if {
	count(lint_rust_yml.deny) == 0 with input as canonical_wf with data.template as template_data
}

test_deny_missing_fmt_run if {
	wf := {"jobs": {"lint": {"steps": [
		{"uses": "actions/checkout@v6"},
		{"uses": "dtolnay/rust-toolchain@stable"},
		{"uses": "Swatinem/rust-cache@v2"},
		{"run": "cargo clippy --all-targets --all-features -- -D warnings"},
	]}}}
	some msg in lint_rust_yml.deny with input as wf with data.template as template_data
	contains(msg, "cargo fmt --all -- --check")
}

test_deny_missing_clippy_run if {
	wf := {"jobs": {"lint": {"steps": [
		{"uses": "actions/checkout@v6"},
		{"uses": "dtolnay/rust-toolchain@stable"},
		{"uses": "Swatinem/rust-cache@v2"},
		{"run": "cargo fmt --all -- --check"},
	]}}}
	some msg in lint_rust_yml.deny with input as wf with data.template as template_data
	contains(msg, "cargo clippy")
}

test_deny_missing_toolchain_uses if {
	wf := {"jobs": {"lint": {"steps": [
		{"uses": "actions/checkout@v6"},
		{"run": "cargo fmt --all -- --check"},
		{"run": "cargo clippy --all-targets --all-features -- -D warnings"},
	]}}}
	some msg in lint_rust_yml.deny with input as wf with data.template as template_data
	contains(msg, "dtolnay/rust-toolchain@stable")
}

test_deny_empty if {
	count(lint_rust_yml.deny) > 0 with input as {} with data.template as template_data
}

# Drift test.
test_data_template_drives_substring if {
	some msg in lint_rust_yml.deny with input as canonical_wf
		with data.template as {"snippet": {"jobs": {"lint": {"steps": [{"run": "custom-runner"}]}}}}
	contains(msg, "custom-runner")
}
```

- [ ] **Step 4: Запустити failing test**

Run: `cd npm && bun run lint-rego`
Expected: FAIL

- [ ] **Step 5: Створити `lint_rust_yml.rego`**

```rego
# Перевірка `.github/workflows/lint-rust.yml` для правила rust (rust.mdc).
#
# Канон надходить через --data: { "template": { "snippet": ... } }
# Структура --data сформована з template/lint-rust.yml.snippet.yml.
# Перевіряємо:
#   - `uses` з template (підмножина): actions/checkout@v6, dtolnay/rust-toolchain@stable,
#     Swatinem/rust-cache@v2 — кожен має бути присутнім серед steps.
#   - `run`-blob: усі рядки з template's run-steps мають бути присутні у конкатенації
#     run-кроків input'а (drift-safe — substring з template веде перевірку).
# Універсальні workflow-перевірки (concurrency, name, branches) — у `ga.workflow_common`.
package rust.lint_rust_yml

import rego.v1

# Усі `uses` з канону workflow.
expected_uses := {u |
	some step in data.template.snippet.jobs.lint.steps
	u := object.get(step, "uses", "")
	u != ""
}

# Конкатенація `run`-кроків з канону.
expected_run_blob := concat("\n", [r |
	some step in data.template.snippet.jobs.lint.steps
	r := object.get(step, "run", "")
	r != ""
])

# Усі `uses` з input workflow.
actual_uses := {u |
	some job in object.get(input, "jobs", {})
	some step in object.get(job, "steps", [])
	u := object.get(step, "uses", "")
	u != ""
}

# Конкатенація всіх `run`-кроків з input workflow.
all_run_text := concat("\n", [run_text |
	some job in object.get(input, "jobs", {})
	some step in object.get(job, "steps", [])
	run_text := step_run_to_text(step)
])

deny contains msg if {
	some required_use in expected_uses
	not required_use in actual_uses
	msg := sprintf("lint-rust.yml: відсутній step з `uses: %s` (rust.mdc)", [required_use])
}

deny contains msg if {
	some step in data.template.snippet.jobs.lint.steps
	expected_run := object.get(step, "run", "")
	expected_run != ""
	not contains(all_run_text, expected_run)
	msg := sprintf("lint-rust.yml: жоден крок run не містить %q (rust.mdc)", [expected_run])
}

step_run_to_text(step) := step.run if is_string(step.run)

else := concat("\n", [s | some s in step.run]) if is_array(step.run)

else := ""
```

- [ ] **Step 6: Запустити rego тест**

Run: `cd npm && bun run lint-rego`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add npm/rules/rust/policy/lint_rust_yml/
git commit -m "feat(rust): lint_rust_yml concern (CI workflow rego)"
```

---

## Task 5: `js/applies/` концерн (Cargo.toml gate)

**Files:**
- Create: `npm/rules/rust/js/applies/check.mjs`
- Create: `npm/rules/rust/js/applies/check.test.mjs`

- [ ] **Step 1: Написати failing test `check.test.mjs`**

```js
import { describe, expect, test } from 'bun:test'
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'

import { applies } from './check.mjs'

describe('rust applies', () => {
  /** @returns {string} */
  function makeRoot() {
    return mkdtempSync(join(tmpdir(), 'rust-applies-'))
  }

  test('true коли Cargo.toml у cwd', async () => {
    const root = makeRoot()
    const orig = process.cwd()
    writeFileSync(join(root, 'Cargo.toml'), '')
    process.chdir(root)
    try {
      expect(await applies()).toBe(true)
    } finally {
      process.chdir(orig)
      rmSync(root, { recursive: true, force: true })
    }
  })

  test('true коли Cargo.toml у src-tauri/', async () => {
    const root = makeRoot()
    const orig = process.cwd()
    mkdirSync(join(root, 'src-tauri'))
    writeFileSync(join(root, 'src-tauri', 'Cargo.toml'), '')
    process.chdir(root)
    try {
      expect(await applies()).toBe(true)
    } finally {
      process.chdir(orig)
      rmSync(root, { recursive: true, force: true })
    }
  })

  test('false коли немає Cargo.toml', async () => {
    const root = makeRoot()
    const orig = process.cwd()
    writeFileSync(join(root, 'package.json'), '{}')
    process.chdir(root)
    try {
      expect(await applies()).toBe(false)
    } finally {
      process.chdir(orig)
      rmSync(root, { recursive: true, force: true })
    }
  })
})
```

- [ ] **Step 2: Запустити failing test**

Run: `cd npm && bun test rules/rust/js/applies/check.test.mjs`
Expected: FAIL (`Cannot find module './check.mjs'`)

- [ ] **Step 3: Створити `check.mjs`**

```js
/**
 * Applies-гейт правила rust: маркер — наявність `Cargo.toml` у `cwd` або
 * в будь-якому workspace-підкаталозі (рекурсивний пошук з пропуском
 * `node_modules`, `.git`, `.next`, `.turbo`). Якщо повертає `false` —
 * `runStandardRule` пропускає всі концерни (JS і policy) цього правила.
 * `check()` друкує тільки context-pass; реальна робота — у policy-концернах.
 */
import { existsSync } from 'node:fs'

import { createCheckReporter } from '../../../../scripts/utils/check-reporter.mjs'

import { hasCargoTomlInTree } from '../../utils/has-cargo-toml.mjs'

const IGNORED_DIR_NAMES = new Set(['node_modules', '.git', '.next', '.turbo'])

/**
 * @returns {Promise<boolean>} `true` — правило застосовне; `false` — пропустити
 */
export async function applies() {
  if (existsSync('Cargo.toml')) return true
  return hasCargoTomlInTree(process.cwd(), IGNORED_DIR_NAMES)
}

/**
 * @returns {number} exit-код (0 — OK, 1 — порушення)
 */
export function check() {
  const reporter = createCheckReporter()
  reporter.pass('Знайдено Cargo.toml — застосовуємо правила rust.mdc')
  return reporter.getExitCode()
}
```

- [ ] **Step 4: Запустити тест — passes**

Run: `cd npm && bun test rules/rust/js/applies/check.test.mjs`
Expected: PASS (3/3)

- [ ] **Step 5: Commit**

```bash
git add npm/rules/rust/js/applies/
git commit -m "feat(rust): applies-gate концерн (Cargo.toml у workspace)"
```

---

## Task 6: `js/tooling/` концерн (orchestrator)

**Files:**
- Create: `npm/rules/rust/js/tooling/check.mjs`
- Create: `npm/rules/rust/js/tooling/check.test.mjs`

- [ ] **Step 1: Написати failing test `check.test.mjs`** (smoke — module loads, exports function)

```js
import { describe, expect, test } from 'bun:test'

import { check } from './check.mjs'

describe('rust tooling check', () => {
  test('експортує функцію check', () => {
    expect(typeof check).toBe('function')
  })
})
```

- [ ] **Step 2: Запустити failing test**

Run: `cd npm && bun test rules/rust/js/tooling/check.test.mjs`
Expected: FAIL (`Cannot find module './check.mjs'`)

- [ ] **Step 3: Створити `check.mjs`**

```js
/**
 * Перевіряє Rust-інструментарій (rust.mdc): FS-існування трьох канонічних
 * документів + делегування content-перевірок у rego через runConftestBatch.
 * Cross-file gating (applies) винесено у `js/applies/check.mjs`.
 *
 * Документи: `package.json`, `.vscode/extensions.json`,
 * `.github/workflows/lint-rust.yml`. Rego-полісі читають канонічні літерали
 * через `--data` (data.template.contains.* / data.template.snippet.*),
 * сформований з `policy/<concern>/template/`.
 */
import { existsSync } from 'node:fs'

import { createCheckReporter } from '../../../../scripts/utils/check-reporter.mjs'
import { runConftestBatch } from '../../../../scripts/utils/run-conftest-batch.mjs'

const DOCS = [
  { path: 'package.json', policyDir: 'rust/package_json', ns: 'rust.package_json' },
  { path: '.vscode/extensions.json', policyDir: 'rust/vscode_extensions', ns: 'rust.vscode_extensions' },
  { path: '.github/workflows/lint-rust.yml', policyDir: 'rust/lint_rust_yml', ns: 'rust.lint_rust_yml' }
]

/**
 * @returns {Promise<number>} 0 — все OK, 1 — є проблеми
 */
export async function check() {
  const reporter = createCheckReporter()
  const { pass, fail } = reporter

  for (const d of DOCS) {
    if (!existsSync(d.path)) {
      fail(`${d.path} не існує — створи з канонічним вмістом (rust.mdc)`)
      continue
    }
    const violations = runConftestBatch({
      policyDirRel: d.policyDir,
      namespace: d.ns,
      files: [d.path]
    })
    if (violations.length === 0) {
      pass(`${d.path} відповідає ${d.ns} (rego)`)
    } else {
      for (const v of violations) fail(v.message)
    }
  }

  return reporter.getExitCode()
}
```

- [ ] **Step 4: Запустити тест**

Run: `cd npm && bun test rules/rust/js/tooling/check.test.mjs`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add npm/rules/rust/js/tooling/
git commit -m "feat(rust): tooling concern (FS-existence + runConftestBatch)"
```

---

## Task 7: `rust.mdc` (людиночитна спека з template-links)

**Files:**
- Create: `npm/rules/rust/rust.mdc`

- [ ] **Step 1: Створити `rust.mdc`**

```markdown
---
description: Перевірка Rust коду
globs: "**/{Cargo.toml,Cargo.lock,rustfmt.toml,clippy.toml,.vscode/extensions.json,package.json},**/*.rs"
alwaysApply: false
version: '1.0'
---

**rustfmt** ([rust-lang/rustfmt](https://github.com/rust-lang/rustfmt)) — форматер; **clippy** ([rust-lang/rust-clippy](https://github.com/rust-lang/rust-clippy)) — лінтер. У скрипті **`lint-rust`** локально йдуть три кроки в одному рядку: `cargo fmt --all` → `cargo clippy --fix --allow-staged --allow-dirty --all-targets --all-features` → фінальний `cargo clippy --all-targets --all-features -- -D warnings`. У CI — без `--fix` (див. `lint-rust.yml`).

`cargo`, `rustfmt`, `clippy` не додавай у `devDependencies` — це Rust toolchain, ставиться через `rustup` локально або через `dtolnay/rust-toolchain@stable` у CI.

Канон `scripts.lint-rust` (substring requirement): [package.json.contains.json](./policy/package_json/template/package.json.contains.json)

У `.vscode/extensions.json` `recommendations` мають містити `rust-lang.rust-analyzer` і `tamasfe.even-better-toml`: [extensions.json.snippet.json](./policy/vscode_extensions/template/extensions.json.snippet.json)

Канон workflow `.github/workflows/lint-rust.yml`: [lint-rust.yml.snippet.yml](./policy/lint_rust_yml/template/lint-rust.yml.snippet.yml)

Перед **`./.github/actions/setup-bun-deps`** в інших workflows йде **`actions/checkout@v6`** (див. **ga.mdc**). У **lint-rust.yml** після checkout — `dtolnay/rust-toolchain@stable` (з `components: rustfmt, clippy`) + `Swatinem/rust-cache@v2` для кешу `~/.cargo` і `target/`.

## Композиція з Tauri

Tauri-проєкт завжди має `src-tauri/Cargo.toml`, тому правило `rust` активується автоматично разом з `tauri`. Поділ обов'язків:

- `rust` — `lint-rust` скрипт, `rust-analyzer`, `even-better-toml`, CI workflow.
- `tauri` — `tauri-apps.tauri-vscode` (див. **tauri.mdc**).

Обидва правила перевіряють `.vscode/extensions.json` за `contains`-семантикою; конкурентного запису немає.
```

- [ ] **Step 2: Перевірити, що markdown-лінки коректні**

Run: `ls npm/rules/rust/policy/package_json/template/package.json.contains.json npm/rules/rust/policy/vscode_extensions/template/extensions.json.snippet.json npm/rules/rust/policy/lint_rust_yml/template/lint-rust.yml.snippet.yml`
Expected: усі три файли існують

- [ ] **Step 3: Commit**

```bash
git add npm/rules/rust/rust.mdc
git commit -m "docs(rust): rust.mdc spec з markdown-links на template"
```

---

## Task 8: Інтеграція в `auto-rules.mjs` (Cargo.toml fact + 'rust' у AUTO_RULE_ORDER)

**Files:**
- Modify: `npm/scripts/auto-rules.mjs:30-55` (AUTO_RULE_ORDER), `:303-325` (updateFileFacts), `:495-525` (collectAutoRuleFacts return-type/init), `:640-660` (autoRuleChecks)
- Modify: `npm/scripts/tests/auto-rules.test.mjs:24-55` (expected list), додати fixture-сценарій

- [ ] **Step 1: Прочитати поточний стан тестів**

Run: `cd npm && bun test scripts/tests/auto-rules.test.mjs --bail`
Expected: усі тести passes (baseline)

- [ ] **Step 2: Додати `'rust'` у `ALL_RULES` фікстуру тесту**

`npm/scripts/tests/auto-rules.test.mjs` (рядки 11-36) — у масиві `ALL_RULES` додати між `'npm-module'` і `'security'`:

```js
  'npm-module',
  'rust',
  'security',
```

- [ ] **Step 3: Додати failing test-сценарії для rust**

У `npm/scripts/tests/auto-rules.test.mjs` додати після опису `describe('detectAutoRules', () => { ... })` нові тести (всередині describe-блоку, поряд з існуючими):

```js
  test('додає "rust" коли в дереві є Cargo.toml', async () => {
    await withTmpCwd(async () => {
      await writeJson('package.json', { name: 'p' })
      await ensureDir('src-tauri')
      await writeFile('src-tauri/Cargo.toml', '[package]\nname = "x"\n', 'utf8')

      const actual = await detectAutoRulesInCwd()

      expect(actual.rules).toContain('rust')
    })
  })

  test('НЕ додає "rust" коли Cargo.toml відсутній', async () => {
    await withTmpCwd(async () => {
      await writeJson('package.json', { name: 'p' })

      const actual = await detectAutoRulesInCwd()

      expect(actual.rules).not.toContain('rust')
    })
  })
```

- [ ] **Step 4: Запустити failing test**

Run: `cd npm && bun test scripts/tests/auto-rules.test.mjs --bail`
Expected: FAIL — тест `додає "rust" коли в дереві є Cargo.toml` fails, бо 'rust' ще не в `AUTO_RULE_ORDER` і немає facts/checks.

- [ ] **Step 5: Додати `'rust'` у `AUTO_RULE_ORDER`** у `npm/scripts/auto-rules.mjs`

Знайти список (приблизно рядки 30-55), додати між `'rego'` і `'security'`:

```js
  'rego',
  'rust',
  'security',
```

- [ ] **Step 6: Додати fact `hasCargoToml` у `updateFileFacts`**

У `npm/scripts/auto-rules.mjs` функція `updateFileFacts(fileName, relPath, facts)` (приблизно рядок 303), додати у кінець:

```js
  if (fileName === 'Cargo.toml') {
    facts.hasCargoToml = true
  }
```

У JSDoc типу parameter `facts` (рядок 293) додати поле:

```js
 *   hasCargoToml: boolean,
```

- [ ] **Step 7: Додати `hasCargoToml: false` у ініціалізацію facts у `collectAutoRuleFacts`**

Рядок ~511 у блоку `const facts = { ... }` додати:

```js
    hasCargoToml: false,
```

Алфавітне місце — між `hasCapacitorConfig` і `hasDockerfile`.

Також додати в JSDoc return-type (рядок ~494):

```js
 *   hasCargoToml: boolean,
```

- [ ] **Step 8: Додати `'rust'` у `autoRuleChecks`**

У `detectAutoRules` (рядок ~640) знайти `autoRuleChecks` масив, додати між `facts.hasRegoFile, id: 'rego'` і `facts.hasVueOrCssSource, id: 'style-lint'`:

```js
    { enabled: facts.hasRegoFile, id: 'rego' },
    { enabled: facts.hasCargoToml, id: 'rust' },
    { enabled: facts.hasVueOrCssSource, id: 'style-lint' }
```

- [ ] **Step 9: Запустити тести — passes**

Run: `cd npm && bun test scripts/tests/auto-rules.test.mjs`
Expected: усі тести pass, включно з новими `rust`-сценаріями

- [ ] **Step 10: Запустити повний test suite (sanity)**

Run: `cd npm && bun test`
Expected: усі тести pass

- [ ] **Step 11: Commit**

```bash
git add npm/scripts/auto-rules.mjs npm/scripts/tests/auto-rules.test.mjs
git commit -m "feat(auto-rules): додано auto-detect для rust (Cargo.toml fact)"
```

---

## Task 9: Refactor `tauri` — звузити vscode_extensions до tauri-only

**Files:**
- Modify: `npm/rules/tauri/policy/vscode_extensions/vscode_extensions.rego`
- Modify: `npm/rules/tauri/policy/vscode_extensions/vscode_extensions_test.rego`
- Modify: `npm/rules/tauri/tauri.mdc`
- Modify: `npm/rules/tauri/js/tooling/check.mjs` (текст fail-повідомлення про missing-extensions)

- [ ] **Step 1: Оновити tauri test — змінити required-set, оновити expected кейси**

`npm/rules/tauri/policy/vscode_extensions/vscode_extensions_test.rego` — переписати:

```rego
package tauri.vscode_extensions_test

import data.tauri.vscode_extensions
import rego.v1

test_allow_with_required_extension if {
	cfg := {"recommendations": ["tauri-apps.tauri-vscode"]}
	count(vscode_extensions.deny) == 0 with input as cfg
}

test_allow_with_additional_extensions if {
	cfg := {"recommendations": ["rust-lang.rust-analyzer", "tauri-apps.tauri-vscode", "tamasfe.even-better-toml"]}
	count(vscode_extensions.deny) == 0 with input as cfg
}

test_deny_missing_tauri_vscode if {
	cfg := {"recommendations": ["rust-lang.rust-analyzer"]}
	some msg in vscode_extensions.deny with input as cfg
	contains(msg, "tauri-apps.tauri-vscode")
}

test_deny_empty_recommendations if {
	count(vscode_extensions.deny) > 0 with input as {"recommendations": []}
}

# rust-lang.rust-analyzer більше НЕ обов'язковий для tauri.vscode_extensions:
# його вимагає правило rust (rust.mdc).
test_allow_without_rust_analyzer if {
	cfg := {"recommendations": ["tauri-apps.tauri-vscode"]}
	count(vscode_extensions.deny) == 0 with input as cfg
}
```

- [ ] **Step 2: Запустити tauri rego тести — failing**

Run: `cd npm && bun run lint-rego`
Expected: FAIL у `test_allow_without_rust_analyzer` (бо rego ще вимагає rust-analyzer)

- [ ] **Step 3: Звузити `vscode_extensions.rego` до tauri-only**

`npm/rules/tauri/policy/vscode_extensions/vscode_extensions.rego` — замінити `required_extensions`:

```rego
# Перевірка `.vscode/extensions.json` для tauri (tauri.mdc).
#
# Викликається з `rules/tauri/js/tooling/check.mjs` через `runConftestBatch` лише
# ПІСЛЯ того, як JS виявив маркер Tauri-проєкту (`src-tauri/` каталог,
# `tauri.conf.json` у будь-якому пакеті, або залежність `@tauri-apps/*`).
# Без `target.json` поруч (не auto-discoverable через `n-cursor fix`) — це
# conditional правило.
#
# Canonical (tauri.mdc): `recommendations` має містити `tauri-apps.tauri-vscode`.
# `rust-lang.rust-analyzer` і `tamasfe.even-better-toml` — вимагаються правилом
# `rust` (rust.mdc), бо Tauri-проєкт завжди має `src-tauri/Cargo.toml`.
package tauri.vscode_extensions

import rego.v1

required_extensions := {"tauri-apps.tauri-vscode"}

missing_extension_template := ".vscode/extensions.json: recommendations має містити %q (tauri.mdc)"

recommendations_set := {r | some r in object.get(input, "recommendations", [])}

deny contains msg if {
	some required in required_extensions
	not required in recommendations_set
	msg := sprintf(missing_extension_template, [required])
}
```

- [ ] **Step 4: Запустити tauri rego тести — passes**

Run: `cd npm && bun run lint-rego`
Expected: PASS

- [ ] **Step 5: Оновити `tauri.mdc`**

`npm/rules/tauri/tauri.mdc` — повний replace:

````markdown
---
description: Tauri
globs: "**/src-tauri/**,**/tauri.conf.json"
alwaysApply: false
version: '1.2'
---

У `.vscode/extensions.json` `recommendations` має містити `tauri-apps.tauri-vscode`:

```json title=".vscode/extensions.json"
{
  "recommendations": ["tauri-apps.tauri-vscode"]
}
```

Розширені Rust-вимоги (`rust-lang.rust-analyzer`, `tamasfe.even-better-toml`, скрипт `lint-rust`, CI `lint-rust.yml`) — у правилі **`rust`** (`n-rust.mdc`). Tauri-проєкт завжди має `src-tauri/Cargo.toml`, тому `rust` активується автоматично разом з `tauri`.
````

(version bumped до `1.2`).

- [ ] **Step 6: Оновити fail-повідомлення у `tauri/js/tooling/check.mjs`**

У `npm/rules/tauri/js/tooling/check.mjs` (рядки 73-75) — змінити рядок повідомлення:

Знайти:
```js
    fail(
      `${extPath} не існує — створи з recommendations "tauri-apps.tauri-vscode" і "rust-lang.rust-analyzer" (tauri.mdc)`
    )
```

Замінити на:
```js
    fail(`${extPath} не існує — створи з recommendations "tauri-apps.tauri-vscode" (tauri.mdc)`)
```

- [ ] **Step 7: Запустити всі тести — passes**

Run: `cd npm && bun test && bun run lint-rego`
Expected: усі pass

- [ ] **Step 8: Commit**

```bash
git add npm/rules/tauri/
git commit -m "refactor(tauri): звузити vscode_extensions до tauri-only

rust-lang.rust-analyzer і tamasfe.even-better-toml перенесено у нове правило rust (rust.mdc) — Tauri-проєкт завжди активує його через src-tauri/Cargo.toml."
```

---

## Task 10: Реєстрація `n-rust.mdc` у `CLAUDE.md`

**Files:**
- Modify: `/Users/vitaliytv/www/nitra/cursor/CLAUDE.md`

- [ ] **Step 1: Додати рядок у CLAUDE.md**

У `/Users/vitaliytv/www/nitra/cursor/CLAUDE.md` знайти список `@.cursor/rules/n-*.mdc` (алфавітний), додати між `@.cursor/rules/n-rego.mdc` і `@.cursor/rules/n-security.mdc`:

```
@.cursor/rules/n-rust.mdc
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude): зареєструвати n-rust.mdc у CLAUDE.md"
```

---

## Task 11: Верифікація на реальному `npx @nitra/cursor fix`

**Files:** (read-only)

- [ ] **Step 1: Прогнати unit-тести у пакеті**

Run: `cd npm && bun test`
Expected: усі pass (зокрема нові rust-тести)

- [ ] **Step 2: Прогнати rego-лінт**

Run: `cd npm && bun run lint-rego`
Expected: PASS, включно з усіма `rust.*_test` пакетами

- [ ] **Step 3: Прогнати full lint у root проєкту**

Run: `bun run lint`
Expected: PASS (knip + oxlint + eslint + jscpd чисто)

- [ ] **Step 4: Прогнати `npx @nitra/cursor fix rust` на тимчасовому Rust-fixture**

```bash
TMP=$(mktemp -d)
cd "$TMP"
echo '[package]\nname = "x"' > Cargo.toml
echo '{"name":"p","scripts":{"lint-rust":"cargo fmt --all && cargo clippy --fix --allow-staged --allow-dirty --all-targets --all-features && cargo clippy --all-targets --all-features -- -D warnings"}}' > package.json
mkdir -p .vscode && echo '{"recommendations":["rust-lang.rust-analyzer","tamasfe.even-better-toml"]}' > .vscode/extensions.json
mkdir -p .github/workflows
cp /Users/vitaliytv/www/nitra/cursor/npm/rules/rust/policy/lint_rust_yml/template/lint-rust.yml.snippet.yml .github/workflows/lint-rust.yml
npx /Users/vitaliytv/www/nitra/cursor/npm fix rust
```

Expected: exit code 0, повідомлення `✅` для всіх трьох документів

- [ ] **Step 5: Прогнати `npx @nitra/cursor fix rust` на «зламаному» fixture (без `-D warnings`)**

```bash
echo '{"name":"p","scripts":{"lint-rust":"cargo fmt --all && cargo clippy --fix --allow-staged --allow-dirty"}}' > package.json
npx /Users/vitaliytv/www/nitra/cursor/npm fix rust
```

Expected: exit code 1, повідомлення про відсутній `cargo clippy ... -D warnings` у scripts.lint-rust

- [ ] **Step 6: Прибрати fixture**

```bash
cd / && rm -rf "$TMP"
```

- [ ] **Step 7: Самоперевірка на самому `cursor`-репо**

Run: `cd /Users/vitaliytv/www/nitra/cursor && npx . fix`
Expected: усі правила pass (без `rust`, бо в репо немає `Cargo.toml`)

---

## Task 12: CHANGELOG + version bump (фінальний крок)

**Files:**
- Modify: `npm/package.json` (version)
- Modify: `npm/CHANGELOG.md` (нова секція)

- [ ] **Step 1: Прочитати поточну версію**

Run: `cat npm/package.json | grep '"version"'`
Запам'ятати поточну, інкрементувати **minor** (нове правило + breaking-narrowing у tauri).

- [ ] **Step 2: Bump version у `npm/package.json`**

Знайти рядок `"version": "<old>"` і замінити на `"version": "<new-minor>"` (наприклад `1.13.54` → `1.14.0`; точну версію визначити з поточного значення).

- [ ] **Step 3: Додати секцію в `npm/CHANGELOG.md`**

Додати на верх файлу (після заголовка-h1, якщо є):

```markdown
## <new-minor> — 2026-05-23

### Added
- Правило `rust` (rustfmt + clippy): канонічний `lint-rust` скрипт у `package.json`, CI workflow `lint-rust.yml` (`dtolnay/rust-toolchain@stable` + `Swatinem/rust-cache@v2`), VSCode-екстеншени `rust-lang.rust-analyzer` + `tamasfe.even-better-toml`. Auto-trigger — наявність `Cargo.toml` (`hasCargoToml` fact).

### Changed
- Правило `tauri` звужено: вимога `rust-lang.rust-analyzer` у `.vscode/extensions.json` перенесена у нове правило `rust`. Tauri-проєкт автоматично активує `rust` через `src-tauri/Cargo.toml`.
```

- [ ] **Step 4: Прогнати `npx @nitra/cursor fix changelog`**

Run: `cd npm && npx . fix changelog`
Expected: PASS (CHANGELOG-секція матчиться з версією)

- [ ] **Step 5: Final lint sanity**

Run: `cd npm && bun test && bun run lint-rego && cd .. && bun run lint`
Expected: усі pass

- [ ] **Step 6: Commit**

```bash
git add npm/package.json npm/CHANGELOG.md
git commit -m "release(npm): v<new-minor> — нове правило rust + narrow tauri.vscode_extensions"
```
