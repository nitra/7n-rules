---
bump: minor
section: Added
---
autonomous fix orchestrator: npx @nitra/cursor fix runs convergence-loop (T0-auto + LLM via pi) without agent involvement; meta.json orchestrator:true flag; _fix-check internal command; llm-worker C1 pattern with workspace-aware path extraction
