---
bump: minor
section: Changed
---
`doc-files` враховує revision рендеру в CRC, відхиляє непідтверджені file/config markers, рендерить повний коментарний контракт JS, Rust і Python без LLM, показує inline Rust tests без line-number шуму та дає local docgen достатній timeout.
