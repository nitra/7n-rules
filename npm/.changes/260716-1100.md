---
bump: patch
section: Fixed
---

`@e18e/eslint-plugin` додано у dependencies: канонічний `.oxlintrc.json` (jsPlugins) вимагає його у споживацьких репо, але пакет ніхто не постачав — на чистому CI oxlint падав «Failed to load JS plugin»; краш oxlint тепер показує stderr/exit-код замість «json не розпарсено»
