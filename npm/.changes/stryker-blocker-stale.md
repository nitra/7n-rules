---
bump: patch
section: Changed
---

План уточнено: `test/stryker_config` більше не виняток, а єдиний справжній залишок портування — його блокер («napi-міст будує `FixRequest::files` лише з полів `file` violations») застарів ще до того, як план його записав. §2.95 реєстру прямо каже, що порт більше не заблокований формою мосту; у коді це `crates/rules-napi/src/lib.rs:1287-1300`.

Записано й причину, чому це не спіймалось: таблиця `FIX_STAYS_IN_JS` машинно звіряється за складом, але її *причини* — вільний текст, який не звіряється ні з чим.
