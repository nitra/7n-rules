---
bump: patch
section: Changed
---

План завершення міграції перезібрано з машинних джерел: залишок fix-міграції — **нуль**, борг «спершу парність» — теж нуль. Одинадцять JS-фіксерів, що лишились, це чотири LLM-воркери й сім задокументованих винятків із названою причиною на кожен.

Дві попередні редакції таблиці (69→12, потім 42→2) були хибні однаково: рахували гілки `detect` за гілки `fix`. Ця зібрана з машинно звірюваної `FIX_STAYS_IN_JS` і доккоментарів гостей.

Зафіксовано вимкнення всіх 15 воркфлоу на час міграції та умову їх повернення.
