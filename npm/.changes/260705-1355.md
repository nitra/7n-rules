---
bump: patch
section: Changed
---
rule_meta: rule-level main.json.lint скасовано — заборона залишкового поля (lint-scope декларується у `<concern>/concern.json#lint`); прибрано parseRuleLintSpec і поле lint зі схеми rule-meta.json; поле lint видалено з main.json 14 правил; npm_publish_yml template приведено до канону persist-credentials:false + explicit push auth; lint-тулзи (github-actionlint, jscpd, oxlint, v8r) перенесено з devDependencies у dependencies — вони викликаються через bunx у рантаймі lint, як eslint/knip
