name: n-rules release
description: Aggregate .changes/* into version bump + CHANGELOG, tag <name>@<version>, commit back

runs:
  using: composite
  steps:
    - name: Release (bump + CHANGELOG + tag)
      shell: bash
      run: npx @7n/rules release
