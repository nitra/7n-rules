---
type: JS Module
title: main.mjs
resource: npm/rules/doc-files/docgen-files-batch/main.mjs
docgen:
  crc: 5ad5a782
  model: openai-codex/gpt-5.5
  tier: cloud-avg
  score: 80
  issues: internal-name:generateOne,internal-name:runBatchPass,judge-refine:kept-original,judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Модуль добирає цільові файли для документації (`selectTargets`), перевіряє доступність batch-генерації (`nativeBatchAvailable`), запускає мережеву генерацію Markdown-документів через LLM (`runGenerationBatch`, `runDocFilesGenCli`), підтримує індекси `docs/` (`generateDirIndex`), прибирає документацію для відсутнього коду (`purgeOrphanedDocs`) і оновлює службові позначки документів (`runDocFilesStampCli`).

Він потрібен, щоб генерація документації поруч із кодом була безпечною для повторного запуску: модуль кешує стан у межах одного прогону, перехоплює помилки та завершує роботу fail-safe без викидання винятків назовні.

## Поведінка

`runDocFilesGenCli` запускає повний прогін генерації: прибирає сирітські документи через `purgeOrphanedDocs`, вибирає актуальні цілі через `selectTargets`, передає їх у `runGenerationBatch` і повертає exit-код за підсумком. Дані надходять із поточного дерева проєкту, а результатами стають оновлені Markdown-доки, індекси директорій і підсумкова статистика у stdout.

`selectTargets` тримає конвеєр збіжним: у звичайному режимі бере відсутні, застарілі або degraded-доки, але не нескінченно ретраїть той самий degraded-результат для незмінного джерела. Режим overwrite обходить це правило й обирає всі доступні цілі.

`runGenerationBatch` є спільним ядром для CLI і scoped-перевірок: спершу перевіряє доступність локального бекенда, потім обирає batch-шлях або послідовний фолбек. `nativeBatchAvailable` кешує перевірку native-аддона в межах прогону; якщо batch недоступний або заданий м’який дедлайн, виконання лишається послідовним. Для локального batch-бекенда використовується явний provider endpoint `http://127.0.0.1:8000/v1/`, щоб уникнути неявного вибору іншого адаптера.

Помилки не пробиваються назовні як винятки: вони класифікуються, накопичуються у статистиці й перетворюються на exit-коди. Завеликі запити пропускаються без retry, інфраструктурні збої рахуються як помилки, а системні збої можуть достроково зупинити послідовний прогін. Уже записані документи лишаються на диску, тому наступний запуск продовжує роботу за станом CRC.

`generateDirIndex` підтримує локальні індекси в директоріях `docs/` після генерації, штампування або очищення. `purgeOrphanedDocs` видаляє документацію без відповідного source-файлу, після чого оновлює або прибирає індекси й порожні `docs/`-директорії.

`runDocFilesStampCli` виконує детерміноване оновлення frontmatter для вже наявних документів без звернення до LLM. Він зберігає наявні поля моделі та якості, оновлює прив’язку до source/CRC і синхронізує індекси директорій.

## Публічний API

- selectTargets — Цілі генерації:
  - default      → застарілі (stale) АБО degraded-доки, які ще не доретраювали при цьому CRC;
  - `--overwrite` → усі.
Degraded-док отримує рівно ОДИН доретрай на версію джерела: після невдалого доретраю
(лишився degraded) штампується `retried: true` і його більше не чіпають до зміни джерела
(нова версія → CRC-mismatch → stale → лічильник скидається). Конвеєр сходиться без прапора.
- nativeBatchAvailable — Чи доступний native-аддон `@7n/llm-lib` для 2b-batch (T8, рішення Р). Викликає
`submitBatchImpl` з порожнім `items` — це не робить жодного LLM-виклику
(Rust-крейт повертає порожній результат до резолву моделі), лише перевіряє,
що napi-аддон завантажується. Zero-native споживачі (аддон не зібраний/не
підтримувана платформа) отримують `false` і йдуть у послідовний фолбек.
- generateDirIndex — Генерує/оновлює `index.md` у директорії `docs/` — OKF Directory Index із таблицею
всіх наявних doc-файлів у цій директорії. Не зачіпає `index.md` при відсутності
інших doc-файлів.
- purgeOrphanedDocs — Видаляє сирітські доки (source-файл не існує) і оновлює/прибирає index.md.
Якщо після видалення в docs/-директорії лишились тільки index.md або нічого — очищує її.
- runDocFilesGenCli — `doc-files gen` — згенерувати документацію для застарілих/відсутніх док.
- runGenerationBatch — Спільне ядро генерації: preflight локального бекенда → послідовний прогін
`targets` через `generateOne` з circuit-breaker'ом (K systemic-збоїв підряд →
abort) → підсумковий звіт. Перевикористовують і батч-CLI (`runDocFilesGenCli`),
і opportunistic lint-крок doc-files (scoped-набір змінених файлів).

`deadlineAt` (epoch ms): м'який дедлайн fix-pipeline — перед стартом КОЖНОГО
наступного файлу (перший стартує завжди) батч звіряється з дедлайном і, коли час
вийшов, завершується штатно з частковим прогресом. Той самий дедлайн прокидається
у generateDoc: per-call LLM-таймаути ріжуться під залишок бюджету, тож і файл
У ПРОЦЕСІ обривається на дедлайні (transient-помилка), а не живе батчем-зомбі
поверх наступного rung-а. Зроблене записано по одному файлу (durable, свіжий
CRC) — наступний прогін підбирає решту за CRC.
T8 (2b-batch, рішення Р): коли доступний native-аддон `@7n/llm-lib` (`nativeBatchAvailable`)
і рунг БЕЗ `deadlineAt` (fix-pipeline рунги лишаються на послідовному шляху —
там дедлайн підтримується), увесь `targets` іде ОДНИМ `submitBatch` через
`runBatchPass` замість цього циклу по одному файлу. Zero-native споживачі
(аддон не зібраний/платформа не підтримується) автоматично лишаються на
послідовному шляху нижче — жодної відмінності в CLI/skill/hook-контракті.
- runDocFilesStampCli — `doc-files stamp` — детерміновано (пере)штампувати frontmatter `source`+`crc`
у НАЯВНИХ доках без виклику LLM. Для міграції док, які ще не мають CRC.
Поля `model`, `tier` та якості (`score`/`issues`/`judgeModel`) при цьому зберігаються
з наявного frontmatter.

## Гарантії поведінки

- Перехоплює помилки і не пропускає винятків назовні (fail-safe).
- Кешує результати в межах одного прогону.
