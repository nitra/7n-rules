---
type: JS Module
title: check.mjs
resource: npm/rules/graphql/check.mjs
docgen:
  crc: 29825671
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  score: 100
---

Модуль виконує логіку перевірки правил у контексті прогону. Він може ініціювати перевірку правила або запускати повну оркестрацію, що включає завантаження конфігурації та перевірку білого списку, якщо він працює як окремий інструмент. Результат виконання визначається кодом виходу процесу. У межах одного прогону відбувається кешування даних.

## Поведінка

1. Викликається функція `run` для запуску перевірки правила.
2. Перевірка виконується у режимі бібліотеки, використовуючи контекст прогону.
3. Якщо код виконується як окремий інструмент командного рядка, запускається повна оркестрація правила.
4. Оркестрація включає завантаження конфігурації, перевірку білого списку та підсумок.
5. Результат виконання повертає код виходу процесу.

## Публічний API

run — виконує послідовність перевірок: застосовує правила, аналізує JS-занепокоєння, перевіряє політику та посилання MDC.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
- Кешує результати в межах одного прогону.
