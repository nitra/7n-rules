---
type: JS Module
title: main.mjs
resource: npm/rules/efes/package_json_shared/main.mjs
docgen:
  crc: 9e535b35
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 85
  issues: anchor-miss:(efes.mdc),surzhik,judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Огляд: Код виконує валідацію структури та залежностей у файлі `package.json`, забезпечуючи відповідність внутрішнім політикам команди, що ґрунтуються на конфігурації в `package.json`. Перевірка гарантує необхідність наявності залежності `@nitra/efes-shared`, яка є критичною для коректної роботи системи згідно з правилами `efes.mdc`. При відсутності `package.json` механізм виявляє цю відсутність, посилаючись на необхідність створення файлу з відповідними залежностями.

Поведінка:
1. Виконує перевірку відповідності політики для файлу `package.json`.
2. Якщо файл `package.json` відсутній, повідомляє про необхідність його створення та додати `devDependencies['@nitra/efes-shared']` у зgodі з правилами `efes.mdc`.
3. Повертає уніфікований результат перевірки, включаючи звіт про порушення, у якому також згадуються маркери повідомлень, визначені в `efes.mdc`.

## Поведінка

1. Виконує перевірку відповідності політики для файлу `package.json`.
2. Якщо файл `package.json` відсутній, інформує, що необхідно створити його та додати `devDependencies['@nitra/efes-shared']` відповідно до правил `efes.mdc`.
3. Повертає уніфікований результат перевірки з переліком виявлених порушень.

## Публічний API

lint — виявляє політики, пов'язані з певними аспектами коду.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
