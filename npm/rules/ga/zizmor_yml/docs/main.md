---
type: JS Module
title: main.mjs
resource: npm/rules/ga/zizmor_yml/main.mjs
docgen:
  crc: 6c1413d7
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 95
  issues: anchor-miss:(ga.mdc),judge:inaccurate:0.99
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Огляд
Модуль виконує перевірку конфігурації відповідно до правил, визначених у `ga.mdc`, використовуючи публічну функцію `lint`.

Поведінка
1. Перевіряє конфігурацію, викликаючи функцію `lint` згідно з правилами `ga.mdc`.
2. При виконанні перевірки ігноруються шляхи, що знаходяться в каталогах `.github` та `.git`.
3. При взаємодії з агентами використовуються маркери повідомлень, зазначені у `ga.mdc`.

## Поведінка

1. Викликає функцію lint для перевірки конфігурації згідно з правилом ga.mdc.
2. Перевіряє наявність файлу .github/zizmor.yml; у разі відсутності виводиться відповідне повідомлення.
3. При виконанні перевірки ігноруються шляхи, що знаходяться в каталогах .github та .git.

## Публічний API

lint — визначає політику щодо проблем, які можуть бути виявлені в коді.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
- Свідомо пропускає шляхи: `.github`, `.git`.
