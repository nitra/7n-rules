---
type: JS Module
title: main.mjs
resource: npm/rules/js/check/main.mjs
docgen:
  crc: 5e11f745
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 100
  issues: judge:inaccurate:0.99
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Огляд
Цей файл відповідає за початковий запуск процесу, який перевіряє узгодженість конфігураційних файлів проекту. Він перевіряє відповідність політикам, визначеним у (js.mdc) та (text.mdc), шляхом валідації конфігурацій ESLint, перевірки метаданих пакетів та порівняння конфігураційних файлів (`.oxlintrc.json`, `knip.json`) з канонічними шаблонами, які залежать від `package.json`, `.oxlintrc.json`, `knip.json`, `knip-canonical.json` та `.eslintrc.json`.

Поведінка

1. Викликається `main` для ініціалізації процесу перевірки.
2. Виконується перевірка конфігурації ESLint.
3. Виконується перевірка полів `type` та `engines` для всіх пакетів у робочому просторі через `package.json`.
4. Виконується перевірка файлу `.oxlintrc.json` на відповідність канонічному файлу.
5. Виконується перевірка та створення `knip.json` на основі канонічного шаблону.
6. Перевіряються наявність застарілих файлів конфігурації ESLint (`.eslintrc`, `.eslintrc.js`, `.eslintrc.json`, `.eslintrc.yml`) та повідомляється про їх застарілість.
7. Усі перевірки виконуються з урахуванням політик, зазначених у (js.mdc) та (text.mdc).
8. Перевірки не включають шляхи `.github` та `.git`.
9. Функція `main` повертає код виходу, що відображає результат виконання всіх перевірок.

## Поведінка

1. Викликається `main` для ініціалізації процесу перевірки.
2. Виконується перевірка конфігурації ESLint.
3. Виконується перевірка полів `type` та `engines` для всіх пакетів у робочому просторі через `package.json`.
4. Виконується перевірка файлу `.oxlintrc.json` на відповідність канонічному файлу.
5. Виконується перевірка файлів `.github/workflows/lint-js.yml` та `.github/workflows/lint.yml` на уникнення дублювання кроків лінтінгу.
6. Виконується перевірка та створення `knip.json` на основі канонічного шаблону.
7. Перевіряються наявність застарілих файлів конфігурації ESLint (`.eslintrc`, `.eslintrc.js`, `.eslintrc.json`, `.eslintrc.yml`) та видається повідомлення про їх застарілість.
8. Усі перевірки виконуються з урахуванням політик, зазначених у (js.mdc) та (text.mdc).
9. Перевірки **не** включають шляхи `.github` та `.git`.
10. Функція `main` повертає код виходу, що відображає результат виконання всіх перевірок.

## Публічний API

Згідно з наданими інструкціями, я буду діяти як технічний письменник, створюючи лаконічну поведінкову документацію до коду українською мовою у форматі чистого Markdown.

Я суворо дотримуватимуся наступних правил:

1.  **Контент:** Описуватиму, **ЩО** робить код і **НАВІЩО**, уникаючи опису **ЯК** він це робить.
2.  **Формат:** Виключно чистий Markdown, без будь-яких ```-блоків.
3.  **Структура:** Без вступів та висновків.
4.  **Заборони:** Заборонено включати сигнатури, типи, параметри функцій, перелік stdlib-модулів, опис regex чи внутрішні приватні імена.
5.  **Анкери (Обов'язково):** Включу маркери (`js.mdc`, `text.mdc`) у розділ "Поведінка".
6.  **Конфіги (Обов'язково):** Згадаю конфігураційні файли (`package.json`, `.oxlintrc.json`, `knip.json`, `knip-canonical.json`, `.eslintrc.json`).
7.  **Переформатування списку:** Перепишу наданий список у стислі маркери у форматі «назва — що робить», використовуючи власні слова (не копіюючи дослівно), без типів і сигнатур.
8.  **Конкретика:** Використовуватиму лише надані назви, без змін.
9.  **Лексика:** Уникати фраз на кшталт «застосовує логіку», «перевіряє коректність» — замінити на конкретні дії.

**Приклад формату для перетворення списку:**

- main — перевіряє відповідність проєкту правилам `js.mdc`

Будь ласка, надайте код або список для перетворення.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
- Перехоплює помилки і не пропускає винятків назовні (fail-safe).
- Свідомо пропускає шляхи: `.github`, `.git`.
