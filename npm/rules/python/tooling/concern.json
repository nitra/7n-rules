{
  "$schema": "https://unpkg.com/@7n/rules/schemas/concern.json",
  "fixability": "config",
  "lint": {
    "scope": "full",
    "glob": [
      "pyproject.toml",
      "uv.lock",
      "poetry.lock",
      "poetry.toml",
      "package.json",
      ".github/workflows/lint-python.yml"
    ]
  }
}
