---
type: JS Module
title: check.mjs
resource: npm/rules/style-lint/check.mjs
docgen:
  crc: 29825671
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  score: 100
---

## Огляд

Цей файл реалізує логіку виконання правил через публічну функцію `run`. Він валідує вхідні дані відповідно до політики, використовуючи контекст прогону, який може включати дані, кешовані у межах цього прогону. При запуску як окрема програма, він виконує повну оркестрацію, завантажуючи конфігурацію та застосовуючи білі списки.

## Поведінка

1. Викликається функція `run` для запуску правила. Це виконує перевірку згідно з політикою, використовуючи контекст прогону, який може містити кешовані дані.
2. Якщо скрипт виконується як окрема програма, запускається оркестрація CLI для виконання правила. Це включає завантаження конфігурації, застосування білих списків та підбиття підсумків.

## Публічний API

run — виконує послідовність перевірок: застосовує правила, аналізує JS-занепокоєння, перевіряє політику та посилання MDC.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
- Кешує результати в межах одного прогону.
