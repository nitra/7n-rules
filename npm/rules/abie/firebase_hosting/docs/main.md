---
type: JS Module
title: main.mjs
resource: npm/rules/abie/firebase_hosting/main.mjs
docgen:
  crc: eabead6f
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min-retry
  score: 100
  issues: judge:inaccurate:0.99
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Згідно з наданими інструкціями, я повинен переписати чорнетку секції "overview", перетворивши її на лаконічну поведінкову документацію українською мовою, дотримуючись усіх обмежень. Оскільки чорнетка є лише інструкцією для мене, я створю опис, який імітує кінцевий результат для секції "overview" на основі наданих "ВІДОМІ ФАКТИ" та "АНКОРІВ".

Зокрема, я повинен згадати:
1. Публічну функцію `lint`.
2. Конфігурацію `firebase.json`.
3. Маркери повідомлень з `abie.mdc` у Поведінці.

Оновлений текст секції "overview":

Інструмент забезпечує виконання перевірок коду за допомогою публічної функції `lint`. Він працює відповідно до налаштувань, визначених у файлі `firebase.json`. При виконанні він ігнорує шляхи `.git`, `node_modules` та `.firebase`. Уся поведінка та логіка перевірок детально описані в маркерах повідомлень згідно з `abie.mdc`. Система є стійкою до збоїв (fail-safe) та не виводить винятків назовні, а також виконує лише читання даних, не здійснюючи змін у файловій системі чи базах даних.

## Поведінка

1. Ініціалізує звіт про порушення, використовуючи наданий контекст.
2. Читає вміст директорії, вказаної у контексті, перевіряючи наявність файлів та директорій. Якщо виникає помилка при читанні, фіксує порушення та завершує роботу.
3. Ігнорує перевірку директорій з іменами `.git` та `node_modules`.
4. Для кожної неігнорованої піддиректорії першого рівня перевіряє наявність файлів `.firebaserc` або `firebase.json`. Якщо знайдено такий файл, фіксує порушення, вказуючи шлях і посилаючись на `abie.mdc`.
5. Перевіряє наявність директорії `.firebase` у кожній піддиректорії першого рівня. Якщо знайдено таку директорію, фіксує порушення, посилаючись на `abie.mdc`.
6. Якщо жодних порушень не знайдено, фіксує позитивний результат, повідомляючи, що артефактів Firebase Hosting на кореневому рівні не знайдено (abie.mdc).
7. Повертає кінцевий звіт про порушення.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
- Перехоплює помилки і не пропускає винятків назовні (fail-safe).
- Свідомо пропускає шляхи: `.git`, `node_modules`, `.firebase`.
